/**
 * 共享的 Vue Test Utils stub 定义
 * 用于测试中统一替换 shadcn-vue / Radix Vue 的 portal、transition 等组件
 */

export const TeleportStub = {
  template: '<div><slot /></div>',
}

export const DialogPortalStub = {
  template: '<div><slot /></div>',
}

export const TransitionStub = {
  template: '<div><slot /></div>',
}
