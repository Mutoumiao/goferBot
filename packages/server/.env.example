# 服务端环境变量说明
#
# 项目已统一在根目录 .env.example 中维护完整的环境变量模板。
# 本地开发或部署时，请将根目录 .env.example 复制为根目录 .env。
#
# 服务端 ConfigModule 加载顺序：
#   packages/server/.env → 根目录 .env
# 即：根目录 .env 中的同名变量会覆盖 packages/server/.env 中的值。
#
# 如果你需要为服务端设置独立的覆盖值（例如服务端单独的数据库地址），
# 可以在此文件添加同名变量。否则请统一在根目录 .env 中维护。
#
# 配置中心迁移计划：
#   LLM / Embedding / RAG / Reranker 等模型配置将逐步迁移到数据库配置中心，
#   通过 UI 后台管理。详见 docs/guide/backend/configuration-guide.md
#
# 完整变量清单与注释请见：根目录 .env.example
