---
date: 2026-06-03
author: agent
scope: E2E 测试覆盖范围盘点
status: current
---

# E2E 测试覆盖表

总计 **75 个用例**，分布在 11 个测试文件中。

---

## 按模块分类

| 模块 | 测试文件 | 用例数 | 覆盖功能 |
|------|---------|--------|---------|
| **认证** | `specs/auth-ui.spec.ts` | 6 | 登录/注册页面渲染、表单校验、路由跳转、已登录重定向 |
| **聊天标签栏** | `specs/chat-tabs.spec.ts` | 5 | 标签初始化、新建标签、切换标签、首页不可关闭、重命名标签 |
| **聊天界面** | `specs/chat-ui.spec.ts` | 2 | 页面加载、多行文本输入 |
| **知识库选择器** | `specs/kb-selector.spec.ts` | 5 | @触发下拉、选择标签、多选标签、Escape关闭、删除标签 |
| **知识库管理** | `specs/knowledge-base-ui.spec.ts` | 7 | 列表加载、进入详情、右键菜单、外部关闭、删除确认/取消、确认删除移除 |
| **会话历史** | `specs/session-history.spec.ts` | 8 | 页面加载、会话列表、恢复对话、新建聊天、删除确认/取消/移除、重命名、显示标题时间 |
| **设置页面** | `specs/settings.spec.ts` | 8 | 页面加载、LLM Tab、切换Tab保持状态、保存触发API、保存失败提示、API Key输入框、Embedding Tab、温度滑块 |
| **聊天+RAG** | `flows/chat-with-rag.spec.ts` | 9 | 页面加载、发送消息、SSE流式响应、@触发选择器、选择标签、多选标签、删除标签、请求payload、SSE错误处理 |
| **入职旅程** | `flows/onboarding-journey-ui.spec.ts` | 4 | 登录后进入聊天、新建会话发消息、AI响应显示、无KB直接聊天 |
| **会话管理** | `flows/session-management.spec.ts` | 11 | 标签初始化/新建/切换/关闭、首页不可关闭、重命名、历史列表、恢复对话、删除确认、重命名历史、空历史提示 |
| **设置持久化** | `flows/settings-persist.spec.ts` | 8 | 页面加载、LLM Tab显示、修改API Key保存、切换默认提供商、刷新恢复、Embedding保存、温度保存、无效温度验证、空API Key保存 |

---

## 按功能域分类

| 功能域 | 用例数 | 覆盖点 |
|--------|--------|--------|
| **认证与授权** | 6 | 登录页、注册页、表单验证、路由守卫、Token重定向 |
| **聊天核心** | 15 | 页面加载、输入框、发送消息、消息显示、AI响应、多行文本、无KB聊天 |
| **标签/会话管理** | 16 | 新建、切换、关闭、重命名、首页保护、历史列表、恢复对话、删除 |
| **知识库** | 12 | 列表、详情、右键菜单、置顶、编辑、删除确认、@选择器、标签操作 |
| **设置** | 16 | LLM提供商、API Key、默认提供商、Embedding、温度参数、保存/恢复、验证错误 |
| **SSE/RAG** | 9 | 流式响应、错误处理、请求payload、知识库关联 |

---

## 详细用例清单

### specs/auth-ui.spec.ts（6 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | 登录页面元素完整 | 邮箱/密码/按钮/链接可见 |
| 2 | 注册页面元素完整 | 邮箱/密码/确认密码/按钮/链接可见 |
| 3 | 注册时表单验证失败显示字段错误 | 无效邮箱/短密码/不一致密码显示3个错误 |
| 4 | 点击注册链接跳转注册页 | 登录页→注册页路由 |
| 5 | 点击登录链接跳转登录页 | 注册页→登录页路由 |
| 6 | 已登录用户访问登录页重定向 | 有Token时访问/login→/app/chat |

### specs/chat-tabs.spec.ts（5 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | TC-F04-001: 初始状态仅显示首页标签 | 只有一个"首页"标签 |
| 2 | TC-F04-002: 新建标签创建新会话 | 点击+按钮新增标签 |
| 3 | TC-F04-003: 切换标签显示对应会话内容 | 点击标签切换active状态 |
| 4 | TC-F04-007: 首页标签不可关闭 | 首页无关闭按钮 |
| 5 | TC-F04-008: 重命名标签 | 双击标题编辑→回车确认 |

### specs/chat-ui.spec.ts（2 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | 聊天页面正常加载 | EmptySession或ChatInput可见 |
| 2 | 输入框支持多行文本 | textarea支持换行 |

### specs/kb-selector.spec.ts（5 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | TC-F11-001: @触发下拉显示知识库列表 | @显示2个KB选项 |
| 2 | TC-F11-002: 选择知识库显示标签 | 选择后显示pill标签 |
| 3 | TC-F11-003: 多选知识库显示多个标签 | 选择2个显示2个pill |
| 4 | TC-F11-005: 按Escape关闭下拉 | Escape关闭选择器 |
| 5 | TC-F11-006: 删除已选标签 | 点击pill删除按钮移除 |

### specs/knowledge-base-ui.spec.ts（7 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | 知识库列表页面正常加载 | 列表和创建按钮可见 |
| 2 | 点击知识库进入详情 | 点击进入文件浏览器 |
| 3 | 右键知识库弹出上下文菜单 | 右键显示置顶/编辑/删除 |
| 4 | 点击外部关闭右键菜单 | 点击body关闭菜单 |
| 5 | 删除知识库显示确认对话框 | 点击删除→确认对话框 |
| 6 | 确认删除后知识库从列表移除 | 确认后KB消失 |
| 7 | 取消删除对话框保持知识库 | 取消后KB仍在 |

### specs/session-history.spec.ts（8 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | 历史记录页面正常加载 | 列表可见 |
| 2 | 显示会话列表 | 2条历史记录 |
| 3 | 点击会话恢复对话 | 点击→跳转聊天页 |
| 4 | 新建聊天按钮跳转到首页 | 新建聊天按钮→/app/chat |
| 5 | 删除会话显示确认对话框 | 删除→确认对话框 |
| 6 | 确认删除后会话从列表移除 | 确认后移除 |
| 7 | 取消删除对话框保持会话 | 取消后保留 |
| 8 | 重命名会话更新显示 | 重命名→列表更新 |
| 9 | 会话列表显示标题和时间 | 显示"RAG 测试" |

### specs/settings.spec.ts（8 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | 设置页面正常加载 | Tab导航可见 |
| 2 | LLM提供商Tab显示正确 | OpenAI/Claude/DeepSeek可见 |
| 3 | 切换Tab保持状态 | 切换后输入值保留 |
| 4 | 保存设置触发API调用 | 保存→POST /api/settings |
| 5 | 保存失败显示错误提示 | 400错误→显示错误 |
| 6 | LLM提供商配置显示API Key输入框 | API Key输入框可见 |
| 7 | Embedding配置Tab显示 | Embedding Tab可见 |
| 8 | 温度参数滑块可调节 | 滑块可见 |

### flows/chat-with-rag.spec.ts（9 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | AC-01: 聊天页面正常加载 | 输入框+发送按钮 |
| 2 | AC-02: 发送消息显示在用户消息列表 | 发送"Hello SSE"可见 |
| 3 | AC-03: SSE流式响应显示AI回复 | AI消息"你好！"出现 |
| 4 | AC-04: @触发知识库选择器下拉 | @显示下拉 |
| 5 | AC-05: 选择知识库显示标签pill | 选择后显示pill |
| 6 | AC-06: 多选知识库显示多个标签 | 选择2个→2个pill |
| 7 | AC-07: 删除已选标签 | 删除pill→0个 |
| 8 | AC-08: 发送请求payload包含knowledgeBaseIds | 请求体包含KB IDs |
| 9 | AC-08b: SSE错误时显示错误提示 | 500错误→显示toast |

### flows/onboarding-journey-ui.spec.ts（4 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | AC-08: 新用户注册后进入聊天页面 | 已登录→聊天页 |
| 2 | AC-11: 新建会话并发送首条消息 | 发送消息→消息可见 |
| 3 | AC-12: 验证AI响应显示 | AI响应出现 |
| 4 | AC-13: 未创建KB直接聊天可用 | 无KB也能聊天 |

### flows/session-management.spec.ts（11 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | AC-09: 初始状态仅显示首页标签 | 1个标签，无关闭按钮 |
| 2 | AC-10: 新建标签创建新会话 | +按钮→2个标签 |
| 3 | AC-11: 切换标签显示对应会话内容 | 切换→data-active更新 |
| 4 | AC-12: 关闭非首页标签 | 关闭→标签减少 |
| 5 | AC-13: 首页标签不可关闭 | 首页无关闭按钮 |
| 6 | AC-14: 重命名标签更新显示 | 重命名→文本更新 |
| 7 | AC-15: 历史记录页面显示会话列表 | 历史页2条记录 |
| 8 | AC-16: 点击历史会话恢复对话 | 点击→跳转聊天页+消息列表 |
| 9 | AC-17: 删除历史会话显示确认对话框 | 删除→确认对话框 |
| 10 | AC-18: 重命名历史会话更新显示 | 重命名→列表更新 |
| 11 | AC-19: 空历史状态显示提示 | 空列表→"暂无对话历史" |

### flows/settings-persist.spec.ts（8 用例）

| # | 用例名称 | 说明 |
|---|---------|------|
| 1 | AC-01: 设置页面正常加载 | 模型设置/账户设置可见 |
| 2 | AC-02: LLM提供商Tab显示正确 | 5个Tab可见 |
| 3 | AC-03: 修改API Key并保存 | 修改→保存→请求体正确 |
| 4 | AC-04: 切换默认提供商 | 选DeepSeek→保存→defaultProvider更新 |
| 5 | AC-05: 刷新页面后设置恢复 | 保存→刷新→值保留 |
| 6 | AC-06: Embedding配置保存 | 配置→保存→请求体正确 |
| 7 | AC-07: 温度参数保存 | 设置温度→保存→请求体正确 |
| 8 | AC-14: 保存无效temperature显示验证错误 | temperature=2.5→保存→错误提示 |
| 9 | AC-15: 保存空API Key允许（非必填） | 空Key→保存→成功 |

---

## 未覆盖的功能域

| 功能域 | 说明 | 建议优先级 |
|--------|------|-----------|
| **文档上传** | 文件上传、解析进度、分块结果展示 | 高 |
| **向量检索可视化** | 检索结果、相似度分数、来源引用高亮 | 中 |
| **移动端适配** | 响应式布局、触摸交互、侧边栏折叠 | 低 |
| **深色模式** | 主题切换、颜色token正确性 | 低 |
| **离线/网络异常** | 断网提示、重连机制、请求队列 | 中 |
| **多语言** | i18n切换、文本完整性 | 低 |
| **性能/加载** | 首屏加载时间、大数据量消息渲染 | 中 |
| **文件预览** | PDF/图片预览、Markdown渲染 | 中 |
| **用户资料** | 头像上传、昵称修改、密码修改 | 低 |

---

## 相关文档

- `docs/guide/testing/e2e-testing-guide.md` — E2E 测试规范
- `tests/e2e/pages/` — Page Object 目录
- `tests/e2e/mocks/http-routes.ts` — Mock API 路由定义
