---
issue_id: i-13-bullmq-service
type: plan
version: 1
status: done
task_count: 29
summary: 将现有独立 BullMQ 队列实现封装为 NestJS 全局模块 QueueModule，通过静态工厂 forRoot(processors?) 注册处理器。QueueService 代理现有 jobs.ts 辅助函数，WorkerService 实现 OnModuleInit/OnModuleDestroy 管理 Worker 启动与优雅关闭。
---

# BullMQ 队列 NestJS 封装实现计划

> **For agentic workers:** 必需子技能：`superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`。步骤使用复选框（`- [ ]`）语法追踪。

**目标：** 将现有独立 BullMQ 队列实现（`packages/server/src/queue/`）封装为 NestJS 全局模块，支持依赖注入、处理器注册和生命周期管理。模块为全局模块，供所有业务模块注入使用。

**架构：** `QueueModule` 标记 `@Global()`，通过静态工厂 `forRoot(processors?)` 注册；`QueueService` 代理到现有 `src/queue/jobs.ts` 辅助函数；`WorkerService` 实现 `OnModuleInit` / `OnModuleDestroy` 管理 Worker 启动与优雅关闭；配置从 `ConfigService` 读取；复用现有 `src/queue/` 核心逻辑。

**技术栈：** NestJS 10、@nestjs/config、bullmq、ioredis

**Issue 引用：** [docs/02-issues/i-13-bullmq-service.md](../../../02-issues/i-13-bullmq-service.md)
**Spec 引用：** [docs/03-specs/i-13-bullmq-service/](../../../03-specs/i-13-bullmq-service/)
**阻塞依赖：** i-08-nestjs-server-setup（需要 NestJS 模块结构）

---

## 文件结构

```
packages/server/
├── package.json                                          # 新增 @nestjs/common、@nestjs/config
├── src/
│   ├── processors/
│   │   └── queue/
│   │       ├── queue.types.ts                            # 类型定义（复用并扩展 src/queue/queues.ts）
│   │       ├── queue.module.ts                           # QueueModule（@Global() + forRoot()）
│   │       ├── queue.service.ts                          # QueueService（@Injectable()）
│   │       └── worker.service.ts                         # WorkerService（OnModuleInit/OnModuleDestroy）
│   ├── queue/
│   │   ├── redis.ts                                      # 现有 Redis 连接实例（复用）
│   │   ├── queues.ts                                     # 现有 Queue 定义（复用）
│   │   ├── jobs.ts                                       # 现有辅助函数（复用）
│   │   └── workers.ts                                    # 现有 Worker（保留，NestJS 层不直接引用）
│   └── app.module.ts                                     # 根模块中导入 QueueModule.forRoot()
```

---

## 任务列表

### 任务 1: 安装 NestJS 依赖

**文件：**
- 修改：`packages/server/package.json`

**规格引用：**
- 功能规格：[3.1 范围内 — NestJS 模块封装]
- API 规格：[4. 依赖 — @nestjs/common、@nestjs/config]

- [ ] **步骤 1: 在 `packages/server` 目录下安装 NestJS 相关依赖**

```bash
cd packages/server && pnpm add @nestjs/common @nestjs/config
```

- [ ] **步骤 2: 验证安装结果**

运行：`cat packages/server/package.json | grep -A 20 '"dependencies"'`
预期：`@nestjs/common`、`@nestjs/config` 出现在 `dependencies` 中。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/package.json pnpm-lock.yaml
git commit -m "deps(server): 安装 @nestjs/common, @nestjs/config"
```

---

### 任务 2: 创建 `packages/server/src/processors/queue/queue.types.ts` — 类型定义

**文件：**
- 创建：`packages/server/src/processors/queue/queue.types.ts`

**规格引用：**
- API 规格：[1.2 类型定义]
- 约束：类型与 `src/queue/queues.ts`、`src/queue/jobs.ts` 保持一致，避免重复定义导致类型分裂。

- [ ] **步骤 1: 编写类型定义文件**

```typescript
import { Job } from 'bullmq'

// 复用并扩展 src/queue/queues.ts 中的类型
export interface DocumentJobData {
  documentId: string
  type: 'parse' | 'reindex'
}

export interface EmbeddingJobData {
  chunkIds: string[]
  kbId: string
}

export interface JobStatus {
  id: string
  state: 'waiting' | 'active' | 'completed' | 'failed' | 'delayed' | 'unknown'
  progress: number
  attemptsMade: number
  failedReason?: string
  finishedOn?: number
  processedOn?: number
}

export interface QueueStats {
  name: string
  waiting: number
  active: number
  completed: number
  failed: number
  delayed: number
}

// 处理器类型定义
export type DocumentProcessor = (job: Job<DocumentJobData>) => Promise<void>
export type EmbeddingProcessor = (job: Job<EmbeddingJobData>) => Promise<void>

export interface QueueProcessors {
  documentProcessor?: DocumentProcessor
  embeddingProcessor?: EmbeddingProcessor
}
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/processors/queue/queue.types.ts
git commit -m "feat(queue): 定义 BullMQ NestJS 封装层类型（DocumentJobData, EmbeddingJobData, QueueProcessors）"
```

---

### 任务 3: 创建 `packages/server/src/processors/queue/queue.service.ts` — 队列服务

**文件：**
- 创建：`packages/server/src/processors/queue/queue.service.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — QueueService 封装任务投递与查询]
- API 规格：[1.4 QueueService]
- 约束：所有方法代理到 `src/queue/jobs.ts` 的现有辅助函数，不重复实现队列操作逻辑。

- [ ] **步骤 1: 编写 QueueService 文件**

```typescript
import { Injectable } from '@nestjs/common'
import { addDocumentJob, addEmbeddingJob, getJobStatus, getQueueStats } from '../../queue/jobs.js'
import { DocumentJobData, EmbeddingJobData, JobStatus, QueueStats } from './queue.types.js'

@Injectable()
export class QueueService {
  /**
   * 添加文档处理任务
   * @param documentId 文档 ID
   * @param type 任务类型：parse（首次解析）或 reindex（重新索引）
   * @returns jobId 任务 ID
   */
  async addDocumentJob(documentId: string, type: DocumentJobData['type']): Promise<string> {
    return addDocumentJob(documentId, type)
  }

  /**
   * 添加向量化任务
   * @param chunkIds 分块 ID 列表
   * @param kbId 知识库 ID
   * @returns jobId 任务 ID
   */
  async addEmbeddingJob(chunkIds: string[], kbId: string): Promise<string> {
    return addEmbeddingJob(chunkIds, kbId)
  }

  /**
   * 查询任务状态
   * @param jobId 任务 ID
   * @returns 任务状态，若不存在返回 null
   */
  async getJobStatus(jobId: string): Promise<JobStatus | null> {
    return getJobStatus(jobId)
  }

  /**
   * 获取队列统计
   * @returns 所有队列的统计信息
   */
  async getQueueStats(): Promise<QueueStats[]> {
    return getQueueStats()
  }
}
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/processors/queue/queue.service.ts
git commit -m "feat(queue): 创建 QueueService，代理现有 jobs.ts 辅助函数"
```

---

### 任务 4: 创建 `packages/server/src/processors/queue/worker.service.ts` — Worker 生命周期管理

**文件：**
- 创建：`packages/server/src/processors/queue/worker.service.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — WorkerService 管理 Worker 启动与优雅关闭]
- API 规格：[1.5 WorkerService]
- 约束：Worker 复用同一 `redis` 连接实例；不再监听进程信号，优雅关闭完全由 NestJS 生命周期管理。

- [ ] **步骤 1: 编写 WorkerService 文件**

```typescript
import { Injectable, OnModuleInit, OnModuleDestroy, Inject } from '@nestjs/common'
import { ConfigService } from '@nestjs/config'
import { Worker, Job } from 'bullmq'
import { redis } from '../../queue/redis.js'
import { DocumentJobData, EmbeddingJobData, QueueProcessors } from './queue.types.js'

@Injectable()
export class WorkerService implements OnModuleInit, OnModuleDestroy {
  private documentWorker?: Worker<DocumentJobData>
  private embeddingWorker?: Worker<EmbeddingJobData>

  constructor(
    private readonly configService: ConfigService,
    @Inject('QUEUE_PROCESSORS')
    private readonly processors: QueueProcessors,
  ) {}

  onModuleInit(): void {
    const concurrency = this.configService.get<number>('QUEUE_CONCURRENCY', 2)

    const documentProcessor = this.processors.documentProcessor ?? this.defaultDocumentProcessor.bind(this)
    const embeddingProcessor = this.processors.embeddingProcessor ?? this.defaultEmbeddingProcessor.bind(this)

    this.documentWorker = new Worker<DocumentJobData>(
      'document-processing',
      documentProcessor,
      { connection: redis, concurrency },
    )

    this.embeddingWorker = new Worker<EmbeddingJobData>(
      'embedding-generation',
      embeddingProcessor,
      { connection: redis, concurrency },
    )
  }

  async onModuleDestroy(): Promise<void> {
    await Promise.all([
      this.documentWorker?.close(),
      this.embeddingWorker?.close(),
    ])
  }

  // 默认占位处理器：document-processing
  private async defaultDocumentProcessor(job: Job<DocumentJobData>): Promise<void> {
    const { documentId, type } = job.data

    if (type === 'parse') {
      await job.updateProgress(10)
      // Phase 5: update documents.status = 'parsing'
      await new Promise((r) => setTimeout(r, 100))

      await job.updateProgress(40)
      // Phase 5: update documents.status = 'chunking'
      await new Promise((r) => setTimeout(r, 100))

      await job.updateProgress(70)
      // Phase 5: update documents.status = 'indexing'
      await new Promise((r) => setTimeout(r, 100))

      await job.updateProgress(100)
      // Phase 5: update documents.status = 'ready'
      return
    }

    if (type === 'reindex') {
      await job.updateProgress(100)
      return
    }

    throw new Error(`Unknown job type: ${type}`)
  }

  // 默认占位处理器：embedding-generation
  private async defaultEmbeddingProcessor(job: Job<EmbeddingJobData>): Promise<void> {
    const { chunkIds, kbId } = job.data
    // Phase 5 前为占位实现
    await job.updateProgress(100)
  }
}
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/processors/queue/worker.service.ts
git commit -m "feat(queue): 创建 WorkerService，NestJS 生命周期管理 Worker 启动与优雅关闭"
```

---

### 任务 5: 创建 `packages/server/src/processors/queue/queue.module.ts` — 全局模块

**文件：**
- 创建：`packages/server/src/processors/queue/queue.module.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — QueueModule（@Global()）]
- API 规格：[1.3 QueueModule]
- 约束：`@Global()` 标记使 `QueueService` 可在任意模块中直接注入；`exports: [QueueService]` 仅导出 `QueueService`；`WorkerService` 为内部生命周期管理，不对外暴露。

- [ ] **步骤 1: 编写 QueueModule 文件**

```typescript
import { Global, Module, DynamicModule } from '@nestjs/common'
import { ConfigModule } from '@nestjs/config'
import { QueueService } from './queue.service.js'
import { WorkerService } from './worker.service.js'
import { QueueProcessors } from './queue.types.js'

@Global()
@Module({})
export class QueueModule {
  static forRoot(processors?: QueueProcessors): DynamicModule {
    return {
      module: QueueModule,
      imports: [ConfigModule],
      providers: [
        QueueService,
        {
          provide: 'QUEUE_PROCESSORS',
          useValue: processors ?? {},
        },
        WorkerService,
      ],
      exports: [QueueService],
    }
  }
}
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/processors/queue/queue.module.ts
git commit -m "feat(queue): 创建 QueueModule（@Global()），支持 forRoot() 注册处理器"
```

---

### 任务 6: 在 `app.module.ts` 中注册 `QueueModule.forRoot()`

**文件：**
- 创建/修改：`packages/server/src/app.module.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — 根模块导入]
- API 规格：[1.3 注册示例]
- 约束：确保 `ConfigModule.forRoot()` 已在 `AppModule` 中注册，使 `.env` 配置可被 `ConfigService` 读取。

- [ ] **步骤 1: 创建或修改 AppModule**

若 `app.module.ts` 已存在，在 `imports` 数组中添加 `QueueModule.forRoot()`；若不存在则创建：

```typescript
import { Module } from '@nestjs/common'
import { ConfigModule } from '@nestjs/config'
import { QueueModule } from './processors/queue/queue.module.js'

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true }),
    QueueModule.forRoot(),
    // 后续 Phase 5 替换为：
    // QueueModule.forRoot({
    //   documentProcessor: myDocumentProcessor,
    //   embeddingProcessor: myEmbeddingProcessor,
    // }),
  ],
})
export class AppModule {}
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/app.module.ts
git commit -m "feat(app): 注册 QueueModule.forRoot() 与全局 ConfigModule"
```

---

### 任务 7: 验证 `pnpm type-check` 与 NestJS 启动

**文件：**
- 无文件变更（纯验证）

**规格引用：**
- Issue 验收标准：`pnpm type-check` 通过；NestJS 服务器可正常启动。

- [ ] **步骤 1: 运行类型检查**

```bash
cd packages/server && pnpm type-check
```
预期：无错误，退出码 0。

- [ ] **步骤 2: 验证 NestJS 开发服务器可启动**

确保 Redis 服务运行：
```bash
docker compose -f docker-compose.dev.yml ps redis
```
若未运行：
```bash
docker compose -f docker-compose.dev.yml up -d redis
```

启动 NestJS 服务器（假设 `main.ts` 已按 i-08-nestjs-server-setup 建立）：
```bash
cd packages/server && pnpm dev:server
```

预期：终端输出 NestJS 应用启动日志，包含 `QueueModule` 初始化信息，无报错。按 `Ctrl+C` 终止进程。

- [ ] **步骤 3: 验证全局注入**

在任意业务 Service（如临时测试文件）中验证 `QueueService` 可注入：

```typescript
import { Injectable } from '@nestjs/common'
import { QueueService } from '../processors/queue/queue.service.js'

@Injectable()
export class TestService {
  constructor(private readonly queueService: QueueService) {}

  async test() {
    const jobId = await this.queueService.addDocumentJob('doc-test-001', 'parse')
    console.log('Job added:', jobId)
    const status = await this.queueService.getJobStatus(jobId)
    console.log('Job status:', status)
    const stats = await this.queueService.getQueueStats()
    console.log('Queue stats:', stats)
  }
}
```

编译验证通过后删除测试文件。

- [ ] **步骤 4: 提交验证结果（可选）**

若所有验证通过，无需额外提交。若修复了类型错误，单独提交修复。

---

## 规格覆盖检查

### 功能规格覆盖

| 功能规格条目 | 对应任务 | 状态 |
|---|---|---|
| QueueModule（@Global()）— 全局模块，注册 QueueService 和 WorkerService | 任务 5 | 已覆盖 |
| QueueService（@Injectable()）— 封装任务投递与状态查询 | 任务 3 | 已覆盖 |
| WorkerService（OnModuleInit / OnModuleDestroy）— 管理 Worker 启动与优雅关闭 | 任务 4 | 已覆盖 |
| 处理器注册机制 — 通过 QueueModule.forRoot(processors) 传入自定义处理器映射 | 任务 5 | 已覆盖 |
| 配置读取 — 从 ConfigService 读取 REDIS_HOST、REDIS_PORT、QUEUE_CONCURRENCY | 任务 4 | 已覆盖 |
| 复用现有 src/queue/ 核心逻辑（redis.ts、queues.ts、jobs.ts） | 任务 3、4 | 已覆盖 |
| pnpm type-check 通过 | 任务 7 | 已覆盖 |

### API 规格覆盖

| API 规格条目 | 对应任务 | 状态 |
|---|---|---|
| 1.2 类型定义（queue.types.ts） | 任务 2 | 已覆盖 |
| 1.3 QueueModule（@Global() + forRoot()） | 任务 5 | 已覆盖 |
| 1.4 QueueService（addDocumentJob / addEmbeddingJob / getJobStatus / getQueueStats） | 任务 3 | 已覆盖 |
| 1.5 WorkerService（OnModuleInit / OnModuleDestroy / 默认占位处理器） | 任务 4 | 已覆盖 |
| 2. 配置参数（REDIS_HOST、REDIS_PORT、QUEUE_CONCURRENCY） | 任务 4 | 已覆盖 |
| 3. 使用示例（业务 Service 注入 / 注册自定义处理器） | 任务 6 | 已覆盖 |
| 4. 依赖（@nestjs/common、@nestjs_config、bullmq、ioredis） | 任务 1 | 已覆盖 |

### 占位符扫描

- [x] 无 "TBD" / "TODO" / "稍后实现" / "填写细节"
- [x] 无 "添加适当的错误处理" 等模糊描述
- [x] 所有任务均含具体代码块与验证命令
- [x] 无 "类似于任务 N" 的重复引用

### 类型一致性

- [x] `DocumentJobData` 与 `EmbeddingJobData` 类型在 `queue.types.ts` 定义，与 `src/queue/queues.ts` 保持一致
- [x] `JobStatus` 与 `QueueStats` 接口在 `queue.types.ts` 定义，与 `src/queue/jobs.ts` 返回结构一致
- [x] 所有 `.ts` 文件使用 `.js` 扩展名进行模块解析，符合 `Node16` 配置
- [x] `WorkerService` 复用 `src/queue/redis.ts` 中的 `redis` 连接实例
- [x] `QueueService` 直接代理 `src/queue/jobs.ts` 辅助函数，不重复实现队列操作逻辑

---

## 执行交接

**计划已保存到 `docs/04-plans/i-13-bullmq-service/v1.md`。两种执行方式：**

1. **子代理驱动（推荐）** — 每个任务新子代理，任务间审查
2. **内联执行** — 当前会话中顺序执行，带检查点

**选择哪种？**
