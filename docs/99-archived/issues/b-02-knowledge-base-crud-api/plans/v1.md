---
issue_id: b-02-knowledge-base-crud-api
type: plan
version: 1
status: done
task_count: 24
summary: 实现知识库和虚拟文件夹的 8 个 CRUD 端点，包含认证、用户隔离、Zod 校验、级联删除。基于 NestJS 模块化设计，KnowledgeBaseModule 内聚 Service + Controller，通过 JwtAuthGuard + Prisma 处理级联关系。
---

# 知识库 CRUD API 实现计划

> **For agentic workers:** 必需子技能：superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans。步骤使用复选框（`- [ ]`）语法追踪。

**目标：** 实现知识库和虚拟文件夹的 8 个 CRUD 端点，包含认证、用户隔离、Zod 校验、级联删除。

**架构：** NestJS 模块化设计 — `KnowledgeBaseModule` 包含 Service + Controller，Folder 逻辑内聚在同一模块。通过 `JwtAuthGuard` + `@CurrentUser()` 获取用户 ID，Prisma 处理级联关系。

**技术栈：** NestJS 10 + Fastify + Prisma 5 + Zod + nestjs-zod + Passport JWT

**Issue 引用：** [docs/02-issues/b-02-knowledge-base-crud-api.md](../../02-issues/b-02-knowledge-base-crud-api.md)
**Spec 引用：** [docs/03-specs/b-02-knowledge-base-crud-api/](../../03-specs/b-02-knowledge-base-crud-api/)
**测试用例：** [docs/08-test-cases/b-02/](../../08-test-cases/b-02/)（待创建）

---

## 文件结构

| 文件 | 操作 | 说明 |
|------|------|------|
| `packages/server/src/modules/knowledge-base/knowledge-base.module.ts` | 创建 | 模块定义 |
| `packages/server/src/modules/knowledge-base/knowledge-base.service.ts` | 创建 | 知识库 + 文件夹业务逻辑 |
| `packages/server/src/modules/knowledge-base/knowledge-base.controller.ts` | 创建 | 知识库 HTTP 端点 |
| `packages/server/src/modules/knowledge-base/folder.controller.ts` | 创建 | 文件夹 HTTP 端点 |
| `packages/server/src/modules/knowledge-base/dto/create-kb.dto.ts` | 创建 | 创建知识库 DTO |
| `packages/server/src/modules/knowledge-base/dto/update-kb.dto.ts` | 创建 | 更新知识库 DTO |
| `packages/server/src/modules/knowledge-base/dto/create-folder.dto.ts` | 创建 | 创建文件夹 DTO |
| `packages/server/src/modules/knowledge-base/dto/update-folder.dto.ts` | 创建 | 更新文件夹 DTO |
| `packages/server/src/app.module.ts` | 修改 | 导入 KnowledgeBaseModule |

---

## 任务 1: 创建 DTO（Zod Schema）

**文件：**
- 创建：`packages/server/src/modules/knowledge-base/dto/create-kb.dto.ts`
- 创建：`packages/server/src/modules/knowledge-base/dto/update-kb.dto.ts`
- 创建：`packages/server/src/modules/knowledge-base/dto/create-folder.dto.ts`
- 创建：`packages/server/src/modules/knowledge-base/dto/update-folder.dto.ts`

**规格引用：**
- API 规格：[第 4 节 - DTO 定义]

- [ ] **步骤 1: 创建 create-kb.dto.ts**

```typescript
import { createZodDto } from 'nestjs-zod'
import { z } from 'zod'

export const createKbSchema = z.object({
  name: z.string().min(1, '知识库名称不能为空').max(100, '名称过长'),
  description: z.string().max(500, '描述过长').optional(),
  icon: z.string().max(10, '图标过长').optional(),
})

export class CreateKbDto extends createZodDto(createKbSchema) {}
```

- [ ] **步骤 2: 创建 update-kb.dto.ts**

```typescript
import { createZodDto } from 'nestjs-zod'
import { z } from 'zod'

export const updateKbSchema = z.object({
  name: z.string().min(1, '知识库名称不能为空').max(100, '名称过长').optional(),
  description: z.string().max(500, '描述过长').nullable().optional(),
  isPinned: z.boolean().optional(),
  sortOrder: z.number().int().min(0, '排序值不能为负数').optional(),
  icon: z.string().max(10, '图标过长').nullable().optional(),
})

export class UpdateKbDto extends createZodDto(updateKbSchema) {}
```

- [ ] **步骤 3: 创建 create-folder.dto.ts**

```typescript
import { createZodDto } from 'nestjs-zod'
import { z } from 'zod'

export const createFolderSchema = z.object({
  name: z.string().min(1, '文件夹名称不能为空').max(100, '名称过长'),
  parentId: z.string().uuid('parentId 格式非法').nullable().optional(),
})

export class CreateFolderDto extends createZodDto(createFolderSchema) {}
```

- [ ] **步骤 4: 创建 update-folder.dto.ts**

```typescript
import { createZodDto } from 'nestjs-zod'
import { z } from 'zod'

export const updateFolderSchema = z.object({
  name: z.string().min(1, '文件夹名称不能为空').max(100, '名称过长'),
})

export class UpdateFolderDto extends createZodDto(updateFolderSchema) {}
```

- [ ] **步骤 5: 验证类型检查**

运行：`cd packages/server && pnpm type-check`
预期：PASS

- [ ] **步骤 6: 提交**

```bash
git add packages/server/src/modules/knowledge-base/dto/
git commit -m "feat(kb): 添加知识库和文件夹的 Zod DTO"
```

---

## 任务 2: 创建 KnowledgeBaseService

**文件：**
- 创建：`packages/server/src/modules/knowledge-base/knowledge-base.service.ts`

**规格引用：**
- API 规格：[第 3 节 - 端点详情]、[第 8 节 - 边界情况]
- Prisma schema：`KnowledgeBase`、`Folder` 模型

- [ ] **步骤 1: 编写 KnowledgeBaseService**

```typescript
import {
  Injectable,
  NotFoundException,
  ForbiddenException,
} from '@nestjs/common'
import { PrismaService } from '../../processors/database/prisma.service.js'
import type { CreateKbDto } from './dto/create-kb.dto.js'
import type { UpdateKbDto } from './dto/update-kb.dto.js'
import type { CreateFolderDto } from './dto/create-folder.dto.js'
import type { UpdateFolderDto } from './dto/update-folder.dto.js'

@Injectable()
export class KnowledgeBaseService {
  constructor(private readonly prisma: PrismaService) {}

  // ---- 知识库 ----

  async list(userId: string) {
    return this.prisma.knowledgeBase.findMany({
      where: { userId },
      orderBy: [{ sortOrder: 'asc' }, { createdAt: 'desc' }],
    })
  }

  async create(userId: string, dto: CreateKbDto) {
    const maxOrder = await this.prisma.knowledgeBase.aggregate({
      where: { userId },
      _max: { sortOrder: true },
    })

    return this.prisma.knowledgeBase.create({
      data: {
        userId,
        name: dto.name,
        description: dto.description ?? null,
        icon: dto.icon ?? null,
        sortOrder: (maxOrder._max.sortOrder ?? -1) + 1,
      },
    })
  }

  async update(userId: string, id: string, dto: UpdateKbDto) {
    await this.ensureOwnership(userId, id)

    return this.prisma.knowledgeBase.update({
      where: { id },
      data: {
        ...(dto.name !== undefined && { name: dto.name }),
        ...(dto.description !== undefined && { description: dto.description }),
        ...(dto.isPinned !== undefined && { isPinned: dto.isPinned }),
        ...(dto.sortOrder !== undefined && { sortOrder: dto.sortOrder }),
        ...(dto.icon !== undefined && { icon: dto.icon }),
      },
    })
  }

  async remove(userId: string, id: string) {
    await this.ensureOwnership(userId, id)
    await this.prisma.knowledgeBase.delete({ where: { id } })
    return { id, deleted: true }
  }

  // ---- 文件夹 ----

  async listFolders(userId: string, kbId: string, parentId?: string) {
    await this.ensureOwnership(userId, kbId)

    return this.prisma.folder.findMany({
      where: {
        kbId,
        parentId: parentId ?? null,
      },
      orderBy: { createdAt: 'asc' },
    })
  }

  async createFolder(userId: string, kbId: string, dto: CreateFolderDto) {
    await this.ensureOwnership(userId, kbId)

    if (dto.parentId) {
      const parent = await this.prisma.folder.findFirst({
        where: { id: dto.parentId, kbId },
      })
      if (!parent) {
        throw new NotFoundException({
          code: 'NOT_FOUND',
          message: '父文件夹不存在',
        })
      }
    }

    return this.prisma.folder.create({
      data: {
        kbId,
        parentId: dto.parentId ?? null,
        name: dto.name,
      },
    })
  }

  async updateFolder(
    userId: string,
    kbId: string,
    folderId: string,
    dto: UpdateFolderDto,
  ) {
    await this.ensureOwnership(userId, kbId)

    const folder = await this.prisma.folder.findFirst({
      where: { id: folderId, kbId },
    })
    if (!folder) {
      throw new NotFoundException({
        code: 'NOT_FOUND',
        message: '文件夹不存在',
      })
    }

    return this.prisma.folder.update({
      where: { id: folderId },
      data: { name: dto.name },
    })
  }

  async removeFolder(userId: string, kbId: string, folderId: string) {
    await this.ensureOwnership(userId, kbId)

    const folder = await this.prisma.folder.findFirst({
      where: { id: folderId, kbId },
    })
    if (!folder) {
      throw new NotFoundException({
        code: 'NOT_FOUND',
        message: '文件夹不存在',
      })
    }

    await this.prisma.folder.delete({ where: { id: folderId } })
    return { id: folderId, deleted: true }
  }

  // ---- 私有方法 ----

  private async ensureOwnership(userId: string, kbId: string) {
    const kb = await this.prisma.knowledgeBase.findUnique({
      where: { id: kbId },
      select: { userId: true },
    })

    if (!kb) {
      throw new NotFoundException({
        code: 'NOT_FOUND',
        message: '知识库不存在',
      })
    }

    if (kb.userId !== userId) {
      throw new ForbiddenException({
        code: 'FORBIDDEN',
        message: '无权访问该资源',
      })
    }
  }
}
```

- [ ] **步骤 2: 验证类型检查**

运行：`cd packages/server && pnpm type-check`
预期：PASS

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/modules/knowledge-base/knowledge-base.service.ts
git commit -m "feat(kb): 实现 KnowledgeBaseService（知识库 + 文件夹 CRUD）"
```

---

## 任务 3: 创建 Controllers

**文件：**
- 创建：`packages/server/src/modules/knowledge-base/knowledge-base.controller.ts`
- 创建：`packages/server/src/modules/knowledge-base/folder.controller.ts`

**规格引用：**
- API 规格：[第 3 节 - 端点详情]、[第 5 节 - NestJS 控制器示例]

- [ ] **步骤 1: 创建 KnowledgeBaseController**

```typescript
import {
  Controller,
  Get,
  Post,
  Patch,
  Delete,
  Body,
  Param,
  UseGuards,
} from '@nestjs/common'
import { JwtAuthGuard } from '../../auth/guards/jwt.guard.js'
import { CurrentUser } from '../../auth/decorators/current-user.decorator.js'
import { ZodValidationPipe } from '../../common/pipes/zod-validation.pipe.js'
import { KnowledgeBaseService } from './knowledge-base.service.js'
import { CreateKbDto, createKbSchema } from './dto/create-kb.dto.js'
import { UpdateKbDto, updateKbSchema } from './dto/update-kb.dto.js'

@Controller('api/knowledge-bases')
@UseGuards(JwtAuthGuard)
export class KnowledgeBaseController {
  constructor(private readonly kbService: KnowledgeBaseService) {}

  @Get()
  async list(@CurrentUser('id') userId: string) {
    return this.kbService.list(userId)
  }

  @Post()
  async create(
    @CurrentUser('id') userId: string,
    @Body(new ZodValidationPipe(createKbSchema)) dto: CreateKbDto,
  ) {
    return this.kbService.create(userId, dto)
  }

  @Patch(':id')
  async update(
    @CurrentUser('id') userId: string,
    @Param('id') id: string,
    @Body(new ZodValidationPipe(updateKbSchema)) dto: UpdateKbDto,
  ) {
    return this.kbService.update(userId, id, dto)
  }

  @Delete(':id')
  async remove(
    @CurrentUser('id') userId: string,
    @Param('id') id: string,
  ) {
    return this.kbService.remove(userId, id)
  }
}
```

- [ ] **步骤 2: 创建 FolderController**

```typescript
import {
  Controller,
  Get,
  Post,
  Patch,
  Delete,
  Body,
  Param,
  Query,
  UseGuards,
} from '@nestjs/common'
import { JwtAuthGuard } from '../../auth/guards/jwt.guard.js'
import { CurrentUser } from '../../auth/decorators/current-user.decorator.js'
import { ZodValidationPipe } from '../../common/pipes/zod-validation.pipe.js'
import { KnowledgeBaseService } from './knowledge-base.service.js'
import { CreateFolderDto, createFolderSchema } from './dto/create-folder.dto.js'
import { UpdateFolderDto, updateFolderSchema } from './dto/update-folder.dto.js'

@Controller('api/knowledge-bases/:kbId/folders')
@UseGuards(JwtAuthGuard)
export class FolderController {
  constructor(private readonly kbService: KnowledgeBaseService) {}

  @Get()
  async list(
    @CurrentUser('id') userId: string,
    @Param('kbId') kbId: string,
    @Query('parentId') parentId?: string,
  ) {
    return this.kbService.listFolders(userId, kbId, parentId)
  }

  @Post()
  async create(
    @CurrentUser('id') userId: string,
    @Param('kbId') kbId: string,
    @Body(new ZodValidationPipe(createFolderSchema)) dto: CreateFolderDto,
  ) {
    return this.kbService.createFolder(userId, kbId, dto)
  }

  @Patch(':folderId')
  async update(
    @CurrentUser('id') userId: string,
    @Param('kbId') kbId: string,
    @Param('folderId') folderId: string,
    @Body(new ZodValidationPipe(updateFolderSchema)) dto: UpdateFolderDto,
  ) {
    return this.kbService.updateFolder(userId, kbId, folderId, dto)
  }

  @Delete(':folderId')
  async remove(
    @CurrentUser('id') userId: string,
    @Param('kbId') kbId: string,
    @Param('folderId') folderId: string,
  ) {
    return this.kbService.removeFolder(userId, kbId, folderId)
  }
}
```

- [ ] **步骤 3: 验证类型检查**

运行：`cd packages/server && pnpm type-check`
预期：PASS

- [ ] **步骤 4: 提交**

```bash
git add packages/server/src/modules/knowledge-base/*.controller.ts
git commit -m "feat(kb): 添加 KnowledgeBaseController 和 FolderController"
```

---

## 任务 4: 创建模块并注册到 AppModule

**文件：**
- 创建：`packages/server/src/modules/knowledge-base/knowledge-base.module.ts`
- 修改：`packages/server/src/app.module.ts`

**规格引用：**
- API 规格：[第 5 节 - NestJS 控制器示例]

- [ ] **步骤 1: 创建 KnowledgeBaseModule**

```typescript
import { Module } from '@nestjs/common'
import { KnowledgeBaseService } from './knowledge-base.service.js'
import { KnowledgeBaseController } from './knowledge-base.controller.js'
import { FolderController } from './folder.controller.js'

@Module({
  providers: [KnowledgeBaseService],
  controllers: [KnowledgeBaseController, FolderController],
})
export class KnowledgeBaseModule {}
```

- [ ] **步骤 2: 注册到 AppModule**

在 `packages/server/src/app.module.ts` 的 `imports` 数组中添加：

```typescript
import { KnowledgeBaseModule } from './modules/knowledge-base/knowledge-base.module.js'
```

```typescript
imports: [
  // ...existing imports
  KnowledgeBaseModule,
]
```

- [ ] **步骤 3: 验证类型检查**

运行：`cd packages/server && pnpm type-check`
预期：PASS

- [ ] **步骤 4: 提交**

```bash
git add packages/server/src/modules/knowledge-base/knowledge-base.module.ts packages/server/src/app.module.ts
git commit -m "feat(kb): 注册 KnowledgeBaseModule 到 AppModule"
```

---

## 任务 5: 运行服务器并手动验证端点

**规格引用：**
- API 规格：[第 2 节 - 端点清单]

- [ ] **步骤 1: 启动服务器**

```bash
cd packages/server && pnpm dev:server
```

- [ ] **步骤 2: 验证健康检查**

```bash
curl http://localhost:3000/api/health
```
预期：`{ "data": { "status": "ok" } }`

- [ ] **步骤 3: 测试登录获取 Token**

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"123456"}'
```

- [ ] **步骤 4: 测试知识库 CRUD**

```bash
# 1. 创建知识库
curl -X POST http://localhost:3000/api/knowledge-bases \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"name":"测试知识库","description":"测试","icon":"📁"}'

# 2. 列表
curl http://localhost:3000/api/knowledge-bases \
  -H "Authorization: Bearer <token>"

# 3. 更新
curl -X PATCH http://localhost:3000/api/knowledge-bases/<id> \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"name":"重命名"}'

# 4. 删除
curl -X DELETE http://localhost:3000/api/knowledge-bases/<id> \
  -H "Authorization: Bearer <token>"
```

- [ ] **步骤 5: 测试文件夹 CRUD**

```bash
# 1. 创建文件夹
curl -X POST http://localhost:3000/api/knowledge-bases/<kbId>/folders \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"name":"产品文档"}'

# 2. 列表
curl "http://localhost:3000/api/knowledge-bases/<kbId>/folders" \
  -H "Authorization: Bearer <token>"

# 3. 更新
curl -X PATCH http://localhost:3000/api/knowledge-bases/<kbId>/folders/<folderId> \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"name":"技术文档"}'

# 4. 删除
curl -X DELETE http://localhost:3000/api/knowledge-bases/<kbId>/folders/<folderId> \
  -H "Authorization: Bearer <token>"
```

- [ ] **步骤 6: 验证错误场景**

| 场景 | 命令 | 预期 |
|------|------|------|
| 无 Token | 不带 `Authorization` | 401 |
| 操作他人知识库 | 用 userA 的 token 操作 userB 的 kbId | 403 |
| 不存在的知识库 | `PATCH /api/knowledge-bases/fake-id` | 404 |
| 无效 parentId | 创建文件夹时传不存在的 parentId | 404 |

- [ ] **步骤 7: 提交（如需要修复）**

```bash
git commit -m "fix(kb): 根据手动测试修复边界情况（如有）"
```

---

## 规格覆盖检查

| 规格项 | 对应任务 | 状态 |
|--------|----------|------|
| GET /api/knowledge-bases | 任务 3 | ✅ |
| POST /api/knowledge-bases | 任务 2 + 3 | ✅ |
| PATCH /api/knowledge-bases/:id | 任务 2 + 3 | ✅ |
| DELETE /api/knowledge-bases/:id | 任务 2 + 3 | ✅ |
| GET /api/knowledge-bases/:id/folders | 任务 2 + 3 | ✅ |
| POST /api/knowledge-bases/:id/folders | 任务 2 + 3 | ✅ |
| PATCH /api/knowledge-bases/:id/folders/:folderId | 任务 2 + 3 | ✅ |
| DELETE /api/knowledge-bases/:id/folders/:folderId | 任务 2 + 3 | ✅ |
| JWT 认证 | 任务 3（@UseGuards） | ✅ |
| 用户隔离（userId 过滤） | 任务 2（ensureOwnership） | ✅ |
| 级联删除（Prisma onDelete: Cascade） | 任务 2（Prisma delete） | ✅ |
| Zod DTO 校验 | 任务 1 | ✅ |
| 错误码规范（400/401/403/404） | 任务 2 + 3 | ✅ |

---

## 执行交接

**计划已保存到 `docs/04-plans/b-02-knowledge-base-crud-api/v1.md`。两种执行方式：**

1. **子代理驱动（推荐）** — 每个任务新子代理，任务间审查
2. **内联执行** — 当前会话中顺序执行，带检查点

**选择哪种？**
