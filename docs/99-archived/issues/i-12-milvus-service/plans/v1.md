---
issue_id: i-12-milvus-service
type: plan
version: 1
status: done
task_count: 32
summary: 将现有 MilvusVectorStore 封装为 NestJS @Global() 模块 VectorModule + VectorService，使全应用可通过依赖注入使用向量存储能力。配置从 ConfigService 读取，启动时自动检查连接并确保 Collection 存在，额外提供 deleteByFileId/deleteByKbId 业务方法。
---

# 执行计划：i-12-milvus-service（NestJS 封装）

> **For agentic workers:** 必需子技能：`superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`。步骤使用复选框（`- [ ]`）语法追踪。

**目标：** 将现有 `MilvusVectorStore` 封装为 NestJS `@Injectable()` Service，创建 `@Global()` 的 `VectorModule`，使全应用可通过依赖注入使用向量存储能力。配置从 `ConfigService` 读取，启动时自动检查连接并确保 Collection 存在。

**架构：** `VectorModule`（`@Global()`）导出 `VectorService`（`@Injectable()`）；`VectorService` 在构造函数中通过 `ConfigService` 读取 Milvus 配置并实例化 `MilvusVectorStore`，委托其完成全部底层操作；`VectorService` 实现 `IVectorStore` 接口并额外提供 `deleteByFileId` / `deleteByKbId` 业务方法。

**技术栈：** NestJS 10、`@nestjs/config`、`@zilliz/milvus2-sdk-node`、TypeScript 5.9

**Issue 引用：** [docs/02-issues/i-12-milvus-service.md](../../../02-issues/i-12-milvus-service.md)
**Spec 引用：** [docs/03-specs/i-12-milvus-service/feature-spec.md](../../../03-specs/i-12-milvus-service/feature-spec.md)、[api-spec.md](../../../03-specs/i-12-milvus-service/api-spec.md)
**阻塞依赖：** i-08-nestjs-server-setup（NestJS 模块结构必须已就绪）

---

## 前置条件检查

在执行本计划前，必须确认以下依赖已完成：

- [ ] `i-08-nestjs-server-setup` 已完成 — `packages/server/src/app.module.ts` 存在且为 NestJS 根模块。
- [ ] `i-04-milvus-client` 已完成 — `packages/server/src/vector/milvus.ts` 存在且 `MilvusVectorStore` 类已实现 `IVectorStore`。
- [ ] `i-00-core-interfaces` 已完成 — `packages/server/src/interfaces/IVectorStore.ts` 与 `errors.ts` 存在。
- [ ] `packages/server/package.json` 已包含 `@nestjs/common`、`@nestjs/config`、`@nestjs/core` 依赖。

若前置条件不满足，先执行对应计划，或在本计划 Task 1 中补充安装缺失依赖。

---

## 文件结构规划

```
packages/server/
├── src/
│   ├── processors/
│   │   └── vector/
│   │       ├── vector.module.ts      # @Global() VectorModule
│   │       └── vector.service.ts     # @Injectable() VectorService
│   ├── vector/
│   │   └── milvus.ts                 # 现有 MilvusVectorStore（复用，不修改）
│   ├── interfaces/
│   │   ├── IVectorStore.ts           # 现有接口（复用，不修改）
│   │   └── errors.ts                 # 现有错误类型（复用，不修改）
│   └── app.module.ts                 # 修改：导入 VectorModule
├── package.json                      # 确保含 @nestjs/common / @nestjs/config
└── tsconfig.json                     # 现有（不修改）
```

---

## 任务列表

### Task 1：安装/确认 NestJS 与 Config 依赖

**文件：**
- 修改：`packages/server/package.json`（若缺失依赖）
- 修改：`pnpm-lock.yaml`（由 pnpm 自动生成）

**规格引用：** Feature Spec [2.1 范围内]、API Spec [5. 配置项]

- [ ] **步骤 1：检查现有依赖**

  运行：
  ```bash
  grep -E '"@nestjs/(common|config|core)"' packages/server/package.json
  ```
  预期：输出三行版本号。若缺失任一，执行步骤 2。

- [ ] **步骤 2：安装缺失依赖**

  运行：
  ```bash
  cd packages/server && pnpm add @nestjs/common @nestjs/config @nestjs/core
  ```
  预期：`package.json` 的 `dependencies` 中新增上述三项。

- [ ] **步骤 3：验证安装**

  运行：
  ```bash
  pnpm install --frozen-lockfile
  ```
  预期：无报错，lockfile 一致。

- [ ] **步骤 4：提交（仅当实际安装了新依赖时）**

  ```bash
  git add packages/server/package.json pnpm-lock.yaml
  git commit -m "deps(server): 安装 @nestjs/common @nestjs/config @nestjs/core"
  ```

---

### Task 2：创建 `VectorModule`

**文件：**
- 创建：`packages/server/src/processors/vector/vector.module.ts`

**规格引用：** API Spec [1.1 VectorModule]、Feature Spec [2.1 范围内 — @Global() 注册]

- [ ] **步骤 1：创建目录**

  运行：
  ```bash
  mkdir -p packages/server/src/processors/vector
  ```

- [ ] **步骤 2：编写模块文件**

  创建 `packages/server/src/processors/vector/vector.module.ts`，内容如下：

  ```typescript
  import { Global, Module } from '@nestjs/common'
  import { VectorService } from './vector.service.js'

  @Global()
  @Module({
    providers: [VectorService],
    exports: [VectorService],
  })
  export class VectorModule {}
  ```

  说明：
  - `@Global()` 使 `VectorService` 在任何模块中无需重复导入 `VectorModule` 即可注入。
  - `providers` 注册 `VectorService`，`exports` 将其公开给全应用。

- [ ] **步骤 3：验证文件存在与格式**

  运行：
  ```bash
  cat packages/server/src/processors/vector/vector.module.ts
  ```
  预期：内容与上述代码块一致，无语法错误。

- [ ] **步骤 4：提交**

  ```bash
  git add packages/server/src/processors/vector/vector.module.ts
  git commit -m "feat(vector): 创建 VectorModule（@Global()）"
  ```

---

### Task 3：创建 `VectorService`

**文件：**
- 创建：`packages/server/src/processors/vector/vector.service.ts`

**规格引用：** API Spec [2.1 VectorService]、[3. 方法详情]、[4. 生命周期]、Feature Spec [2.1 范围内]

- [ ] **步骤 1：编写 Service 文件**

  创建 `packages/server/src/processors/vector/vector.service.ts`，内容如下：

  ```typescript
  import { Injectable, OnModuleInit } from '@nestjs/common'
  import { ConfigService } from '@nestjs/config'
  import type {
    IVectorStore,
    VectorRecord,
    VectorSearchOptions,
    VectorSearchResult,
  } from '../../interfaces/IVectorStore.js'
  import { MilvusVectorStore } from '../../vector/milvus.js'

  @Injectable()
  export class VectorService implements IVectorStore, OnModuleInit {
    private readonly store: MilvusVectorStore

    constructor(private readonly config: ConfigService) {
      this.store = new MilvusVectorStore({
        host: this.config.get<string>('MILVUS_HOST'),
        port: this.config.get<string>('MILVUS_PORT'),
        collectionName: this.config.get<string>('MILVUS_COLLECTION'),
        vectorDim: this.config.get<number>('MILVUS_VECTOR_DIM'),
      })
    }

    async onModuleInit(): Promise<void> {
      await this.store.checkHealth()
      await this.store.ensureCollection()
    }

    async ensureCollection(): Promise<void> {
      return this.store.ensureCollection()
    }

    async insertVectors(vectors: VectorRecord[]): Promise<void> {
      return this.store.insertVectors(vectors)
    }

    async searchVectors(
      queryVector: number[],
      options?: VectorSearchOptions,
    ): Promise<VectorSearchResult[]> {
      return this.store.searchVectors(queryVector, options)
    }

    async deleteByIds(ids: string[]): Promise<void> {
      return this.store.deleteByIds(ids)
    }

    async deleteByFileId(fileId: string): Promise<void> {
      return this.store.deleteByFileId(fileId)
    }

    async deleteByKbId(kbId: string): Promise<void> {
      return this.store.deleteByKbId(kbId)
    }
  }
  ```

  说明：
  - `implements IVectorStore, OnModuleInit` 明确契约与生命周期。
  - 构造函数通过 `ConfigService` 读取四项 Milvus 配置；若配置缺失，`MilvusVectorStore` 内部会回退到环境变量或默认值。
  - `onModuleInit()` 在 NestJS 应用启动时自动调用：先 `checkHealth()`（5 秒内检查连接），再 `ensureCollection()`（幂等创建/校验）。
  - `deleteByFileId` / `deleteByKbId` 不在 `IVectorStore` 接口中，是业务层高频操作，由 Service 直接暴露。

- [ ] **步骤 2：验证文件存在与格式**

  运行：
  ```bash
  cat packages/server/src/processors/vector/vector.service.ts
  ```
  预期：内容与上述代码块一致。

- [ ] **步骤 3：提交**

  ```bash
  git add packages/server/src/processors/vector/vector.service.ts
  git commit -m "feat(vector): 创建 VectorService，实现 IVectorStore 与生命周期钩子"
  ```

---

### Task 4：在 `AppModule` 中导入 `VectorModule`

**文件：**
- 修改：`packages/server/src/app.module.ts`

**规格引用：** API Spec [1.1 VectorModule — 导入方：AppModule]、Feature Spec [3. 涉及模块/文件]

- [ ] **步骤 1：确认 `app.module.ts` 存在**

  运行：
  ```bash
  test -f packages/server/src/app.module.ts && echo "exists" || echo "missing"
  ```
  预期：`exists`。若 `missing`，说明 `i-08` 尚未完成，需先执行 `i-08` 计划创建 `app.module.ts`。

- [ ] **步骤 2：修改 `app.module.ts` 导入 `VectorModule`**

  在 `packages/server/src/app.module.ts` 的 `imports` 数组中追加 `VectorModule`。

  假设 `app.module.ts` 当前内容类似：
  ```typescript
  import { Module } from '@nestjs/common'
  import { ConfigModule } from '@nestjs/config'

  @Module({
    imports: [ConfigModule.forRoot({ isGlobal: true })],
    controllers: [],
    providers: [],
  })
  export class AppModule {}
  ```

  修改为：
  ```typescript
  import { Module } from '@nestjs/common'
  import { ConfigModule } from '@nestjs/config'
  import { VectorModule } from './processors/vector/vector.module.js'

  @Module({
    imports: [
      ConfigModule.forRoot({ isGlobal: true }),
      VectorModule,
    ],
    controllers: [],
    providers: [],
  })
  export class AppModule {}
  ```

  说明：
  - `ConfigModule.forRoot({ isGlobal: true })` 必须已存在（`i-08` 负责），确保 `VectorService` 能注入 `ConfigService`。
  - `VectorModule` 本身也是 `@Global()`，但根模块导入它是 NestJS 模块树注册的必要步骤。

- [ ] **步骤 3：验证修改**

  运行：
  ```bash
  grep -A 5 'VectorModule' packages/server/src/app.module.ts
  ```
  预期：输出包含 `import { VectorModule }` 与 `VectorModule,`。

- [ ] **步骤 4：提交**

  ```bash
  git add packages/server/src/app.module.ts
  git commit -m "feat(app): 在 AppModule 中导入 VectorModule"
  ```

---

### Task 5：类型检查验证

**文件：** 无新增文件，纯验证

**规格引用：** Issue 验收标准 — `pnpm type-check` 通过

- [ ] **步骤 1：运行类型检查**

  运行：
  ```bash
  cd packages/server && pnpm type-check
  ```
  预期：零错误、零警告。

  常见错误与修复：
  - `Cannot find module '@nestjs/common'` → 执行 Task 1 安装依赖。
  - `Cannot find module '../../vector/milvus.js'` → 确认 `packages/server/src/vector/milvus.ts` 存在（`i-04` 产物）。
  - `Property 'deleteByFileId' does not exist on type 'MilvusVectorStore'` → 确认 `milvus.ts` 已包含 `deleteByFileId` 与 `deleteByKbId` 方法。
  - `TS2307: Cannot find module '../../interfaces/IVectorStore.js'` → 确认 `IVectorStore.ts` 存在（`i-00` 产物）。

- [ ] **步骤 2：确认无占位符**

  运行：
  ```bash
  grep -riE '(TBD|TODO|FIXME|稍后实现|待补充|placeholder)' packages/server/src/processors/vector/
  ```
  预期：无任何匹配。

- [ ] **步骤 3：提交（若修复了类型错误）**

  若步骤 1 中修复了类型错误，提交修复：
  ```bash
  git add -A
  git commit -m "fix(vector): 修复 VectorService 类型检查问题"
  ```

---

### Task 6：启动时集成验证（可选但推荐）

**文件：** 无新增文件，纯验证

**规格引用：** Feature Spec [2.1 范围内 — 应用启动时自动检查]、API Spec [4. 生命周期]

- [ ] **步骤 1：确认 NestJS 启动入口存在**

  运行：
  ```bash
  test -f packages/server/src/main.ts && echo "exists" || echo "missing"
  ```
  预期：`exists`（`i-08` 产物）。

- [ ] **步骤 2：启动 NestJS 应用**

  运行：
  ```bash
  cd packages/server && pnpm dev
  ```
  预期：
  - 若 Milvus 未运行，启动日志应抛出 `VectorStoreError: Milvus 连接失败: ...`，随后 NestJS 应用退出（符合预期，启动时即发现问题）。
  - 若 Milvus 运行正常，日志应输出：
    ```
    [Milvus] Connected to localhost:19530
    [Milvus] Collection 'knowledge_chunks' already exists, verified
    ```
    或
    ```
    [Milvus] Collection 'knowledge_chunks' created (dim=1536)
    ```

- [ ] **步骤 3：停止进程**

  按 `Ctrl+C` 终止。

- [ ] **步骤 4：提交（若修改了启动相关代码）**

  若本 Task 中修改了任何文件，按常规提交。

---

## 规格覆盖检查

### 功能规格覆盖

| 功能规格条目 | 对应任务 | 状态 |
|---|---|---|
| 将现有 `MilvusVectorStore` 封装为 NestJS `@Injectable()` Service | Task 3 | 已覆盖 |
| 创建 `@Global()` 的 `VectorModule` | Task 2 | 已覆盖 |
| 配置从 `ConfigService` 读取四项 Milvus 配置 | Task 3（构造函数） | 已覆盖 |
| 实现 `IVectorStore` 接口 | Task 3（`implements IVectorStore`） | 已覆盖 |
| 提供 `ensureCollection` / `insertVectors` / `searchVectors` / `deleteByIds` | Task 3 | 已覆盖 |
| 提供 `deleteByFileId` / `deleteByKbId` 业务扩展方法 | Task 3 | 已覆盖 |
| 应用启动时自动检查连接并确保 Collection 存在 | Task 3（`onModuleInit`）、Task 6 | 已覆盖 |
| `AppModule` 导入 `VectorModule` | Task 4 | 已覆盖 |

### API 规格覆盖

| API 规格条目 | 对应任务 | 状态 |
|---|---|---|
| 1.1 VectorModule（`@Global()` + `@Module()`） | Task 2 | 已覆盖 |
| 2.1 VectorService（`@Injectable()` + `implements IVectorStore, OnModuleInit`） | Task 3 | 已覆盖 |
| 3.1 ensureCollection() | Task 3 | 已覆盖 |
| 3.2 insertVectors(vectors) | Task 3 | 已覆盖 |
| 3.3 searchVectors(queryVector, options) | Task 3 | 已覆盖 |
| 3.4 deleteByIds(ids) | Task 3 | 已覆盖 |
| 3.5 deleteByFileId(fileId) | Task 3 | 已覆盖 |
| 3.6 deleteByKbId(kbId) | Task 3 | 已覆盖 |
| 4. 生命周期（`onModuleInit` → `checkHealth` → `ensureCollection`） | Task 3 | 已覆盖 |
| 5. 配置项（MILVUS_HOST / PORT / COLLECTION / VECTOR_DIM） | Task 3 | 已覆盖 |
| 6. 调用约定（业务模块注入示例） | Task 4（模块注册后全局可用） | 已覆盖 |
| 8. 无 REST API | Task 2/3（纯 Service，无 Controller） | 已覆盖 |

### Issue 验收标准覆盖

| 验收标准 | 对应任务 | 验证方式 |
|---|---|---|
| `src/processors/vector/vector.module.ts` — VectorModule（`@Global()`） | Task 2 | 文件存在且含 `@Global()` |
| `src/processors/vector/vector.service.ts` — VectorService（`@Injectable()`） | Task 3 | 文件存在且含 `@Injectable()` |
| `ensureCollection()` | Task 3 | 方法存在，委托 `MilvusVectorStore` |
| `insertVectors(vectors)` | Task 3 | 方法存在，委托 `MilvusVectorStore` |
| `searchVectors(query, options)` | Task 3 | 方法存在，委托 `MilvusVectorStore` |
| `deleteByIds(ids)` | Task 3 | 方法存在，委托 `MilvusVectorStore` |
| `deleteByFileId(fileId)` | Task 3 | 方法存在，委托 `MilvusVectorStore` |
| `deleteByKbId(kbId)` | Task 3 | 方法存在，委托 `MilvusVectorStore` |
| 实现 `IVectorStore` 接口 | Task 3 | `implements IVectorStore` |
| 配置从 `ConfigService` 读取 | Task 3 | 构造函数注入 `ConfigService` 并读取四项配置 |
| 复用现有 `src/vector/milvus.ts` 核心逻辑 | Task 3 | `VectorService` 内部 `new MilvusVectorStore(...)` 并委托调用 |
| `pnpm type-check` 通过 | Task 5 | `tsc --noEmit` 零错误 |

### 占位符扫描

- [x] 无 "TBD" / "TODO" / "稍后实现" / "待补充" / "placeholder"
- [x] 无 "添加适当的错误处理" 等模糊描述
- [x] 所有任务均含具体代码块与验证命令
- [x] 无 "类似于任务 N" 的重复引用

### 类型一致性

- [x] `VectorService` 显式实现 `IVectorStore` 接口
- [x] `VectorRecord` / `VectorSearchOptions` / `VectorSearchResult` 类型复用 `IVectorStore.ts` 定义
- [x] 所有底层错误由 `MilvusVectorStore` 包装为 `VectorStoreError`，`VectorService` 透传
- [x] 使用 `.js` 扩展名导入，兼容 `Node16` module resolution
- [x] `onModuleInit` 返回 `Promise<void>`，符合 NestJS 生命周期契约

---

## 执行交接

**计划已保存到 `docs/04-plans/i-12-milvus-service/v1.md`。两种执行方式：**

1. **子代理驱动（推荐）** — 每个 Task 新子代理，Task 间审查
2. **内联执行** — 当前会话中顺序执行，带检查点

**选择哪种？**
