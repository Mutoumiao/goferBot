---
issue_id: f-04-tab-bar
type: plan
version: 1
status: done
task_count: 15
summary: 实现 Chat 页面内部的多标签栏，支持会话多开、切换、关闭、重命名。标签状态由独立 Pinia Store 管理，标签只保存 sessionId 引用，消息数据在 session store 中管理，标签栏作为 ChatView 子组件嵌入顶部。
---

# 聊天页面标签栏实现计划

> **For agentic workers:** 必需子技能：superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans。步骤使用复选框（`- [ ]`）语法追踪。

**目标：** 实现 Chat 页面内部的多标签栏，支持会话多开、切换、关闭、重命名。

**架构：** 标签状态由独立的 Pinia Store 管理，标签只保存 `sessionId` 引用，消息数据在 `session store` 中管理。标签栏作为 ChatView 的子组件嵌入顶部。

**技术栈：** Vue 3 + Pinia + Tailwind CSS + shadcn-vue

**Issue 引用：** [docs/issues/f-04-tab-bar.md](../../issues/f-04-tab-bar.md)
**Spec 引用：** [docs/03-specs/f-04-tab-bar/](../../03-specs/f-04-tab-bar/)

---

## 文件结构

- 创建：`packages/webui/src/stores/chatTabs.ts` — 标签状态管理
- 修改：`packages/webui/src/views/ChatView.vue` — 嵌入标签栏，适配多标签逻辑
- 修改：`packages/webui/src/components/TabBar.vue` — 适配新 Store，增加重命名功能
- 修改：`packages/webui/src/stores/session.ts` — 适配多标签下的会话创建和切换

---

## 任务 1: 创建 chatTabs Store

**文件：**
- 创建：`packages/webui/src/stores/chatTabs.ts`

**规格引用：**
- 功能规格：[标签状态与会话数据分离]
- 行为规格：[初始状态、正常流程]

- [ ] **步骤 1: 定义 Tab 接口**

```typescript
export interface ChatTab {
  id: string
  title: string
  sessionId: string | null // null 表示首页（未创建会话）
  closable: boolean
}
```

- [ ] **步骤 2: 实现 Store**

```typescript
export const useChatTabsStore = defineStore('chatTabs', () => {
  const tabs = ref<ChatTab[]>([{ id: 'home', title: '首页', sessionId: null, closable: false }])
  const activeTabId = ref<string>('home')

  const activeTab = computed(() => tabs.value.find((t) => t.id === activeTabId.value) ?? null)

  function addTab(sessionId: string, title: string) {
    const id = `tab-${Date.now()}`
    tabs.value.push({ id, title, sessionId, closable: true })
    activeTabId.value = id
  }

  function closeTab(tabId: string) {
    const idx = tabs.value.findIndex((t) => t.id === tabId)
    if (idx === -1) return
    const tab = tabs.value[idx]
    if (!tab.closable) return
    tabs.value.splice(idx, 1)
    // 切换到左侧相邻
    if (activeTabId.value === tabId) {
      const newIdx = Math.max(0, idx - 1)
      activeTabId.value = tabs.value[newIdx]?.id ?? 'home'
    }
  }

  function switchTab(tabId: string) {
    activeTabId.value = tabId
  }

  function renameTab(tabId: string, title: string) {
    const tab = tabs.value.find((t) => t.id === tabId)
    if (tab) tab.title = title
  }

  return { tabs, activeTabId, activeTab, addTab, closeTab, switchTab, renameTab }
})
```

- [ ] **步骤 3: 验证类型检查**

运行：`pnpm type-check`
预期：PASS

---

## 任务 2: 适配 TabBar 组件

**文件：**
- 修改：`packages/webui/src/components/TabBar.vue`

**规格引用：**
- 行为规格：[重命名流程、关闭后切换到左侧标签]

- [ ] **步骤 1: 增加重命名功能**

添加内联编辑状态：
```typescript
const editingTabId = ref<string | null>(null)
const editingTitle = ref('')

function startRename(tab: ChatTab) {
  editingTabId.value = tab.id
  editingTitle.value = tab.title
}

function confirmRename(tabId: string) {
  const trimmed = editingTitle.value.trim()
  if (trimmed) {
    emit('rename', tabId, trimmed)
  }
  editingTabId.value = null
}

function cancelRename() {
  editingTabId.value = null
}
```

- [ ] **步骤 2: 更新模板**

标签标题区域改为条件渲染：
- 编辑状态：显示 `<input>`，支持 Enter 确认、Esc 取消
- 普通状态：显示文本，支持双击进入编辑

- [ ] **步骤 3: 适配 Store 类型**

将 `Tab` 类型改为使用 `ChatTab`（从 `chatTabs.ts` 导入）。

---

## 任务 3: 适配 ChatView 和 session Store

**文件：**
- 修改：`packages/webui/src/views/ChatView.vue`
- 修改：`packages/webui/src/stores/session.ts`

**规格引用：**
- 行为规格：[新建标签、切换标签]

- [ ] **步骤 1: ChatView 嵌入 TabBar**

在 ChatView 顶部添加 TabBar：
```vue
<template>
  <div class="flex h-full flex-col bg-surface-1">
    <ChatTabBar
      :tabs="chatTabsStore.tabs"
      :active-tab-id="chatTabsStore.activeTabId"
      @switch="onTabSwitch"
      @close="onTabClose"
      @new-chat="onNewChat"
      @rename="onTabRename"
    />
    <!-- 现有内容 -->
  </div>
</template>
```

- [ ] **步骤 2: 处理标签切换**

```typescript
function onTabSwitch(tabId: string) {
  chatTabsStore.switchTab(tabId)
  const tab = chatTabsStore.activeTab
  if (tab?.sessionId) {
    sessionStore.loadSession(tab.sessionId)
  } else {
    sessionStore.activeSessionId = null
  }
}
```

- [ ] **步骤 3: 处理新建标签**

```typescript
async function onNewChat() {
  const session = await sessionStore.createSession()
  chatTabsStore.addTab(session.id, session.title || '新会话')
}
```

- [ ] **步骤 4: 处理关闭标签**

```typescript
function onTabClose(tabId: string) {
  chatTabsStore.closeTab(tabId)
  // 切换到新激活标签对应的会话
  const tab = chatTabsStore.activeTab
  if (tab?.sessionId) {
    sessionStore.loadSession(tab.sessionId)
  } else {
    sessionStore.activeSessionId = null
  }
}
```

- [ ] **步骤 5: 处理重命名**

```typescript
async function onTabRename(tabId: string, title: string) {
  const tab = chatTabsStore.tabs.find((t) => t.id === tabId)
  if (tab?.sessionId) {
    await sessionStore.renameSession(tab.sessionId, title)
  }
  chatTabsStore.renameTab(tabId, title)
}
```

---

## 任务 4: 适配发送消息逻辑

**文件：**
- 修改：`packages/webui/src/stores/session.ts`

**规格引用：**
- 行为规格：[首页发送消息时创建新会话]

- [ ] **步骤 1: 修改 sendMessage**

当 `activeSessionId` 为 null（首页状态）时，创建新会话后需要通知 chatTabs Store 更新首页标签的 `sessionId`：

```typescript
// 在 createSession 成功后
if (!activeSessionId.value) {
  // 首页发送消息，需要更新首页标签的 sessionId
  // 或者将首页标签替换为新会话标签
}
```

决策：首页发送第一条消息时，将首页标签的 `sessionId` 更新为新创建的会话 ID，标题更新为消息摘要。

---

## 任务 5: 验证与提交

- [ ] **步骤 1: 类型检查**

运行：`pnpm type-check`
预期：PASS

- [ ] **步骤 2: 功能验证**

1. 进入 Chat 页面，显示"首页"标签
2. 点击 `+` 新建标签，创建新会话
3. 在标签间切换，消息区域正确更新
4. 关闭标签，自动切换到左侧
5. 双击标签重命名，调用 API 保存

- [ ] **步骤 3: 提交**

```bash
git add packages/webui/src/stores/chatTabs.ts packages/webui/src/components/TabBar.vue packages/webui/src/views/ChatView.vue packages/webui/src/stores/session.ts
git commit -m "feat(chat): 实现 Chat 页面多标签栏 (f-04)"
```
