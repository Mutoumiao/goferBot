---
issue_id: i-02-prisma-setup
type: plan
version: 1
status: done
task_count: 13
summary: 在 packages/server 中完成 Prisma 5 + @prisma/client 的安装与配置，定义完整数据模型（8 张表），生成初始迁移，提供 NestJS 原生的 PrismaService 和 DatabaseModule，并清理废弃的 Drizzle ORM 文件。
---

# 执行计划：Prisma ORM 初始化（i-02-prisma-setup）

> 生成日期：2026-05-16
> 对应 Issue：`docs/issues/i-02-prisma-setup.md`
> 参考模板：`D:/projects/ai-stared-project/nest-http-prisma-zod`

---

## 目标

在 `packages/server` 中完成 Prisma 5 + `@prisma/client` 的安装与配置，定义完整数据模型（8 张表），生成初始迁移，提供 NestJS 原生的 `PrismaService` 和 `DatabaseModule`，并清理废弃的 Drizzle ORM 文件。

---

## 前置条件

- `i-01-docker-compose-infra` 已完成，PostgreSQL 容器运行中（`localhost:5432`）。
- `i-08-nestjs-server-setup` 已完成，`packages/server` 已具备 NestJS 模块结构（`src/processors/` 目录存在）。
- 本地 `.env` 文件已配置 `DATABASE_URL`。

---

## 文件清单（变更范围）

| 操作 | 文件路径                                                                              |
|------|---------------------------------------------------------------------------------------|
| 新增 | `packages/server/prisma/schema.prisma`                                                |
| 新增 | `packages/server/prisma/migrations/`（由 CLI 自动生成）                               |
| 新增 | `packages/server/src/processors/database/prisma.service.ts`                           |
| 新增 | `packages/server/src/processors/database/database.module.ts`                          |
| 新增 | `packages/server/prisma/seed.ts`（可选）                                              |
| 修改 | `packages/server/package.json` — 依赖 + scripts                                       |
| 修改 | `packages/server/.env.example` — 补充 `DATABASE_URL` 说明                             |
| 修改 | `packages/server/tsconfig.json` — 确保 `prisma/` 在 include 外（Prisma 自有生成逻辑） |
| 删除 | `packages/server/drizzle.config.ts`                                                   |
| 删除 | `packages/server/src/db/schema.ts`                                                    |
| 删除 | `packages/server/src/db/index.ts`                                                     |
| 删除 | `packages/server/src/db/client.ts`                                                    |
| 删除 | `packages/server/src/db/type-check.ts`                                                |
| 删除 | `packages/server/drizzle/` 目录                                                       |

---

## 步骤详情

### 步骤 1：安装 Prisma 依赖

**动作**：在 `packages/server` 中安装 Prisma 5 和客户端。

```bash
cd packages/server
pnpm add prisma@^5.7.0 @prisma/client@^5.7.0
pnpm add -D @types/node
```

**验证**：

```bash
pnpm prisma --version
# 期望输出：prisma 5.7.x
```

---

### 步骤 2：创建 `prisma/schema.prisma`

**动作**：在 `packages/server/prisma/schema.prisma` 写入完整数据模型。

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            String         @id @default(uuid())
  email         String         @unique
  name          String?
  avatar        String?
  password      String
  createdAt     DateTime       @default(now()) @map("created_at")
  updatedAt     DateTime       @updatedAt @map("updated_at")

  knowledgeBases KnowledgeBase[]
  sessions       Session[]
  settings       Setting[]

  @@map("users")
}

model KnowledgeBase {
  id          String    @id @default(uuid())
  userId      String    @map("user_id")
  name        String
  description String?
  isPinned    Boolean   @default(false) @map("is_pinned")
  sortOrder   Int       @default(0) @map("sort_order")
  icon        String?
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")

  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  folders   Folder[]
  documents Document[]
  chunks    Chunk[]

  @@map("knowledge_bases")
}

model Folder {
  id        String   @id @default(uuid())
  kbId      String   @map("kb_id")
  parentId  String?  @map("parent_id")
  name      String
  createdAt DateTime @default(now()) @map("created_at")

  knowledgeBase KnowledgeBase @relation(fields: [kbId], references: [id], onDelete: Cascade)
  parent        Folder?       @relation("FolderTree", fields: [parentId], references: [id], onDelete: Cascade)
  children      Folder[]      @relation("FolderTree")
  documents     Document[]

  @@map("folders")
}

model Document {
  id           String   @id @default(uuid())
  kbId         String   @map("kb_id")
  folderId     String?  @map("folder_id")
  name         String
  ext          String?
  mimeType     String?  @map("mime_type")
  size         BigInt?
  storageKey   String   @map("storage_key")
  hash         String?
  status       String   @default("uploaded")
  errorMessage String?  @map("error_message")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  knowledgeBase KnowledgeBase @relation(fields: [kbId], references: [id], onDelete: Cascade)
  folder        Folder?       @relation(fields: [folderId], references: [id], onDelete: Cascade)
  chunks        Chunk[]

  @@map("documents")
}

model Chunk {
  id         String   @id @default(uuid())
  documentId String   @map("document_id")
  kbId       String   @map("kb_id")
  content    String
  tokenCount Int?     @map("token_count")
  chunkIndex Int      @map("chunk_index")
  milvusId   String?  @map("milvus_id")
  createdAt  DateTime @default(now()) @map("created_at")

  document      Document      @relation(fields: [documentId], references: [id], onDelete: Cascade)
  knowledgeBase KnowledgeBase @relation(fields: [kbId], references: [id], onDelete: Cascade)

  @@map("chunks")
}

model Session {
  id        String   @id @default(uuid())
  userId    String   @map("user_id")
  title     String   @default("新对话")
  provider  String?
  model     String?
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  user     User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  messages Message[]

  @@map("sessions")
}

model Message {
  id        String   @id @default(uuid())
  sessionId String   @map("session_id")
  role      String
  content   String
  createdAt DateTime @default(now()) @map("created_at")

  session Session @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@map("messages")
}

model Setting {
  id        String   @id @default(uuid())
  userId    String   @map("user_id")
  key       String
  value     String
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, key])
  @@map("settings")
}
```

**验证**：

```bash
cd packages/server
pnpm prisma validate
# 期望输出：Prisma schema loaded from prisma/schema.prisma
#           The schema is valid!
```

---

### 步骤 3：生成 Prisma Client

**动作**：运行 generate 命令，生成类型安全的客户端代码。

```bash
cd packages/server
pnpm prisma generate
```

**验证**：

```bash
ls node_modules/.bin/prisma
ls node_modules/@prisma/client
# 期望：存在 index.d.ts、index.js、package.json
```

---

### 步骤 4：创建初始迁移并应用到数据库

**动作**：使用 `prisma migrate dev` 生成并应用初始迁移。

```bash
cd packages/server
pnpm prisma migrate dev --name init
```

**交互说明**：
- 若提示是否清空数据库并重新创建，选择 **No**（保留已有数据，除非明确需要全新数据库）。
- 若提示是否应用迁移，选择 **Yes**。

**验证**：

```bash
# 检查迁移目录是否生成
ls prisma/migrations/
# 期望：存在类似 20260516120000_init/migration.sql 的目录

# 检查数据库表是否创建
pnpm prisma db execute --stdin <<EOF
SELECT table_name FROM information_schema.tables WHERE table_schema = 'public';
EOF
# 期望：users, knowledge_bases, folders, documents, chunks, sessions, messages, settings, _prisma_migrations
```

---

### 步骤 5：创建 PrismaService

**动作**：在 `packages/server/src/processors/database/prisma.service.ts` 创建 NestJS Injectable 服务。

```typescript
import { Injectable, OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService
  extends PrismaClient
  implements OnModuleInit, OnModuleDestroy
{
  async onModuleInit() {
    await this.$connect();
  }

  async onModuleDestroy() {
    await this.$disconnect();
  }
}
```

**验证**：文件存在且无 TypeScript 错误。

```bash
cd packages/server
pnpm type-check
```

---

### 步骤 6：创建 DatabaseModule

**动作**：在 `packages/server/src/processors/database/database.module.ts` 创建模块。

```typescript
import { Global, Module } from '@nestjs/common';
import { PrismaService } from './prisma.service';

@Module({
  providers: [PrismaService],
  exports: [PrismaService],
})
@Global()
export class DatabaseModule {}
```

**验证**：文件存在且无 TypeScript 错误。

---

### 步骤 7：更新 `package.json` Scripts 与依赖

**动作**：修改 `packages/server/package.json`。

1. **添加 scripts**：

```json
{
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "type-check": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "prisma:seed": "tsx prisma/seed.ts"
  }
}
```

2. **移除 Drizzle 依赖**（确认 Prisma 已可替代后执行）：

```bash
cd packages/server
pnpm remove drizzle-orm pg
pnpm remove -D drizzle-kit @types/pg
```

3. **清理旧 scripts**：删除 `db:generate`、`db:migrate`、`db:studio`。

**验证**：

```bash
cd packages/server
pnpm type-check
```

---

### 步骤 8：更新 `.env.example`

**动作**：确保 `packages/server/.env.example` 包含 `DATABASE_URL`。

当前 `.env.example` 已包含：

```env
DATABASE_URL=postgresql://user:password@localhost:5432/goferbot
```

无需修改，但需在 PR 描述中提醒开发者复制 `.env.example` 到 `.env` 并替换为实际连接串。

---

### 步骤 9：清理废弃的 Drizzle 文件

**动作**：删除以下 Drizzle ORM 相关文件和目录。

```bash
cd packages/server
rm drizzle.config.ts
rm -rf drizzle/
rm -rf src/db/
```

**验证**：

```bash
ls drizzle.config.ts 2>/dev/null || echo "已删除"
ls src/db/ 2>/dev/null || echo "已删除"
ls drizzle/ 2>/dev/null || echo "已删除"
```

---

### 步骤 10：可选 — 创建 Seed 脚本

**动作**：在 `packages/server/prisma/seed.ts` 创建种子数据脚本。

```typescript
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

async function main() {
  const user = await prisma.user.create({
    data: {
      email: 'seed@goferbot.local',
      name: 'Seed User',
      password: '$2b$10$seed_password_hash_placeholder',
    },
  });

  await prisma.knowledgeBase.create({
    data: {
      userId: user.id,
      name: '示例知识库',
      description: '由 seed 脚本创建的示例知识库',
    },
  });

  console.log('Seed completed:', user.id);
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

**验证**：

```bash
cd packages/server
pnpm prisma:seed
```

---

### 步骤 11：TypeScript 类型检查

**动作**：运行全量类型检查。

```bash
cd packages/server
pnpm type-check
```

**期望**：无错误输出，退出码 0。

---

### 步骤 12：验证 Prisma Studio 可启动

**动作**：确认 Prisma Studio 能正常启动（无需长期运行，验证后 `Ctrl+C` 退出）。

```bash
cd packages/server
pnpm prisma:studio
```

**期望**：终端输出 `Prisma Studio is up on http://localhost:5555`，浏览器可打开该地址查看 8 张表。

---

### 步骤 13：提交代码

**动作**：创建 git 提交。

```bash
git add packages/server/prisma/
git add packages/server/src/processors/database/
git add packages/server/package.json
git add packages/server/.env.example
git rm packages/server/drizzle.config.ts
git rm -r packages/server/drizzle/
git rm -r packages/server/src/db/

git commit -m "feat(prisma): 初始化 Prisma ORM，定义 8 张表数据模型

- 新增 prisma/schema.prisma（User, KnowledgeBase, Folder, Document, Chunk, Session, Message, Setting）
- 新增初始迁移 prisma/migrations/2026xxxx_init
- 新增 PrismaService（OnModuleInit/OnModuleDestroy）
- 新增 DatabaseModule（@Global()）
- 新增 package.json scripts: prisma:generate, prisma:migrate, prisma:studio, prisma:seed
- 清理废弃的 Drizzle ORM 文件和依赖
- 通过 pnpm type-check"
```

---

## 验收标准核对表

| 验收项                                | 验证命令 / 方法                                  | 状态   |
|---------------------------------------|--------------------------------------------------|--------|
| `prisma/schema.prisma` 包含 8 个模型  | `cat prisma/schema.prisma`                       | 待验证 |
| `prisma/migrations/` 存在初始迁移     | `ls prisma/migrations/`                          | 待验证 |
| `PrismaService` 实现生命周期钩子      | `cat src/processors/database/prisma.service.ts`  | 待验证 |
| `DatabaseModule` 导出 `PrismaService` | `cat src/processors/database/database.module.ts` | 待验证 |
| `package.json` 包含 prisma scripts    | `cat package.json`                               | 待验证 |
| `.env.example` 包含 `DATABASE_URL`    | `cat .env.example`                               | 待验证 |
| `pnpm prisma:generate` 成功           | `pnpm prisma:generate`                           | 待验证 |
| `pnpm prisma:migrate` 成功            | `pnpm prisma migrate dev`                        | 待验证 |
| `pnpm type-check` 通过                | `pnpm type-check`                                | 待验证 |
| Drizzle 文件已清理                    | `ls src/db/ 2>/dev/null`                         | 待验证 |

---

## 阻塞与依赖

- **阻塞于**：`i-08-nestjs-server-setup`（需要 NestJS 模块结构和 `src/processors/` 目录）。
- **前置条件**：`i-01-docker-compose-infra`（需要 PostgreSQL 服务运行以执行迁移）。

---

## 范围外

- V1 → V2 数据迁移（由 `i-06-data-migration` 负责）。
- 具体业务查询逻辑（由各自业务模块的 Repository / Service 负责）。
- Prisma Client 扩展（如分页、exists 方法）—— 后续按需添加。

---

## 参考

- Feature Spec：`docs/03-specs/i-02-prisma-setup/feature-spec.md`
- API Spec：`docs/03-specs/i-02-prisma-setup/api-spec.md`
- 参考模板：`D:/projects/ai-stared-project/nest-http-prisma-zod`
