---
issue: f-19
version: 1
status: completed
summary: 单文件修改 SettingsPage.vue，添加 Tabs 结构 + 账户设置 + 登出
---

# f-19 执行计划 v1

## 改动范围

- 修改：`packages/webui/src/components/SettingsPage.vue`

## 步骤

### 1. 导入新依赖
- 添加 `Tabs, TabsContent, TabsList, TabsTrigger` from `@/components/ui/tabs`
- 添加 `useAuthStore` from `@/stores/auth`
- 添加 `useRouter` from `vue-router`
- 添加 `LogOutIcon, UserIcon` from `lucide-vue-next`

### 2. 添加状态和方法
- `activeTab = ref('models')` — 当前激活 Tab
- `handleLogout()` — 调用 authStore.logout() 后 router.push('login')

### 3. 重构 template
- 用 `<Tabs v-model="activeTab">` 包裹内容区
- `<TabsList>` 含两个 `<TabsTrigger value="models|account">` 
- `<TabsContent value="models">` — 原有所有设置卡片 + 保存按钮
- `<TabsContent value="account">` — 账户信息 Card + 登出按钮

### 4. 验证
- `pnpm type-check` 通过
