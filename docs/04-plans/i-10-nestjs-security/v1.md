---
issue_id: i-10-nestjs-security
type: plan
version: 1
status: done
task_count: 13
summary: 建立 NestJS 安全基线：统一响应拦截器（{data} 包装）、全局异常过滤器（生产环境隐藏堆栈）、请求日志拦截器、Zod 验证管道、爬虫防护守卫 SpiderGuard、Helmet 安全头、CORS 白名单、Throttler 速率限制。
---

# NestJS 安全基线 — 执行计划

> 对应 issue: `i-10-nestjs-security`
> 依赖: `i-08-nestjs-server-setup`（NestJS 模块结构已就绪）
> 参考模板: `D:\projects\ai-stared-project\nest-http-prisma-zod`
> 计划日期: 2026-05-16

---

## 0. 前置检查（5 分钟）

**目标**：确认 `i-08-nestjs-server-setup` 已完成，NestJS 项目结构存在。

**动作**：

```bash
# 检查关键文件是否存在
test -f packages/server/src/main.ts && echo "main.ts OK"
test -f packages/server/src/bootstrap.ts && echo "bootstrap.ts OK"
test -f packages/server/src/app.module.ts && echo "app.module.ts OK"
test -d packages/server/src/common && echo "common/ OK"
```

**验证**：以上全部输出 OK。若缺失，停止执行并通知阻塞依赖未完成。

---

## 1. 安装依赖（3 分钟）

**目标**：安装安全基线所需的 NestJS 生态包。

**动作**：

```bash
cd packages/server
pnpm add @nestjs/throttler helmet @fastify/cors @fastify/helmet nestjs-zod zod
pnpm add -D @types/node
```

**验证**：

```bash
cat package.json | grep -E "@nestjs/throttler|helmet|@fastify/cors|@fastify/helmet|nestjs-zod"
```

---

## 2. 创建 `@BypassResponse()` 装饰器（2 分钟）

**目标**：提供跳过响应包装的元数据标记。

**文件**：`packages/server/src/common/decorators/bypass-response.decorator.ts`

**动作**：写入以下代码：

```typescript
import { SetMetadata } from '@nestjs/common'

export const BYPASS_RESPONSE_KEY = 'bypassResponse'
export const BypassResponse = () => SetMetadata(BYPASS_RESPONSE_KEY, true)
```

**验证**：

```bash
test -f packages/server/src/common/decorators/bypass-response.decorator.ts
```

---

## 3. 创建 `ResponseInterceptor`（5 分钟）

**目标**：统一成功响应格式为 `{ data: T }`，数组自动包装为 `{ data: { items: T[] } }`，支持 `@BypassResponse()` 跳过。

**文件**：`packages/server/src/common/interceptors/response.interceptor.ts`

**动作**：写入以下代码：

```typescript
import {
  Injectable,
  NestInterceptor,
  ExecutionContext,
  CallHandler,
} from '@nestjs/common'
import { Reflector } from '@nestjs/core'
import { Observable } from 'rxjs'
import { map } from 'rxjs/operators'
import { BYPASS_RESPONSE_KEY } from '../decorators/bypass-response.decorator'

export interface ApiResponse<T> {
  data: T
}

@Injectable()
export class ResponseInterceptor<T>
  implements NestInterceptor<T, ApiResponse<T>>
{
  constructor(private readonly reflector: Reflector) {}

  intercept(
    context: ExecutionContext,
    next: CallHandler<T>,
  ): Observable<ApiResponse<T>> {
    const handler = context.getHandler()
    const bypass = this.reflector.get<boolean>(BYPASS_RESPONSE_KEY, handler)

    if (bypass) {
      return next.handle() as Observable<ApiResponse<T>>
    }

    return next.handle().pipe(
      map((data) => {
        if (typeof data === 'undefined') {
          context.switchToHttp().getResponse().status(204)
          return data as unknown as ApiResponse<T>
        }

        if (Array.isArray(data)) {
          return { data: { items: data } } as ApiResponse<T>
        }

        return { data } as ApiResponse<T>
      }),
    )
  }
}
```

**验证**：

```bash
test -f packages/server/src/common/interceptors/response.interceptor.ts
```

---

## 4. 创建 `AllExceptionsFilter`（5 分钟）

**目标**：全局捕获所有异常，统一返回 `{ error: { code, message } }`；生产环境隐藏堆栈与 details；开发环境附加调试信息；记录错误日志。

**文件**：`packages/server/src/common/filters/all-exception.filter.ts`

**动作**：写入以下代码：

```typescript
import {
  ExceptionFilter,
  Catch,
  ArgumentsHost,
  HttpException,
  HttpStatus,
  Logger,
} from '@nestjs/common'
import { FastifyReply, FastifyRequest } from 'fastify'

export interface ErrorResponse {
  error: {
    code: string
    message: string
    details?: unknown
    stack?: string
  }
}

@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  private readonly logger = new Logger(AllExceptionsFilter.name)

  catch(exception: unknown, host: ArgumentsHost): void {
    const ctx = host.switchToHttp()
    const response = ctx.getResponse<FastifyReply>()
    const request = ctx.getRequest<FastifyRequest>()

    if (request.method === 'OPTIONS') {
      response.status(HttpStatus.OK).send()
      return
    }

    const status =
      exception instanceof HttpException
        ? exception.getStatus()
        : HttpStatus.INTERNAL_SERVER_ERROR

    const isProduction = process.env.NODE_ENV === 'production'

    let code = 'INTERNAL_ERROR'
    let message = '服务器内部错误'
    let details: unknown | undefined

    if (exception instanceof HttpException) {
      const res = exception.getResponse()
      if (typeof res === 'string') {
        message = res
      } else if (typeof res === 'object' && res !== null) {
        const obj = res as Record<string, unknown>
        code = (obj.code as string) || this.mapStatusToCode(status)
        message = (obj.message as string) || message
        details = obj.details
      }
    } else if (exception instanceof Error) {
      message = exception.message
    }

    const errorResponse: ErrorResponse = {
      error: {
        code,
        message,
      },
    }

    if (!isProduction) {
      if (details) {
        errorResponse.error.details = details
      }
      if (exception instanceof Error && exception.stack) {
        errorResponse.error.stack = exception.stack
      }
    }

    if (status === HttpStatus.INTERNAL_SERVER_ERROR) {
      this.logger.error(
        `${request.method} ${request.url} ${exception instanceof Error ? exception.stack : String(exception)}`,
      )
    } else {
      this.logger.warn(
        `${request.method} ${request.url} ${status} ${message}`,
      )
    }

    response.status(status).type('application/json').send(errorResponse)
  }

  private mapStatusToCode(status: number): string {
    const map: Record<number, string> = {
      [HttpStatus.BAD_REQUEST]: 'VALIDATION_ERROR',
      [HttpStatus.UNAUTHORIZED]: 'AUTH_ERROR',
      [HttpStatus.FORBIDDEN]: 'FORBIDDEN',
      [HttpStatus.NOT_FOUND]: 'NOT_FOUND',
      [HttpStatus.PAYLOAD_TOO_LARGE]: 'PAYLOAD_TOO_LARGE',
      [HttpStatus.TOO_MANY_REQUESTS]: 'RATE_LIMIT_EXCEEDED',
      [HttpStatus.INTERNAL_SERVER_ERROR]: 'INTERNAL_ERROR',
    }
    return map[status] || 'UNKNOWN_ERROR'
  }
}
```

**验证**：

```bash
test -f packages/server/src/common/filters/all-exception.filter.ts
```

---

## 5. 创建 `LoggingInterceptor`（3 分钟）

**目标**：开发环境记录请求方法、路径、耗时、状态码；生产环境静默。

**文件**：`packages/server/src/common/interceptors/logging.interceptor.ts`

**动作**：写入以下代码：

```typescript
import {
  Injectable,
  NestInterceptor,
  ExecutionContext,
  CallHandler,
  Logger,
} from '@nestjs/common'
import { Observable } from 'rxjs'
import { tap } from 'rxjs/operators'

@Injectable()
export class LoggingInterceptor implements NestInterceptor {
  private readonly logger = new Logger(LoggingInterceptor.name)

  intercept(
    context: ExecutionContext,
    next: CallHandler,
  ): Observable<unknown> {
    const isProduction = process.env.NODE_ENV === 'production'
    if (isProduction) {
      return next.handle()
    }

    const request = context.switchToHttp().getRequest()
    const method = request.method
    const url = request.url
    const now = Date.now()

    this.logger.debug(`+++ Request: ${method} ${url}`)

    return next.handle().pipe(
      tap(() => {
        const response = context.switchToHttp().getResponse()
        const statusCode = response.statusCode || 200
        const duration = Date.now() - now
        this.logger.debug(
          `--- Response: ${method} ${url} ${statusCode} +${duration}ms`,
        )
      }),
    )
  }
}
```

**验证**：

```bash
test -f packages/server/src/common/interceptors/logging.interceptor.ts
```

---

## 6. 创建 `ZodValidationPipe`（3 分钟）

**目标**：基于 `nestjs-zod` 创建全局 Zod 验证管道，失败时抛出 422 并统一错误格式。

**文件**：`packages/server/src/common/pipes/zod-validation.pipe.ts`

**动作**：写入以下代码：

```typescript
import { createZodValidationPipe } from 'nestjs-zod'
import { ZodError } from 'zod'
import { UnprocessableEntityException } from '@nestjs/common'

export const ZodValidationPipe = createZodValidationPipe({
  createValidationException: (error: ZodError) => {
    const details = error.errors.map((e) => ({
      field: e.path.join('.'),
      issue: e.message,
    }))

    return new UnprocessableEntityException({
      code: 'VALIDATION_ERROR',
      message: '请求参数校验失败',
      details,
    })
  },
})
```

**验证**：

```bash
test -f packages/server/src/common/pipes/zod-validation.pipe.ts
```

---

## 7. 创建 `SpiderGuard`（3 分钟）

**目标**：基于 User-Agent 黑名单拦截常见爬虫/扫描器；开发环境放行。

**文件**：`packages/server/src/common/guards/spider.guard.ts`

**动作**：写入以下代码：

```typescript
import {
  CanActivate,
  ExecutionContext,
  Injectable,
  ForbiddenException,
} from '@nestjs/common'

@Injectable()
export class SpiderGuard implements CanActivate {
  private readonly spiderPattern =
    /(scrapy|httpclient|axios|python-requests|bot|spider|crawler|curl|wget|java)/i

  private readonly allowList = /(google|baidu|bing)/i

  canActivate(context: ExecutionContext): boolean {
    const isDev = process.env.NODE_ENV !== 'production'
    if (isDev) {
      return true
    }

    const request = context.switchToHttp().getRequest()
    const ua: string = request.headers['user-agent'] || ''

    const isSpider = this.spiderPattern.test(ua) && !this.allowList.test(ua)

    if (isSpider) {
      throw new ForbiddenException({
        code: 'FORBIDDEN',
        message: `爬虫访问被禁止，UA: ${ua}`,
      })
    }

    return true
  }
}
```

**验证**：

```bash
test -f packages/server/src/common/guards/spider.guard.ts
```

---

## 8. 更新 `src/bootstrap.ts`（5 分钟）

**目标**：注册 Helmet、CORS、全局前缀；集成 Throttler、SpiderGuard、LoggingInterceptor。

**文件**：`packages/server/src/bootstrap.ts`

**动作**：编辑该文件，确保包含以下内容（保留已有 NestJS 启动逻辑，追加安全配置）：

```typescript
import { NestFactory } from '@nestjs/core'
import {
  FastifyAdapter,
  NestFastifyApplication,
} from '@nestjs/platform-fastify'
import helmet from '@fastify/helmet'
import { AppModule } from './app.module'
import { LoggingInterceptor } from './common/interceptors/logging.interceptor'
import { SpiderGuard } from './common/guards/spider.guard'

export async function bootstrap() {
  const app = await NestFactory.create<NestFastifyApplication>(
    AppModule,
    new FastifyAdapter({ trustProxy: true }),
  )

  // 1. Helmet 安全头
  await app.register(helmet, {
    contentSecurityPolicy: false, // API 不需要 CSP
    hsts: process.env.NODE_ENV === 'production',
  })

  // 2. CORS（白名单 origin）
  const allowedOrigins = [
    'http://localhost:1420',
    'tauri://localhost',
    'http://localhost:3000',
  ]

  app.enableCors({
    origin: (origin, callback) => {
      if (!origin || allowedOrigins.includes(origin)) {
        callback(null, true)
        return
      }
      callback(new Error('Not allowed by CORS'), false)
    },
    methods: ['GET', 'POST', 'PATCH', 'DELETE'],
    allowedHeaders: ['Content-Type', 'Authorization'],
    credentials: true,
  })

  // 3. 全局前缀
  app.setGlobalPrefix('api')

  // 4. 开发环境日志拦截器
  if (process.env.NODE_ENV !== 'production') {
    app.useGlobalInterceptors(new LoggingInterceptor())
  }

  // 5. 爬虫防护（已在 AppModule 通过 APP_GUARD 注册，此处为双重保险）
  app.useGlobalGuards(new SpiderGuard())

  const port = process.env.PORT ? parseInt(process.env.PORT, 10) : 3000
  await app.listen(port, '0.0.0.0')
  console.log(`Server running on http://0.0.0.0:${port}`)
}
```

**验证**：

```bash
grep -q "@fastify/helmet" packages/server/src/bootstrap.ts
grep -q "enableCors" packages/server/src/bootstrap.ts
grep -q "setGlobalPrefix" packages/server/src/bootstrap.ts
```

---

## 9. 更新 `src/app.module.ts`（5 分钟）

**目标**：注册 `ThrottlerModule`、`ResponseInterceptor`、`AllExceptionsFilter`、`ZodValidationPipe`、`SpiderGuard`、`ThrottlerGuard`。

**文件**：`packages/server/src/app.module.ts`

**动作**：编辑该文件，确保根模块包含以下结构和 provider：

```typescript
import { Module } from '@nestjs/common'
import {
  APP_INTERCEPTOR,
  APP_FILTER,
  APP_GUARD,
  APP_PIPE,
} from '@nestjs/core'
import { ThrottlerModule, ThrottlerGuard } from '@nestjs/throttler'
import { ResponseInterceptor } from './common/interceptors/response.interceptor'
import { LoggingInterceptor } from './common/interceptors/logging.interceptor'
import { AllExceptionsFilter } from './common/filters/all-exception.filter'
import { ZodValidationPipe } from './common/pipes/zod-validation.pipe'
import { SpiderGuard } from './common/guards/spider.guard'

@Module({
  imports: [
    ThrottlerModule.forRoot([
      {
        name: 'default',
        ttl: 60000,
        limit: 60,
      },
      {
        name: 'auth',
        ttl: 60000,
        limit: 5,
      },
    ]),
    // ... 其他已有模块
  ],
  providers: [
    // 全局响应拦截器
    {
      provide: APP_INTERCEPTOR,
      useClass: ResponseInterceptor,
    },
    // 全局日志拦截器（开发环境）
    {
      provide: APP_INTERCEPTOR,
      useClass: LoggingInterceptor,
    },
    // 全局异常过滤器
    {
      provide: APP_FILTER,
      useClass: AllExceptionsFilter,
    },
    // 全局 Zod 验证管道
    {
      provide: APP_PIPE,
      useClass: ZodValidationPipe,
    },
    // 全局速率限制守卫
    {
      provide: APP_GUARD,
      useClass: ThrottlerGuard,
    },
    // 全局爬虫防护守卫
    {
      provide: APP_GUARD,
      useClass: SpiderGuard,
    },
  ],
})
export class AppModule {}
```

**验证**：

```bash
grep -q "ThrottlerModule" packages/server/src/app.module.ts
grep -q "ResponseInterceptor" packages/server/src/app.module.ts
grep -q "AllExceptionsFilter" packages/server/src/app.module.ts
grep -q "ZodValidationPipe" packages/server/src/app.module.ts
grep -q "SpiderGuard" packages/server/src/app.module.ts
grep -q "ThrottlerGuard" packages/server/src/app.module.ts
```

---

## 10. 类型检查（3 分钟）

**目标**：确保所有新增代码通过 TypeScript 类型检查。

**动作**：

```bash
cd packages/server
pnpm type-check
```

**验证**：命令退出码为 0，无 `error TS` 输出。

**若报错**：
- `Cannot find module '@fastify/helmet'` → 确认 `pnpm add @fastify/helmet` 已执行
- `Cannot find module 'nestjs-zod'` → 确认 `pnpm add nestjs-zod` 已执行
- `FastifyReply` 类型缺失 → 确认 `@nestjs/platform-fastify` 已安装（由 `i-08` 负责）

---

## 11. curl 验证（5 分钟）

**目标**：启动服务后，通过 curl 验证安全头、CORS、速率限制、响应格式。

**前置**：确保服务已启动

```bash
cd packages/server
pnpm dev:server &
# 等待 3 秒
sleep 3
```

### 11.1 安全头检查

```bash
curl -I http://localhost:3000/api/health
```

**期望响应头包含**：

```http
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
X-XSS-Protection: 1; mode=block
```

### 11.2 CORS 预检检查

```bash
curl -I -X OPTIONS \
  -H "Origin: http://localhost:1420" \
  -H "Access-Control-Request-Method: POST" \
  http://localhost:3000/api/auth/login
```

**期望响应头包含**：

```http
Access-Control-Allow-Origin: http://localhost:1420
Access-Control-Allow-Methods: GET,POST,PATCH,DELETE
Access-Control-Allow-Credentials: true
```

### 11.3 速率限制检查

```bash
# 快速请求 6 次，第 6 次应返回 429
for i in {1..6}; do
  curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/api/auth/login
done
```

**期望**：前 5 次返回 `400` 或 `404`，第 6 次返回 `429`。

### 11.4 统一响应格式检查

```bash
curl -s http://localhost:3000/api/health | jq .
```

**期望**：

```json
{
  "data": {
    "status": "ok"
  }
}
```

### 11.5 错误响应格式检查

```bash
curl -s -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "invalid"}' | jq .
```

**期望**：

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "请求参数校验失败"
  }
}
```

---

## 12. 提交（2 分钟）

**目标**：将安全基线代码提交到版本控制。

**动作**：

```bash
git add packages/server/src/common/decorators/bypass-response.decorator.ts

git add packages/server/src/common/interceptors/response.interceptor.ts

git add packages/server/src/common/interceptors/logging.interceptor.ts

git add packages/server/src/common/filters/all-exception.filter.ts

git add packages/server/src/common/pipes/zod-validation.pipe.ts

git add packages/server/src/common/guards/spider.guard.ts

git add packages/server/src/bootstrap.ts

git add packages/server/src/app.module.ts

git add packages/server/package.json

git add packages/server/pnpm-lock.yaml 2>/dev/null || true

git commit -m "feat(security): NestJS 安全基线

- 统一响应拦截器 ResponseInterceptor（{ data } 包装，数组自动包装，@BypassResponse 跳过）
- 全局异常过滤器 AllExceptionsFilter（统一错误格式，生产环境隐藏堆栈）
- 请求日志拦截器 LoggingInterceptor（仅开发环境）
- Zod 验证管道 ZodValidationPipe（基于 nestjs-zod，422 统一格式）
- 爬虫防护守卫 SpiderGuard（User-Agent 黑名单，开发环境放行）
- Bootstrap 安全配置：Helmet、CORS 白名单、Throttler 速率限制
- AppModule 全局注册：Interceptor / Filter / Guard / Pipe

对应 issue: i-10-nestjs-security"
```

---

## 附录 A：文件清单

| 文件路径 | 类型 | 说明 |
|----------|------|------|
| `packages/server/src/common/decorators/bypass-response.decorator.ts` | 新建 | `@BypassResponse()` 装饰器 |
| `packages/server/src/common/interceptors/response.interceptor.ts` | 新建 | 统一响应拦截器 |
| `packages/server/src/common/interceptors/logging.interceptor.ts` | 新建 | 请求日志拦截器 |
| `packages/server/src/common/filters/all-exception.filter.ts` | 新建 | 全局异常过滤器 |
| `packages/server/src/common/pipes/zod-validation.pipe.ts` | 新建 | Zod 验证管道 |
| `packages/server/src/common/guards/spider.guard.ts` | 新建 | 爬虫防护守卫 |
| `packages/server/src/bootstrap.ts` | 编辑 | 追加 Helmet、CORS、前缀、全局 Guard/Interceptor |
| `packages/server/src/app.module.ts` | 编辑 | 注册 ThrottlerModule、APP_INTERCEPTOR、APP_FILTER、APP_GUARD、APP_PIPE |
| `packages/server/package.json` | 编辑 | 新增 `@nestjs/throttler` `helmet` `@fastify/cors` `@fastify/helmet` `nestjs-zod` |

---

## 附录 B：参考模板文件

以下文件来自 `D:\projects\ai-stared-project\nest-http-prisma-zod`，可供编码时直接参考：

- `apps/core/src/common/interceptors/response.interceptor.ts`
- `apps/core/src/common/filters/all-exception.filter.ts`
- `apps/core/src/common/interceptors/logging.interceptor.ts`
- `apps/core/src/common/pipes/zod-validation.pipe.ts`
- `apps/core/src/common/guards/spider.guard.ts`
- `apps/core/src/bootstrap.ts`
- `apps/core/src/app.module.ts`
- `apps/core/src/common/decorators/http.decorator.ts`（`Bypass` 装饰器实现）
