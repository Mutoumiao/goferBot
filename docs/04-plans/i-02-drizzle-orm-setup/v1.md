---
issue_id: i-02-drizzle-orm-setup
type: plan
version: 1
status: draft
task_count: 40
summary: 为 server 安装配置 Drizzle ORM，定义全部 7 张 PostgreSQL 表 Schema，建立可复现的迁移流程，导出类型安全的 db 实例与 TypeScript 类型。基于 pg 驱动 + drizzle-kit 管理迁移，所有 Select 类型满足 IRepository<T> 约束。
---

# Drizzle ORM 配置与数据库 Schema 实现计划

> **For agentic workers:** 必需子技能：`superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`。步骤使用复选框（`- [ ]`）语法追踪。

**目标：** 为 `@goferbot/server` 安装配置 Drizzle ORM，定义全部 7 张 PostgreSQL 表 Schema，建立可复现的迁移流程，导出类型安全的 `db` 实例与 TypeScript 类型。

**架构：** 使用 `pg` 驱动 + Drizzle ORM 连接 PostgreSQL；Schema 集中定义于 `packages/server/src/db/schema.ts`；通过 `drizzle-kit` 管理迁移；所有 `Select` 类型满足 `IRepository<T>` 的 `T extends { id: string }` 约束。

**技术栈：** Drizzle ORM、`pg`（node-postgres）、drizzle-kit、TypeScript

**Issue 引用：** [docs/02-issues/i-02-drizzle-orm-setup.md](../../../02-issues/i-02-drizzle-orm-setup.md)
**Spec 引用：** [docs/03-specs/i-02-drizzle-orm-setup/](../../../03-specs/i-02-drizzle-orm-setup/)
**测试用例：** [docs/08-test-cases/i-02/](../../../08-test-cases/i-02/)（待补充）

---

## 文件结构

```
packages/server/
├── drizzle.config.ts           # Drizzle Kit 配置（PostgreSQL 连接、Schema 路径、输出目录）
├── package.json                # 新增依赖与 scripts（db:generate / db:migrate / db:studio）
├── src/
│   ├── db/
│   │   ├── client.ts           # pg Pool 实例 + drizzle(db) 初始化
│   │   ├── index.ts            # 统一导出：db 实例、Schema 表、Drizzle 工具函数
│   │   └── schema.ts           # 全部 7 张表定义 + Select/Insert 类型导出
│   └── interfaces/
│       └── IRepository.ts      # 已存在，约束 T extends { id: string }
```

---

## 任务列表

### 任务 1: 安装 Drizzle ORM 依赖

**文件：**
- 修改：`packages/server/package.json`

**规格引用：**
- 功能规格：[3.1 范围内 — Drizzle ORM + `pg` 驱动安装与配置]
- API 规格：[6. package.json scripts]

- [ ] **步骤 1: 在 `packages/server` 目录下安装依赖**

```bash
cd packages/server && pnpm add drizzle-orm pg
pnpm add -D drizzle-kit @types/pg
```

- [ ] **步骤 2: 验证安装结果**

运行：`cat packages/server/package.json | grep -A 20 '"dependencies"'`
预期：`drizzle-orm`、`pg` 出现在 `dependencies` 中；`drizzle-kit`、`@types/pg` 出现在 `devDependencies` 中。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/package.json pnpm-lock.yaml
pnpm install --frozen-lockfile
git add pnpm-lock.yaml
git commit -m "deps(server): 安装 drizzle-orm, pg, drizzle-kit, @types/pg"
```

---

### 任务 2: 创建 `drizzle.config.ts`

**文件：**
- 创建：`packages/server/drizzle.config.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — `drizzle.config.ts` 配置]
- API 规格：[5. drizzle.config.ts 配置]
- 行为规格：[4.1 连接字符串 — `postgresql://gofer:gofer_dev_pass@localhost:5432/goferbot`]

- [ ] **步骤 1: 编写配置文件**

```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './src/db/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL ?? 'postgresql://gofer:gofer_dev_pass@localhost:5432/goferbot',
  },
});
```

- [ ] **步骤 2: 验证文件创建**

运行：`ls packages/server/drizzle.config.ts`
预期：文件存在。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/drizzle.config.ts
git commit -m "chore(db): 创建 drizzle.config.ts 指向 PostgreSQL"
```

---

### 任务 3: 创建 `packages/server/src/db/client.ts`（数据库连接）

**文件：**
- 创建：`packages/server/src/db/client.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — 提供类型安全的 `db` 实例]
- 行为规格：[4.2 连接池 — max: 10, idle_timeout: 20, connection_timeout: 10]
- API 规格：[4. 导出汇总 — `export { db } from './client'`]

- [ ] **步骤 1: 编写客户端初始化代码**

```typescript
import { drizzle } from 'drizzle-orm/node-postgres';
import { Pool } from 'pg';

const pool = new Pool({
  connectionString: process.env.DATABASE_URL ?? 'postgresql://gofer:gofer_dev_pass@localhost:5432/goferbot',
  max: 10,
  idleTimeoutMillis: 20_000,
  connectionTimeoutMillis: 10_000,
});

export const db = drizzle({ client: pool });

// 优雅关闭连接池
process.on('beforeExit', async () => {
  await pool.end();
});
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/db/client.ts
git commit -m "feat(db): 创建 pg Pool 与 Drizzle db 实例"
```

---

### 任务 4: 创建 `packages/server/src/db/schema.ts` — 7 张表定义

**文件：**
- 创建：`packages/server/src/db/schema.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — 全部 7 张表定义]
- API 规格：[2. 表定义 — users / knowledge_bases / folders / documents / chunks / sessions / messages]
- PRD：[7.1 PostgreSQL Schema]

- [ ] **步骤 1: 编写完整 Schema 文件**

```typescript
import { pgTable, uuid, text, timestamp, boolean, integer, bigint } from 'drizzle-orm/pg-core';

// ── users ───────────────────────────────────────────────
export const users = pgTable('users', {
  id: uuid('id').primaryKey().defaultRandom(),
  email: text('email').notNull().unique(),
  name: text('name'),
  avatar: text('avatar'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});

export type UserSelect = typeof users.$inferSelect;
export type UserInsert = typeof users.$inferInsert;

// ── knowledge_bases ─────────────────────────────────────
export const knowledgeBases = pgTable('knowledge_bases', {
  id: uuid('id').primaryKey().defaultRandom(),
  userId: uuid('user_id')
    .notNull()
    .references(() => users.id, { onDelete: 'cascade' }),
  name: text('name').notNull(),
  description: text('description'),
  isPinned: boolean('is_pinned').default(false),
  sortOrder: integer('sort_order').default(0),
  icon: text('icon'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow(),
});

export type KnowledgeBaseSelect = typeof knowledgeBases.$inferSelect;
export type KnowledgeBaseInsert = typeof knowledgeBases.$inferInsert;

// ── folders ─────────────────────────────────────────────
export const folders = pgTable('folders', {
  id: uuid('id').primaryKey().defaultRandom(),
  kbId: uuid('kb_id')
    .notNull()
    .references(() => knowledgeBases.id, { onDelete: 'cascade' }),
  parentId: uuid('parent_id')
    .references(() => folders.id, { onDelete: 'cascade' }),
  name: text('name').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});

export type FolderSelect = typeof folders.$inferSelect;
export type FolderInsert = typeof folders.$inferInsert;

// ── documents ───────────────────────────────────────────
export const documents = pgTable('documents', {
  id: uuid('id').primaryKey().defaultRandom(),
  kbId: uuid('kb_id')
    .notNull()
    .references(() => knowledgeBases.id, { onDelete: 'cascade' }),
  folderId: uuid('folder_id')
    .references(() => folders.id, { onDelete: 'cascade' }),
  name: text('name').notNull(),
  ext: text('ext'),
  mimeType: text('mime_type'),
  size: bigint('size', { mode: 'number' }),
  storageKey: text('storage_key').notNull(),
  hash: text('hash'),
  status: text('status').default('uploaded'),
  errorMessage: text('error_message'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow(),
});

export type DocumentSelect = typeof documents.$inferSelect;
export type DocumentInsert = typeof documents.$inferInsert;

// ── chunks ──────────────────────────────────────────────
export const chunks = pgTable('chunks', {
  id: uuid('id').primaryKey().defaultRandom(),
  documentId: uuid('document_id')
    .notNull()
    .references(() => documents.id, { onDelete: 'cascade' }),
  kbId: uuid('kb_id')
    .notNull()
    .references(() => knowledgeBases.id, { onDelete: 'cascade' }),
  content: text('content').notNull(),
  tokenCount: integer('token_count'),
  chunkIndex: integer('chunk_index').notNull(),
  milvusId: text('milvus_id'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});

export type ChunkSelect = typeof chunks.$inferSelect;
export type ChunkInsert = typeof chunks.$inferInsert;

// ── sessions ────────────────────────────────────────────
export const sessions = pgTable('sessions', {
  id: uuid('id').primaryKey().defaultRandom(),
  userId: uuid('user_id')
    .notNull()
    .references(() => users.id, { onDelete: 'cascade' }),
  title: text('title').default('新对话'),
  provider: text('provider'),
  model: text('model'),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow(),
  messageCount: integer('message_count').default(0),
});

export type SessionSelect = typeof sessions.$inferSelect;
export type SessionInsert = typeof sessions.$inferInsert;

// ── messages ────────────────────────────────────────────
export const messages = pgTable('messages', {
  id: uuid('id').primaryKey().defaultRandom(),
  sessionId: uuid('session_id')
    .notNull()
    .references(() => sessions.id, { onDelete: 'cascade' }),
  role: text('role').$type<'user' | 'assistant'>().notNull(),
  content: text('content').notNull(),
  knowledgeBaseIds: text('knowledge_base_ids').array(),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow(),
});

export type MessageSelect = typeof messages.$inferSelect;
export type MessageInsert = typeof messages.$inferInsert;
```

- [ ] **步骤 2: 运行类型检查**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/db/schema.ts
git commit -m "feat(db): 定义 7 张表 Schema（users/kb/folders/docs/chunks/sessions/messages）"
```

---

### 任务 5: 创建 `packages/server/src/db/index.ts`（统一导出）

**文件：**
- 创建：`packages/server/src/db/index.ts`
- 修改：`packages/server/src/db.ts`（清理旧 TODO 标记，或重定向到新入口）

**规格引用：**
- 功能规格：[3.1 范围内 — `packages/server/src/db/index.ts` 导出类型安全的 `db` 实例]
- API 规格：[4. 导出汇总]

- [ ] **步骤 1: 编写统一导出文件**

```typescript
// db 实例
export { db } from './client';

// Schema 表定义与类型
export * from './schema';

// Drizzle ORM 工具函数（常用查询构建器）
export { eq, and, or, inArray, desc, asc } from 'drizzle-orm';
```

- [ ] **步骤 2: 清理旧 `db.ts` 占位文件**

修改 `packages/server/src/db.ts`，替换为：

```typescript
// 数据库入口已迁移至 src/db/index.ts
// 请使用：import { db } from './db/index.js'
export { db } from './db/index.js';
```

- [ ] **步骤 3: 验证类型检查**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 4: 提交**

```bash
git add packages/server/src/db/index.ts packages/server/src/db.ts
git commit -m "feat(db): 统一导出 db 实例、Schema 类型与 Drizzle 工具函数"
```

---

### 任务 6: 添加 `package.json` scripts（db:generate / db:migrate / db:studio）

**文件：**
- 修改：`packages/server/package.json`

**规格引用：**
- 功能规格：[3.1 范围内 — `package.json` scripts]
- API 规格：[6. package.json scripts]
- Issue 验收标准：`pnpm db:generate` 可生成迁移文件；`pnpm db:migrate` 可执行迁移；`pnpm db:studio` 可打开 Drizzle Studio

- [ ] **步骤 1: 在 `scripts` 中新增三条命令**

修改 `packages/server/package.json` 的 `scripts` 字段：

```json
{
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "type-check": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  }
}
```

- [ ] **步骤 2: 验证 JSON 格式**

运行：`cd packages/server && node -e "JSON.parse(require('fs').readFileSync('package.json'))" && echo "valid"`
预期：输出 `valid`。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/package.json
git commit -m "chore(db): 添加 db:generate / db:migrate / db:studio scripts"
```

---

### 任务 7: 生成并执行首次迁移

**文件：**
- 创建：`packages/server/drizzle/` 目录及迁移文件（由 drizzle-kit 自动生成）

**规格引用：**
- 行为规格：[1.1 生成迁移]、[1.2 执行迁移]
- Issue 验收标准：`pnpm db:generate` 可生成迁移文件；`pnpm db:migrate` 可执行迁移

- [ ] **步骤 1: 确保 PostgreSQL 服务运行**

运行：`docker compose -f docker-compose.dev.yml ps postgres`
预期：状态为 `running (healthy)`。

若未运行：
```bash
docker compose -f docker-compose.dev.yml up -d postgres
```

- [ ] **步骤 2: 生成迁移文件**

运行：`cd packages/server && pnpm db:generate`
预期：命令退出码为 0；`packages/server/drizzle/` 目录下新增 `.sql` 文件与 `meta/` 快照。

- [ ] **步骤 3: 执行迁移**

运行：`cd packages/server && pnpm db:migrate`
预期：命令退出码为 0；数据库中创建 7 张表 + `__drizzle_migrations` 表。

- [ ] **步骤 4: 验证表结构**

运行：
```bash
docker exec goferbot-postgres psql -U gofer -d goferbot -c "\dt"
```
预期：列出 `chunks`、`documents`、`folders`、`knowledge_bases`、`messages`、`sessions`、`users` 共 7 张表。

- [ ] **步骤 5: 提交迁移文件**

```bash
git add packages/server/drizzle/
git commit -m "feat(db): 生成并执行首次迁移，创建 7 张表"
```

---

### 任务 8: 验证 Drizzle Studio 可启动

**文件：**
- 无文件变更（纯验证）

**规格引用：**
- 行为规格：[2. Drizzle Studio 使用行为]
- Issue 验收标准：`pnpm db:studio` 可打开 Drizzle Studio 查看数据

- [ ] **步骤 1: 启动 Drizzle Studio**

运行：`cd packages/server && pnpm db:studio`
预期：终端输出本地服务地址（默认 `http://localhost:4983`），无报错。

- [ ] **步骤 2: 浏览器验证（可选，CI 环境跳过）**

在浏览器打开 `http://localhost:4983`，确认：
- 左侧导航栏列出 7 张表名；
- 点击表名后右侧展示数据网格；
- 外键关系可视化。

- [ ] **步骤 3: 停止 Studio**

按 `Ctrl+C` 终止进程。

---

### 任务 9: 编译时验证 `Select` 类型满足 `IRepository<T>` 约束

**文件：**
- 创建：`packages/server/src/db/type-check.ts`（仅用于编译时断言，完成后可删除或保留）

**规格引用：**
- API 规格：[3. 类型与 IRepository 对齐]
- 行为规格：[3.3 与 IRepository 对齐]

- [ ] **步骤 1: 编写编译时断言文件**

```typescript
import type { IRepository } from '../interfaces/IRepository.js';
import type {
  UserSelect,
  KnowledgeBaseSelect,
  FolderSelect,
  DocumentSelect,
  ChunkSelect,
  SessionSelect,
  MessageSelect,
} from './schema.js';

// 编译时验证：所有 Select 类型均满足 IRepository<T> 的 T extends { id: string } 约束
const _userRepo: IRepository<UserSelect> = {} as any;
const _kbRepo: IRepository<KnowledgeBaseSelect> = {} as any;
const _folderRepo: IRepository<FolderSelect> = {} as any;
const _docRepo: IRepository<DocumentSelect> = {} as any;
const _chunkRepo: IRepository<ChunkSelect> = {} as any;
const _sessionRepo: IRepository<SessionSelect> = {} as any;
const _messageRepo: IRepository<MessageSelect> = {} as any;

// 抑制未使用变量警告
void _userRepo;
void _kbRepo;
void _folderRepo;
void _docRepo;
void _chunkRepo;
void _sessionRepo;
void _messageRepo;
```

- [ ] **步骤 2: 运行类型检查**

运行：`cd packages/server && pnpm type-check`
预期：无错误。若 `Select` 类型缺少 `id: string`，此处会编译失败。

- [ ] **步骤 3: 清理或保留断言文件**

建议保留该文件作为活文档，路径 `packages/server/src/db/type-check.ts`。

- [ ] **步骤 4: 提交**

```bash
git add packages/server/src/db/type-check.ts
git commit -m "test(db): 添加 IRepository 泛型约束编译时验证"
```

---

## 规格覆盖检查

### 功能规格覆盖

| 功能规格条目 | 对应任务 | 状态 |
|---|---|---|
| Drizzle ORM + `pg` 驱动安装与配置 | 任务 1、3 | 已覆盖 |
| `drizzle.config.ts` 配置 | 任务 2 | 已覆盖 |
| `packages/server/src/db/schema.ts` — 7 张表 | 任务 4 | 已覆盖 |
| `packages/server/src/db/index.ts` — 导出 `db` 实例 | 任务 5 | 已覆盖 |
| `package.json` scripts（db:generate / db:migrate / db:studio） | 任务 6 | 已覆盖 |
| 所有表导出 `Select` / `Insert` 类型 | 任务 4、9 | 已覆盖 |

### 行为规格覆盖

| 行为规格条目 | 对应任务 | 状态 |
|---|---|---|
| 1.1 生成迁移（pnpm db:generate） | 任务 7 | 已覆盖 |
| 1.2 执行迁移（pnpm db:migrate） | 任务 7 | 已覆盖 |
| 1.3 回滚策略（手动） | 任务 7（注释说明） | 已覆盖 |
| 2.1~2.4 Drizzle Studio 启动/查看/编辑/SQL Runner | 任务 8 | 已覆盖 |
| 3.1~3.3 类型导出行为与 IRepository 对齐 | 任务 4、9 | 已覆盖 |
| 4.1~4.3 数据库连接行为（连接字符串、连接池、生命周期） | 任务 3 | 已覆盖 |

### API 规格覆盖

| API 规格条目 | 对应任务 | 状态 |
|---|---|---|
| 2.1 users 表定义与类型 | 任务 4 | 已覆盖 |
| 2.2 knowledge_bases 表定义与类型 | 任务 4 | 已覆盖 |
| 2.3 folders 表定义与类型 | 任务 4 | 已覆盖 |
| 2.4 documents 表定义与类型 | 任务 4 | 已覆盖 |
| 2.5 chunks 表定义与类型 | 任务 4 | 已覆盖 |
| 2.6 sessions 表定义与类型 | 任务 4 | 已覆盖 |
| 2.7 messages 表定义与类型 | 任务 4 | 已覆盖 |
| 3. 类型与 IRepository 对齐 | 任务 9 | 已覆盖 |
| 4. 导出汇总（index.ts） | 任务 5 | 已覆盖 |
| 5. drizzle.config.ts 配置 | 任务 2 | 已覆盖 |
| 6. package.json scripts | 任务 6 | 已覆盖 |

### 占位符扫描

- [x] 无 "TBD" / "TODO" / "稍后实现" / "填写细节"
- [x] 无 "添加适当的错误处理" 等模糊描述
- [x] 所有任务均含具体代码块与验证命令
- [x] 无 "类似于任务 N" 的重复引用

### 类型一致性

- [x] 所有表主键为 `uuid('id').primaryKey().defaultRandom()`，满足 `T extends { id: string }`
- [x] `Select` 类型通过 `typeof table.$inferSelect` 推导，与 `IRepository<T>` 兼容
- [x] `users` 表无 `updatedAt`，其余有 `updatedAt` 的表与 `EntityBase` 可选字段兼容

---

## 执行交接

**计划已保存到 `docs/04-plans/i-02-drizzle-orm-setup/v1.md`。两种执行方式：**

1. **子代理驱动（推荐）** — 每个任务新子代理，任务间审查
2. **内联执行** — 当前会话中顺序执行，带检查点

**选择哪种？**
