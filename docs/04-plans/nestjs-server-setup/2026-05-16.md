# 执行计划：NestJS 服务器初始化

> 对应 Issue：`docs/02-issues/i-08-nestjs-server-setup.md`
> 日期：2026-05-16
> 技术栈：NestJS 10 + Fastify + Prisma + Zod

---

## 前置条件

- 已阅读 `docs/03-specs/features/nestjs-server-setup/feature-spec.md`
- 已阅读 `docs/03-specs/features/nestjs-server-setup/api-spec.md`
- 参考模板项目：`D:\projects\ai-stared-project\nest-http-prisma-zod`
- 当前存在 Hono 旧服务端代码，需完全替换

---

## Step 1 — 清理旧服务端代码

**目标**：移除 Hono/Drizzle 旧代码，为 NestJS 重建腾出空间。

**动作**：

```bash
rm -rf packages/server/src/*
rm -rf packages/server/drizzle
rm -f packages/server/drizzle.config.ts
rm -rf packages/server/dist
rm -rf packages/server/node_modules
rm -f packages/server/package.json
rm -f packages/server/tsconfig.json
```

**验证**：

```bash
ls packages/server/
# 应仅剩 .env.example、.gitignore、.npmrc、scripts/
```

---

## Step 2 — 创建 package.json

**目标**：建立 NestJS 10 + Fastify 的依赖清单。

**文件**：`packages/server/package.json`

```json
{
  "name": "@goferbot/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nest start --watch",
    "build": "nest build",
    "start": "cross-env NODE_ENV=production node dist/main.js",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/config": "^3.1.1",
    "@nestjs/core": "^10.3.0",
    "@nestjs/platform-fastify": "^10.3.0",
    "@fastify/cors": "^9.0.0",
    "@fastify/helmet": "^11.1.0",
    "fastify": "^4.26.0",
    "nestjs-zod": "^3.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.3.0",
    "@nestjs/schematics": "^10.1.0",
    "@types/node": "^20.10.0",
    "cross-env": "^7.0.3",
    "typescript": "^5.3.3"
  }
}
```

**动作**：

```bash
cat > packages/server/package.json << 'EOF'
{ ... }
EOF
```

---

## Step 3 — 创建 TypeScript 与 Nest CLI 配置

### 3.1 tsconfig.json

**文件**：`packages/server/tsconfig.json`

```json
{
  "compilerOptions": {
    "module": "commonjs",
    "declaration": true,
    "removeComments": true,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "allowSyntheticDefaultImports": true,
    "target": "ES2021",
    "sourceMap": true,
    "outDir": "./dist",
    "baseUrl": ".",
    "incremental": true,
    "skipLibCheck": true,
    "strictNullChecks": true,
    "noImplicitAny": false,
    "strictBindCallApply": false,
    "forceConsistentCasingInFileNames": false,
    "noFallthroughCasesInSwitch": false,
    "resolveJsonModule": true,
    "esModuleInterop": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

### 3.2 nest-cli.json

**文件**：`packages/server/nest-cli.json`

```json
{
  "$schema": "https://json.schemastore.org/nest-cli",
  "collection": "@nestjs/schematics",
  "sourceRoot": "src",
  "compilerOptions": {
    "deleteOutDir": true
  }
}
```

**动作**：

```bash
cat > packages/server/tsconfig.json << 'EOF'
{ ... }
EOF

cat > packages/server/nest-cli.json << 'EOF'
{ ... }
EOF
```

---

## Step 4 — 安装依赖

**目标**：下载 NestJS 与 Fastify 相关包。

**动作**：

```bash
cd packages/server && pnpm install
```

**验证**：

```bash
ls packages/server/node_modules/@nestjs/core
ls packages/server/node_modules/fastify
```

---

## Step 5 — 创建目录结构

**目标**：建立 NestJS 标准目录层级。

**动作**：

```bash
mkdir -p packages/server/src/common/{interceptors,filters,pipes,guards}
mkdir -p packages/server/src/processors/{database,cache,helper}
mkdir -p packages/server/src/modules/health
```

---

## Step 6 — 创建 Fastify 适配器

**目标**：配置 FastifyAdapter，注册全局 Helmet 与 CORS。

**文件**：`packages/server/src/common/adapter/fastify.adapter.ts`

```typescript
import { FastifyAdapter } from '@nestjs/platform-fastify'

export const fastifyApp = new FastifyAdapter({
  trustProxy: true,
})

fastifyApp.getInstance().addHook('onRequest', (request, reply, done) => {
  const url = request.url
  if (url.match(/favicon\.ico$/) || url.match(/manifest\.json$/)) {
    return reply.code(204).send()
  }
  done()
})
```

---

## Step 7 — 创建启动配置 bootstrap.ts

**目标**：初始化 NestJS 应用，挂载 Helmet、CORS、全局前缀 `/api`。

**文件**：`packages/server/src/bootstrap.ts`

```typescript
import { NestFactory } from '@nestjs/core'
import { NestFastifyApplication } from '@nestjs/platform-fastify'
import helmet from '@fastify/helmet'
import cors from '@fastify/cors'

import { AppModule } from './app.module'
import { fastifyApp } from './common/adapter/fastify.adapter'

export async function bootstrap() {
  const app = await NestFactory.create<NestFastifyApplication>(
    AppModule,
    fastifyApp,
  )

  await app.register(helmet, {
    contentSecurityPolicy: false,
  })

  await app.register(cors, {
    origin: process.env.CORS_ORIGIN || '*',
    credentials: true,
  })

  app.setGlobalPrefix('api')

  const port = parseInt(process.env.PORT || '3000', 10)
  await app.listen(port, '0.0.0.0')

  console.log(`Server running on http://localhost:${port}`)
}
```

---

## Step 8 — 创建应用入口 main.ts

**目标**：Node 进程入口，调用 bootstrap。

**文件**：`packages/server/src/main.ts`

```typescript
import { bootstrap } from './bootstrap'

async function main() {
  await bootstrap()
}

main().catch((err) => {
  console.error('Failed to start server:', err)
  process.exit(1)
})
```

---

## Step 9 — 创建根模块 app.module.ts

**目标**：聚合 ConfigModule、LoggerModule、基础设施模块及健康检查模块。

**文件**：`packages/server/src/app.module.ts`

```typescript
import { Module } from '@nestjs/common'
import { ConfigModule } from '@nestjs/config'

import { HealthModule } from './modules/health/health.module'
import { DatabaseModule } from './processors/database/database.module'
import { CacheModule } from './processors/cache/cache.module'
import { HelperModule } from './processors/helper/helper.module'

@Module({
  imports: [
    ConfigModule.forRoot({
      isGlobal: true,
      envFilePath: ['.env'],
    }),
    DatabaseModule,
    CacheModule,
    HelperModule,
    HealthModule,
  ],
})
export class AppModule {}
```

---

## Step 10 — 创建统一响应拦截器

**目标**：对 `/api` 下的成功响应统一包装为 `{ data }`。

**文件**：`packages/server/src/common/interceptors/response.interceptor.ts`

```typescript
import {
  CallHandler,
  ExecutionContext,
  Injectable,
  NestInterceptor,
} from '@nestjs/common'
import { Observable } from 'rxjs'
import { map } from 'rxjs/operators'

export interface Response<T> {
  data: T
}

@Injectable()
export class ResponseInterceptor<T>
  implements NestInterceptor<T, Response<T>>
{
  intercept(
    context: ExecutionContext,
    next: CallHandler<T>,
  ): Observable<Response<T>> {
    return next.handle().pipe(
      map((data) => {
        if (typeof data === 'undefined') {
          context.switchToHttp().getResponse().status(204)
          return data as any
        }
        return { data } as Response<T>
      }),
    )
  }
}
```

---

## Step 11 — 创建全局异常过滤器

**目标**：统一错误响应格式 `{ statusCode, message, error }`。

**文件**：`packages/server/src/common/filters/all-exception.filter.ts`

```typescript
import {
  ArgumentsHost,
  Catch,
  ExceptionFilter,
  HttpException,
  HttpStatus,
  Logger,
} from '@nestjs/common'
import { FastifyReply, FastifyRequest } from 'fastify'

@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  private readonly logger = new Logger(AllExceptionsFilter.name)

  catch(exception: unknown, host: ArgumentsHost) {
    const ctx = host.switchToHttp()
    const response = ctx.getResponse<FastifyReply>()
    const request = ctx.getRequest<FastifyRequest>()

    const status =
      exception instanceof HttpException
        ? exception.getStatus()
        : HttpStatus.INTERNAL_SERVER_ERROR

    const message =
      exception instanceof HttpException
        ? (exception.getResponse() as any).message || exception.message
        : 'Internal server error'

    const error =
      exception instanceof HttpException
        ? exception.name
        : 'Internal Server Error'

    this.logger.error(
      `HTTP ${status} | ${request.method} ${request.url} | ${message}`,
    )

    response.status(status).send({
      statusCode: status,
      message,
      error,
    })
  }
}
```

---

## Step 12 — 创建 Zod 验证管道

**目标**：基于 `nestjs-zod` 提供全局请求体验证。

**文件**：`packages/server/src/common/pipes/zod-validation.pipe.ts`

```typescript
import { createZodValidationPipe } from 'nestjs-zod'
import { ZodError } from 'zod'
import { UnprocessableEntityException } from '@nestjs/common'

export const ZodValidationPipe = createZodValidationPipe({
  createValidationException: (error: ZodError) => {
    const firstError = error.errors[0]
    return new UnprocessableEntityException(
      `\`${firstError.path.join('.')}\`: ${firstError.message}`,
    )
  },
})
```

---

## Step 13 — 创建 JWT 认证守卫（占位）

**目标**：建立 AuthGuard 骨架，后续由 b-01-auth-api 填充 JWT 逻辑。

**文件**：`packages/server/src/common/guards/auth.guard.ts`

```typescript
import {
  CanActivate,
  ExecutionContext,
  Injectable,
  UnauthorizedException,
} from '@nestjs/common'

@Injectable()
export class AuthGuard implements CanActivate {
  async canActivate(context: ExecutionContext): Promise<boolean> {
    const request = context.switchToHttp().getRequest()
    const token = request.headers.authorization

    if (!token) {
      throw new UnauthorizedException('Missing authorization token')
    }

    // TODO: 接入 JWT 验证逻辑（由 b-01-auth-api 实现）
    request.user = { id: 'placeholder' }
    return true
  }
}
```

---

## Step 14 — 创建基础设施模块（占位）

### 14.1 DatabaseModule

**文件**：`packages/server/src/processors/database/database.module.ts`

```typescript
import { Global, Module } from '@nestjs/common'

@Global()
@Module({
  providers: [],
  exports: [],
})
export class DatabaseModule {}
```

### 14.2 CacheModule

**文件**：`packages/server/src/processors/cache/cache.module.ts`

```typescript
import { Global, Module } from '@nestjs/common'

@Global()
@Module({
  providers: [],
  exports: [],
})
export class CacheModule {}
```

### 14.3 HelperModule

**文件**：`packages/server/src/processors/helper/helper.module.ts`

```typescript
import { Global, Module } from '@nestjs/common'

@Global()
@Module({
  providers: [],
  exports: [],
})
export class HelperModule {}
```

---

## Step 15 — 创建健康检查模块

**目标**：提供 `GET /health` 端点，返回 `{ status, timestamp, version }`。

### 15.1 HealthController

**文件**：`packages/server/src/modules/health/health.controller.ts`

```typescript
import { Controller, Get } from '@nestjs/common'
import { version } from '../../../package.json'

@Controller('health')
export class HealthController {
  @Get()
  check() {
    return {
      status: 'ok',
      timestamp: new Date().toISOString(),
      version,
    }
  }
}
```

### 15.2 HealthModule

**文件**：`packages/server/src/modules/health/health.module.ts`

```typescript
import { Module } from '@nestjs/common'
import { HealthController } from './health.controller'

@Module({
  controllers: [HealthController],
})
export class HealthModule {}
```

---

## Step 16 — 更新 .env.example

**目标**：提供 NestJS 环境变量模板。

**文件**：`packages/server/.env.example`

```bash
# 运行环境: development | production
NODE_ENV=development

# 服务端口
PORT=3000

# CORS 来源（开发默认允许所有）
CORS_ORIGIN=*

# 日志级别: verbose | debug | log | warn | error
LOG_LEVEL=log

# 数据库连接（后续由 i-02-prisma-setup 使用）
DATABASE_URL=postgresql://user:password@localhost:5432/goferbot

# Redis 连接（后续由 cache 模块使用）
REDIS_URL=redis://localhost:6379
```

---

## Step 17 — 根 package.json 脚本修正

**目标**：确保 `pnpm dev:server` 正确指向 NestJS 启动命令。

**文件**：`package.json`（根目录）

修改 `scripts` 中的 `dev:server`：

```json
{
  "scripts": {
    "dev:server": "pnpm --filter @goferbot/server dev",
    "type-check": "pnpm -r type-check"
  }
}
```

> 根目录 `package.json` 已包含正确脚本，无需修改，仅确认即可。

---

## Step 18 — 类型检查

**目标**：确保 TypeScript 编译无错误。

**动作**：

```bash
pnpm --filter @goferbot/server type-check
```

**期望**：命令返回退出码 0，无类型错误。

---

## Step 19 — 启动服务验证

**目标**：确认 NestJS 应用正常启动，健康检查返回 200。

### 19.1 启动服务

```bash
pnpm dev:server
```

### 19.2 验证健康检查

另开终端：

```bash
curl http://localhost:3000/health
```

**期望响应**：

```json
{
  "status": "ok",
  "timestamp": "2026-05-16T12:34:56.789Z",
  "version": "0.1.0"
}
```

### 19.3 验证全局前缀

```bash
curl http://localhost:3000/api/health
# 应返回 404，因为 health 模块未挂载在 api 前缀下
```

> 说明：`/health` 直接挂载在根路径，不受 `/api` 前缀影响。业务模块后续挂载在 `/api` 下。

---

## Step 20 — 提交代码

**目标**：保存所有变更到 git。

**动作**：

```bash
git add packages/server/
git add docs/04-plans/nestjs-server-setup/2026-05-16.md
git commit -m "feat(server): NestJS 10 + Fastify 服务器初始化

- 替换 Hono/Drizzle 为 NestJS 10 + Fastify
- 建立标准目录结构（common / processors / modules）
- 全局配置：Helmet、CORS、全局前缀 /api
- 通用组件：响应拦截器、异常过滤器、Zod 验证管道、JWT 守卫占位
- 基础设施模块：DatabaseModule、CacheModule、HelperModule（占位）
- 健康检查端点 GET /health
- pnpm type-check 通过，pnpm dev:server 启动成功"
```

---

## 回滚方案

若执行中途失败，可一键回退到 Hono 旧版本：

```bash
git checkout -- packages/server/
```

> 建议在执行 Step 1 前先创建分支：`git checkout -b feat/nestjs-server-setup`

---

## 验收清单

| 检查项 | 状态 |
|--------|------|
| `src/main.ts` — NestJS 应用入口（FastifyAdapter） | 待验证 |
| `src/app.module.ts` — 根模块（ConfigModule + 基础设施模块） | 待验证 |
| `src/bootstrap.ts` — 启动配置（Helmet、CORS、全局前缀 /api） | 待验证 |
| `.env.example` — 环境变量模板 | 待验证 |
| `package.json` — 依赖完整（@nestjs/*、fastify、@fastify/*） | 待验证 |
| `tsconfig.json` / `nest-cli.json` — 配置正确 | 待验证 |
| `pnpm type-check` 通过 | 待验证 |
| `pnpm dev:server` 启动成功 | 待验证 |
| `curl http://localhost:3000/health` 返回 200 | 待验证 |
