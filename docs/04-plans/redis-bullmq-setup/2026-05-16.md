# Redis + BullMQ 缓存与队列层实现计划

> **For agentic workers:** 必需子技能：`superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`。步骤使用复选框（`- [ ]`）语法追踪。

**目标：** 为 `@goferbot/server` 安装配置 Redis 连接与 BullMQ 队列系统，建立异步任务处理框架，提供 `document-processing` 和 `embedding-generation` 队列、Worker 占位处理器、任务投递与状态查询接口。

**架构：** 使用 `ioredis` 创建单一 Redis 连接实例供 BullMQ 复用；在 `packages/server/src/queue/` 目录下按职责拆分四个文件（redis.ts / queues.ts / workers.ts / jobs.ts）；Worker 占位实现预留 `parse → chunk → embed` 三阶段状态更新接口，Phase 5 替换具体逻辑即可；通过 Hono 路由暴露两个 HTTP 端点供外部查询任务状态与队列统计。

**技术栈：** bullmq、ioredis、TypeScript、Hono

**Issue 引用：** [docs/02-issues/i-05-redis-bullmq-setup.md](../../../02-issues/i-05-redis-bullmq-setup.md)
**Spec 引用：** [docs/03-specs/features/redis-bullmq-setup/](../../../03-specs/features/redis-bullmq-setup/)
**测试用例：** [docs/08-test-cases/i-05/](../../../08-test-cases/i-05/)（待补充）

---

## 文件结构

```
packages/server/
├── package.json                          # 新增 bullmq、ioredis 依赖
├── src/
│   ├── queue/
│   │   ├── redis.ts                      # ioredis 连接实例 + 启动时 ping 检查
│   │   ├── queues.ts                     # Queue 定义（document-processing / embedding-generation）
│   │   ├── workers.ts                    # Worker 注册框架 + 占位处理器 + 优雅关闭
│   │   └── jobs.ts                       # 任务投递辅助函数 + 状态查询 + 队列统计
│   ├── routes/
│   │   └── jobs.ts                       # GET /api/jobs/:jobId/status, GET /api/jobs/stats
│   └── index.ts                          # 引入 queue 模块触发初始化
├── .env.example                          # 新增 QUEUE_CONCURRENCY
```

---

## 任务列表

### 任务 1: 安装 bullmq + ioredis 依赖

**文件：**
- 修改：`packages/server/package.json`

**规格引用：**
- 功能规格：[3.1 范围内 — Redis 客户端与 BullMQ 安装]
- API 规格：[4. 依赖 — bullmq、ioredis]

- [ ] **步骤 1: 在 `packages/server` 目录下安装依赖**

```bash
cd packages/server && pnpm add bullmq ioredis
```

- [ ] **步骤 2: 验证安装结果**

运行：`cat packages/server/package.json | grep -A 10 '"dependencies"'`
预期：`bullmq`、`ioredis` 出现在 `dependencies` 中。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/package.json pnpm-lock.yaml
git commit -m "deps(server): 安装 bullmq, ioredis"
```

---

### 任务 2: 创建 `packages/server/src/queue/redis.ts` — Redis 连接配置

**文件：**
- 创建：`packages/server/src/queue/redis.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — Redis 客户端连接配置与实例导出]
- 行为规格：[1.1 服务启动时创建 Queue 和 Worker 实例 — ping 检查连通性]
- API 规格：[1.2 Redis 连接实例]

- [ ] **步骤 1: 编写 Redis 连接文件**

```typescript
import { Redis } from 'ioredis'

export const redis = new Redis({
  host: process.env.REDIS_HOST || 'localhost',
  port: Number(process.env.REDIS_PORT) || 6379,
  maxRetriesPerRequest: null, // BullMQ 要求必须设为 null
})

export async function checkRedisConnection(): Promise<void> {
  try {
    await redis.ping()
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err)
    throw new Error(`Redis connection failed: ${message}`)
  }
}
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/queue/redis.ts
git commit -m "feat(queue): 创建 ioredis 连接实例与启动时连通性检查"
```

---

### 任务 3: 创建 `packages/server/src/queue/queues.ts` — Queue 实例定义

**文件：**
- 创建：`packages/server/src/queue/queues.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — BullMQ Queue 初始化与命名规范]
- 行为规格：[1.1 服务启动时创建 Queue 和 Worker 实例]
- API 规格：[1.3 Queue 定义]

- [ ] **步骤 1: 编写 Queue 定义文件**

```typescript
import { Queue } from 'bullmq'
import { redis } from './redis.js'

export interface DocumentJobData {
  documentId: string
  type: 'parse' | 'reindex'
}

export interface EmbeddingJobData {
  chunkIds: string[]
  kbId: string
}

export const documentQueue = new Queue<DocumentJobData>('document-processing', {
  connection: redis,
  defaultJobOptions: {
    attempts: 3,
    backoff: {
      type: 'exponential',
      delay: 2000,
    },
    removeOnComplete: 100,
    removeOnFail: 50,
  },
})

export const embeddingQueue = new Queue<EmbeddingJobData>('embedding-generation', {
  connection: redis,
  defaultJobOptions: {
    attempts: 3,
    backoff: {
      type: 'exponential',
      delay: 2000,
    },
    removeOnComplete: 100,
    removeOnFail: 50,
  },
})

export type QueueName = 'document-processing' | 'embedding-generation'
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/queue/queues.ts
git commit -m "feat(queue): 定义 document-processing 与 embedding-generation 队列"
```

---

### 任务 4: 创建 `packages/server/src/queue/workers.ts` — Worker 框架与占位处理器

**文件：**
- 创建：`packages/server/src/queue/workers.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — BullMQ Worker 框架与注册机制]
- 行为规格：[3.1 Worker 消费任务]、[3.2 占位处理流程（parse 类型）]、[3.3 进度报告]、[4.1 失败重试]、[4.3 失败状态同步]、[6.3 关闭时]
- API 规格：[1.4 Worker 注册框架]

- [ ] **步骤 1: 编写 Worker 框架文件**

```typescript
import { Worker, Job } from 'bullmq'
import { redis } from './redis.js'
import { documentQueue, embeddingQueue, DocumentJobData, EmbeddingJobData } from './queues.js'

const concurrency = Number(process.env.QUEUE_CONCURRENCY) || 2

// 占位处理器：document-processing
async function processDocumentJob(job: Job<DocumentJobData>): Promise<void> {
  const { documentId, type } = job.data

  if (type === 'parse') {
    // parse 阶段
    await job.updateProgress(10)
    // Phase 5: update documents.status = 'parsing'
    await new Promise((r) => setTimeout(r, 100))

    // chunk 阶段
    await job.updateProgress(40)
    // Phase 5: update documents.status = 'chunking'
    await new Promise((r) => setTimeout(r, 100))

    // embed 阶段
    await job.updateProgress(70)
    // Phase 5: update documents.status = 'indexing'
    await new Promise((r) => setTimeout(r, 100))

    // 完成
    await job.updateProgress(100)
    // Phase 5: update documents.status = 'ready'
    return
  }

  if (type === 'reindex') {
    await job.updateProgress(100)
    return
  }

  throw new Error(`Unknown job type: ${type}`)
}

// 占位处理器：embedding-generation
async function processEmbeddingJob(job: Job<EmbeddingJobData>): Promise<void> {
  const { chunkIds, kbId } = job.data
  // Phase 5 前为占位实现
  await job.updateProgress(100)
}

export const documentWorker = new Worker<DocumentJobData>(
  'document-processing',
  processDocumentJob,
  { connection: redis, concurrency }
)

export const embeddingWorker = new Worker<EmbeddingJobData>(
  'embedding-generation',
  processEmbeddingJob,
  { connection: redis, concurrency }
)

// 优雅关闭
function gracefulShutdown(signal: string) {
  console.log(`Received ${signal}, shutting down workers...`)
  Promise.all([
    documentWorker.close(),
    embeddingWorker.close(),
  ]).then(() => {
    process.exit(0)
  })
}

process.on('SIGTERM', () => gracefulShutdown('SIGTERM'))
process.on('SIGINT', () => gracefulShutdown('SIGINT'))
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/queue/workers.ts
git commit -m "feat(queue): 创建 Worker 框架与占位处理器（parse/chunk/embed 三阶段）"
```

---

### 任务 5: 创建 `packages/server/src/queue/jobs.ts` — 任务投递与状态查询

**文件：**
- 创建：`packages/server/src/queue/jobs.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — 任务投递接口、任务状态查询接口]
- 行为规格：[2.1 添加文档处理任务]、[5.1 任务状态查询]、[5.2 队列统计查询]
- API 规格：[1.5 任务投递与状态查询辅助函数]

- [ ] **步骤 1: 编写 jobs.ts 辅助函数文件**

```typescript
import { Job } from 'bullmq'
import { documentQueue, embeddingQueue, DocumentJobData, EmbeddingJobData } from './queues.js'

export async function addDocumentJob(
  documentId: string,
  type: DocumentJobData['type']
): Promise<string> {
  const job = await documentQueue.add('process-document', { documentId, type })
  if (!job.id) {
    throw new Error('Failed to add document job: job.id is undefined')
  }
  return job.id
}

export async function addEmbeddingJob(
  chunkIds: string[],
  kbId: string
): Promise<string> {
  const job = await embeddingQueue.add('generate-embedding', { chunkIds, kbId })
  if (!job.id) {
    throw new Error('Failed to add embedding job: job.id is undefined')
  }
  return job.id
}

export interface JobStatus {
  id: string
  state: 'waiting' | 'active' | 'completed' | 'failed' | 'delayed' | 'unknown'
  progress: number
  attemptsMade: number
  failedReason?: string
  finishedOn?: number
  processedOn?: number
}

export async function getJobStatus(jobId: string): Promise<JobStatus | null> {
  let job = await Job.fromId(documentQueue, jobId)

  if (!job) {
    job = await Job.fromId(embeddingQueue, jobId)
  }

  if (!job) return null

  const state = await job.getState()

  return {
    id: job.id!,
    state: state as JobStatus['state'],
    progress: (job.progress as number) ?? 0,
    attemptsMade: job.attemptsMade ?? 0,
    failedReason: job.failedReason ?? undefined,
    finishedOn: job.finishedOn ?? undefined,
    processedOn: job.processedOn ?? undefined,
  }
}

export interface QueueStats {
  name: string
  waiting: number
  active: number
  completed: number
  failed: number
  delayed: number
}

export async function getQueueStats(): Promise<QueueStats[]> {
  const queues = [
    { name: 'document-processing', queue: documentQueue },
    { name: 'embedding-generation', queue: embeddingQueue },
  ]

  return Promise.all(
    queues.map(async ({ name, queue }) => {
      const [waiting, active, completed, failed, delayed] = await Promise.all([
        queue.getWaitingCount(),
        queue.getActiveCount(),
        queue.getCompletedCount(),
        queue.getFailedCount(),
        queue.getDelayedCount(),
      ])

      return { name, waiting, active, completed, failed, delayed }
    })
  )
}
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/queue/jobs.ts
git commit -m "feat(queue): 添加任务投递与状态查询辅助函数"
```

---

### 任务 6: 在 `src/index.ts` 中引入队列模块并执行 Redis 连通性检查

**文件：**
- 修改：`packages/server/src/index.ts`

**规格引用：**
- 行为规格：[1.1 服务启动时创建 Queue 和 Worker 实例 — ping 检查连通性]
- Issue 验收标准：启动时检查 Redis 连接，失败时给出明确错误

- [ ] **步骤 1: 修改入口文件引入 queue 模块**

在 `packages/server/src/index.ts` 顶部添加以下 import（放在现有 import 之后）：

```typescript
import { checkRedisConnection } from './queue/redis.js'
import './queue/workers.js'
```

在 `const PORT = ...` 之后、`const app = new Hono()` 之前添加 Redis 检查调用：

```typescript
await checkRedisConnection()
console.log('Redis connection established')
```

> 注意：`src/index.ts` 当前为同步文件。由于需要 `await checkRedisConnection()`，需将启动逻辑包裹在 async IIFE 中。

完整修改后的 `src/index.ts` 应如下：

```typescript
import { Hono } from 'hono'
import { serve } from '@hono/node-server'
import { cors } from 'hono/cors'
import chatRoutes from './routes/chat.js'
import sessionRoutes from './routes/sessions.js'
import knowledgeBaseRoutes from './routes/knowledgeBases.js'
import settingsRoutes from './routes/settings.js'
import { checkRedisConnection } from './queue/redis.js'
import './queue/workers.js'

const PORT = process.env.PORT ? parseInt(process.env.PORT, 10) : 3000

async function main() {
  try {
    await checkRedisConnection()
    console.log('Redis connection established')
  } catch (err) {
    console.error('Failed to start server:', err instanceof Error ? err.message : String(err))
    process.exit(1)
  }

  const app = new Hono()

  app.onError((err, c) => {
    console.error('Hono error:', err)
    return c.json({ error: 'Internal server error' }, 500)
  })

  app.use('*', cors({
    origin: process.env.CORS_ORIGIN ?? 'http://localhost:5173',
    allowMethods: ['GET', 'POST', 'PUT', 'PATCH', 'DELETE', 'OPTIONS'],
    allowHeaders: ['Content-Type'],
    credentials: true,
  }))

  app.get('/health', (c) => c.json({ status: 'ok' }))

  app.route('/chat', chatRoutes)
  app.route('/sessions', sessionRoutes)
  app.route('/knowledge-bases', knowledgeBaseRoutes)
  app.route('/settings', settingsRoutes)

  serve({
    fetch: app.fetch,
    port: PORT,
    hostname: '0.0.0.0',
  }, (info) => {
    console.log(`Server running on http://0.0.0.0:${info.port}`)
  })
}

main()

process.on('uncaughtException', (err) => {
  console.error('Uncaught exception:', err)
})

process.on('unhandledRejection', (reason) => {
  console.error('Unhandled rejection:', reason)
})
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/index.ts
git commit -m "feat(server): 启动时检查 Redis 连通性并引入 Worker 模块"
```

---

### 任务 7: 创建任务状态查询 API 路由

**文件：**
- 创建：`packages/server/src/routes/jobs.ts`
- 修改：`packages/server/src/index.ts`

**规格引用：**
- API 规格：[2.1 获取任务状态]、[2.2 获取队列统计]
- 行为规格：[5.1 任务状态查询]、[5.2 队列统计查询]

- [ ] **步骤 1: 编写 jobs 路由文件**

```typescript
import { Hono } from 'hono'
import { getJobStatus, getQueueStats } from '../queue/jobs.js'

const app = new Hono()

// GET /api/jobs/:jobId/status
app.get('/:jobId/status', async (c) => {
  const jobId = c.req.param('jobId')

  try {
    const status = await getJobStatus(jobId)
    if (!status) {
      return c.json({ error: 'Job not found' }, 404)
    }
    return c.json(status, 200)
  } catch (err) {
    console.error('getJobStatus error:', err)
    return c.json({ error: 'Internal server error' }, 500)
  }
})

// GET /api/jobs/stats
app.get('/stats', async (c) => {
  try {
    const stats = await getQueueStats()
    return c.json({ queues: stats }, 200)
  } catch (err) {
    console.error('getQueueStats error:', err)
    return c.json({ error: 'Internal server error' }, 500)
  }
})

export default app
```

- [ ] **步骤 2: 在 index.ts 中注册路由**

在 `packages/server/src/index.ts` 中，在 `app.route('/settings', settingsRoutes)` 之后添加：

```typescript
import jobRoutes from './routes/jobs.js'
```

并在 `app.route('/settings', settingsRoutes)` 之后添加：

```typescript
app.route('/jobs', jobRoutes)
```

> 注意：import 语句需放在文件顶部，与其他 route import 放在一起。

- [ ] **步骤 3: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 4: 提交**

```bash
git add packages/server/src/routes/jobs.ts packages/server/src/index.ts
git commit -m "feat(api): 添加任务状态与队列统计查询路由 (/jobs/:jobId/status, /jobs/stats)"
```

---

### 任务 8: 更新环境变量配置与 `.env.example`

**文件：**
- 修改：`.env.example`

**规格引用：**
- 功能规格：[8. 环境变量 — REDIS_HOST、REDIS_PORT、QUEUE_CONCURRENCY]
- Issue 验收标准：配置从环境变量读取（redis host、port）

- [ ] **步骤 1: 在 `.env.example` 中追加 QUEUE_CONCURRENCY**

在 `.env.example` 末尾追加：

```bash
# --- Queue ---
QUEUE_CONCURRENCY=2
```

- [ ] **步骤 2: 验证文件格式**

运行：`cat .env.example | grep -E 'REDIS|QUEUE'`
预期：输出包含 `REDIS_HOST`、`REDIS_PORT`、`QUEUE_CONCURRENCY`。

- [ ] **步骤 3: 提交**

```bash
git add .env.example
git commit -m "chore(env): 添加 QUEUE_CONCURRENCY 环境变量示例"
```

---

### 任务 9: 创建队列与 Worker 集成测试脚本

**文件：**
- 创建：`packages/server/src/queue/test-queue.ts`

**规格引用：**
- 行为规格：[2.1 添加文档处理任务]、[3.1 Worker 消费任务]、[5.1 任务状态查询]
- Issue 验收标准：提供 `addDocumentJob` 方法、提供 `getJobStatus` 方法

- [ ] **步骤 1: 编写测试脚本**

```typescript
import { addDocumentJob, getJobStatus } from './jobs.js'
import { documentQueue, embeddingQueue } from './queues.js'
import { redis } from './redis.js'

async function runTest() {
  console.log('=== BullMQ Queue Test ===')

  // 1. 投递文档解析任务
  const jobId = await addDocumentJob('doc-test-001', 'parse')
  console.log(`Added document job: ${jobId}`)

  // 2. 查询任务状态（应处于 waiting 或 active）
  let status = await getJobStatus(jobId)
  console.log('Initial status:', status)

  // 3. 等待 Worker 处理完成（最多等待 10 秒）
  let attempts = 0
  while (attempts < 20) {
    await new Promise((r) => setTimeout(r, 500))
    status = await getJobStatus(jobId)
    if (status?.state === 'completed') {
      console.log('Final status:', status)
      break
    }
    attempts++
  }

  if (status?.state !== 'completed') {
    console.error('Job did not complete in time')
    process.exitCode = 1
  } else {
    console.log('Job completed successfully')
  }

  // 4. 清理：关闭队列与 Redis 连接
  await documentQueue.close()
  await embeddingQueue.close()
  await redis.quit()
  console.log('=== Test finished ===')
}

runTest().catch((err) => {
  console.error('Test failed:', err)
  process.exit(1)
})
```

- [ ] **步骤 2: 运行测试脚本（需确保 Redis 已启动）**

确保 Redis 运行：
```bash
docker compose -f docker-compose.dev.yml ps redis
```
若未运行：
```bash
docker compose -f docker-compose.dev.yml up -d redis
```

运行测试：
```bash
cd packages/server && npx tsx src/queue/test-queue.ts
```

预期输出：
```
=== BullMQ Queue Test ===
Added document job: <job-id>
Initial status: { id: '<job-id>', state: 'waiting', progress: 0, ... }
Final status: { id: '<job-id>', state: 'completed', progress: 100, ... }
Job completed successfully
=== Test finished ===
```

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/queue/test-queue.ts
git commit -m "test(queue): 添加队列与 Worker 集成测试脚本"
```

---

### 任务 10: 验证 `pnpm type-check` 与 `pnpm dev:server`

**文件：**
- 无文件变更（纯验证）

**规格引用：**
- Issue 验收标准：验证 `pnpm type-check` 和 `pnpm dev:server`

- [ ] **步骤 1: 运行类型检查**

```bash
cd packages/server && pnpm type-check
```
预期：无错误，退出码 0。

- [ ] **步骤 2: 验证开发服务器可启动**

确保 Redis 服务运行：
```bash
docker compose -f docker-compose.dev.yml ps redis
```

启动服务器：
```bash
cd packages/server && pnpm dev
```

预期：终端输出 `Redis connection established`，随后输出 `Server running on http://0.0.0.0:3000`，无报错。

按 `Ctrl+C` 终止进程。

- [ ] **步骤 3: 验证 HTTP API**

在另一个终端中，先启动服务器（保持运行），然后执行：

```bash
# 测试队列统计接口
curl http://localhost:3000/jobs/stats
```

预期响应：
```json
{
  "queues": [
    { "name": "document-processing", "waiting": 0, "active": 0, "completed": 0, "failed": 0, "delayed": 0 },
    { "name": "embedding-generation", "waiting": 0, "active": 0, "completed": 0, "failed": 0, "delayed": 0 }
  ]
}
```

- [ ] **步骤 4: 提交验证结果（可选）**

若所有验证通过，无需额外提交。若修复了类型错误，单独提交修复。

---

## 规格覆盖检查

### 功能规格覆盖

| 功能规格条目 | 对应任务 | 状态 |
|---|---|---|
| Redis 客户端（ioredis）安装、连接配置与实例导出 | 任务 1、2 | 已覆盖 |
| BullMQ Queue 初始化与命名规范（document-processing / embedding-generation） | 任务 3 | 已覆盖 |
| BullMQ Worker 框架与注册机制（占位处理器） | 任务 4 | 已覆盖 |
| 任务数据类型安全（TypeScript 泛型） | 任务 3、4、5 | 已覆盖 |
| 任务投递接口：`addDocumentJob(documentId, type)` | 任务 5 | 已覆盖 |
| 任务状态查询接口：`getJobStatus(jobId)` | 任务 5 | 已覆盖 |
| 失败重试策略（默认 3 次，指数退避） | 任务 3 | 已覆盖 |
| 启动时 Redis 连通性检查，失败时给出明确错误 | 任务 2、6 | 已覆盖 |

### 行为规格覆盖

| 行为规格条目 | 对应任务 | 状态 |
|---|---|---|
| 1.1 服务启动时创建 Queue 和 Worker 实例（ping 检查） | 任务 2、6 | 已覆盖 |
| 2.1 添加文档处理任务 | 任务 5 | 已覆盖 |
| 2.2 任务数据结构（DocumentJobData） | 任务 3 | 已覆盖 |
| 3.1 Worker 消费任务 | 任务 4 | 已覆盖 |
| 3.2 占位处理流程（parse → chunk → embed） | 任务 4 | 已覆盖 |
| 3.3 进度报告（job.updateProgress） | 任务 4 | 已覆盖 |
| 4.1 失败重试（attempts: 3, exponential backoff） | 任务 3 | 已覆盖 |
| 4.2 死信队列（MVP 不配置独立 DLQ） | 任务 3（removeOnFail 保留） | 已覆盖 |
| 4.3 失败状态同步（预留异常捕获结构） | 任务 4 | 已覆盖 |
| 5.1 任务状态查询（getJobStatus） | 任务 5 | 已覆盖 |
| 5.2 队列统计查询（getQueueStats） | 任务 5 | 已覆盖 |
| 6.1 启动时行为 | 任务 2、6 | 已覆盖 |
| 6.2 运行时行为 | 任务 4 | 已覆盖 |
| 6.3 关闭时行为（gracefulShutdown） | 任务 4 | 已覆盖 |
| 7. 环境行为矩阵（dev / test / prod） | 任务 8（环境变量配置） | 已覆盖 |

### API 规格覆盖

| API 规格条目 | 对应任务 | 状态 |
|---|---|---|
| 1.2 Redis 连接实例（redis.ts） | 任务 2 | 已覆盖 |
| 1.3 Queue 定义（queues.ts） | 任务 3 | 已覆盖 |
| 1.4 Worker 注册框架（workers.ts） | 任务 4 | 已覆盖 |
| 1.5 任务投递与状态查询辅助函数（jobs.ts） | 任务 5 | 已覆盖 |
| 2.1 获取任务状态（GET /api/jobs/:jobId/status） | 任务 7 | 已覆盖 |
| 2.2 获取队列统计（GET /api/jobs/stats） | 任务 7 | 已覆盖 |
| 3. 类型汇总（DocumentJobData / EmbeddingJobData / JobStatus / QueueStats） | 任务 3、5 | 已覆盖 |
| 4. 依赖（bullmq、ioredis） | 任务 1 | 已覆盖 |

### 占位符扫描

- [x] 无 "TBD" / "TODO" / "稍后实现" / "填写细节"
- [x] 无 "添加适当的错误处理" 等模糊描述
- [x] 所有任务均含具体代码块与验证命令
- [x] 无 "类似于任务 N" 的重复引用

### 类型一致性

- [x] `DocumentJobData` 与 `EmbeddingJobData` 类型在 `queues.ts` 定义，被 `workers.ts` 和 `jobs.ts` 复用
- [x] `JobStatus` 与 `QueueStats` 接口在 `jobs.ts` 定义，与 API 路由返回结构一致
- [x] 所有 `.ts` 文件使用 `.js` 扩展名进行模块解析，符合 `Node16` 配置
- [x] `redis.ts` 中 `maxRetriesPerRequest: null` 满足 BullMQ 兼容性要求

---

## 执行交接

**计划已保存到 `docs/04-plans/redis-bullmq-setup/2026-05-16.md`。两种执行方式：**

1. **子代理驱动（推荐）** — 每个任务新子代理，任务间审查
2. **内联执行** — 当前会话中顺序执行，带检查点

**选择哪种？**
