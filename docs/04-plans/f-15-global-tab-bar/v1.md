# 全局标签栏 实现计划

> **For agentic workers:** 使用 `subagent-driven-development`。步骤用复选框追踪。

**目标：** TabBar 从 ChatView 提升至 AuthenticatedLayout header，成为全局跨路由持久标签栏。

**架构：** AuthenticatedLayout 插入 38px header → 承载 TabBar → 侧边栏点击 → tabsStore.addTab → RouterView 由 activeTab 驱动。ChatView 移除内部 TabBar。

**技术栈：** Vue 3 + Pinia + Vue Router + TypeScript

**Issue 引用：** docs/02-issues/f-15-global-tab-bar.md
**Spec 引用：** docs/03-specs/f-15-global-tab-bar/
**测试用例：** docs/08-test-cases/f-15-global-tab-bar/
**前置依赖：** f-16-unified-tab-types（tabsStore 已就绪）

---

## 文件变更清单

| 操作 | 文件路径 |
|------|----------|
| 修改 | `packages/webui/src/layouts/AuthenticatedLayout.vue` — 插入 header + TabBar |
| 修改 | `packages/webui/src/views/ChatView.vue` — 移除 TabBar |
| 移动 | `packages/webui/src/components/TabBar.vue` → `components/layout/TabBar.vue` |
| 修改 | `packages/webui/src/components/layout/AppSidebar.vue` — 导航适配 |
| 修改 | `packages/webui/src/stores/tabs.ts` — 完善关闭决策树 + 自动 home 创建 |

---

### 任务 1: 增强 tabsStore 关闭逻辑

**文件：**
- 修改：`packages/webui/src/stores/tabs.ts`

**规格引用：** behavior-spec.md — 标签关闭决策树

- [ ] **步骤 1: 重新实现 closeTab 支持决策树**

当前 closeTab 只检查 `closable`。需增加混合场景逻辑：

```typescript
function closeTab(tabId: string) {
  const idx = tabs.value.findIndex((t) => t.id === tabId)
  if (idx === -1) return
  const tab = tabs.value[idx]

  // home 标签永远不可关闭
  if (tab.id === 'home') return

  // 检查是否可以关闭（混合场景下的最后一个非 chat 标签）
  if (!canClose(tab)) return

  tabs.value.splice(idx, 1)

  // 激活左侧相邻标签
  if (activeTabId.value === tabId) {
    const newIdx = Math.max(0, idx - 1)
    const target = tabs.value[newIdx]
    if (target) {
      activeTabId.value = target.id
    } else {
      // 全部关闭 → 自动创建 home
      createHomeTab()
    }
  }
}

function canClose(tab: Tab): boolean {
  // home 不可关闭
  if (tab.id === 'home') return false

  // 只有 1 个非 home 标签：可关闭（之后自动创建 home）
  const closableTabs = tabs.value.filter(t => t.id !== 'home')
  if (closableTabs.length <= 1) return true

  // 非 chat 标签在混合场景下检查
  if (tab.type !== 'chat') {
    const nonChatTabs = tabs.value.filter(t => t.type !== 'chat' && t.id !== 'home')
    if (nonChatTabs.length === 1) {
      const chatExists = tabs.value.some(t => t.type === 'chat')
      if (chatExists) return false // 最后一个非 chat，有 chat 存在时不可关
    }
  }

  return true
}

function createHomeTab() {
  tabs.value = [{
    id: 'home',
    type: 'chat',
    title: '首页',
    sessionId: undefined,
    closable: false,
  }]
  activeTabId.value = 'home'
}
```

- [ ] **步骤 2: 运行现有测试确认不破坏**

```bash
pnpm test
```
预期：通过（如有 tabs 相关测试）。

---

### 任务 2: 移动 TabBar 到 layout 目录

**文件：**
- 移动：`packages/webui/src/components/TabBar.vue` → `packages/webui/src/components/layout/TabBar.vue`

- [ ] **步骤 1: 移动文件**

```bash
mv packages/webui/src/components/TabBar.vue packages/webui/src/components/layout/TabBar.vue
```

- [ ] **步骤 2: 更新 TabBar.vue 内部 import 路径（如有相对引用）**

TabBar.vue 当前无相对 import，无需修改内部代码。

---

### 任务 3: 改造 AuthenticatedLayout 承载 TabBar

**文件：**
- 修改：`packages/webui/src/layouts/AuthenticatedLayout.vue`

**规格引用：** feature-spec.md — 布局结构（目标）

- [ ] **步骤 1: 重写 AuthenticatedLayout.vue**

```vue
<script setup lang="ts">
import { watch } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useTabsStore } from '@/stores/tabs'
import AppSidebar from '@/components/layout/AppSidebar.vue'
import TabBar from '@/components/layout/TabBar.vue'

const route = useRoute()
const router = useRouter()
const tabsStore = useTabsStore()

// 侧边栏导航 → 标签操作（不直接 push，URL 由 watch 统一同步）
function handleNavigate(routeName: string) {
  const tabTypeMap: Record<string, import('@/types').TabType> = {
    chat: 'chat',
    knowledgeBase: 'knowledgeBase',
    history: 'history',
    settings: 'settings',
    recycleBin: 'recycleBin',
  }
  const type = tabTypeMap[routeName]
  if (type) {
    tabsStore.addTab(type)
  }
}

// 标签切换 → 同步路由（唯一 URL 同步入口）
watch(() => tabsStore.activeTab, (tab) => {
  if (!tab) return
  const routeMap: Record<string, string> = {
    chat: 'chat',
    knowledgeBase: 'knowledgeBase',
    history: 'history',
    settings: 'settings',
    recycleBin: 'recycleBin',
  }
  const targetRoute = routeMap[tab.type] ?? 'chat'
  if (route.name !== targetRoute) {
    router.push({ name: targetRoute })
  }
})

function onTabSwitch(tabId: string) {
  tabsStore.switchTab(tabId)
}

function onTabClose(tabId: string) {
  tabsStore.closeTab(tabId)
}

function onNewChat() {
  // 创建空 chat 标签（无 session），session 在用户发送第一条消息时创建
  tabsStore.addTab('chat')
}

function onTabRename(tabId: string, title: string) {
  tabsStore.renameTab(tabId, title)
}
</script>

<template>
  <div class="flex h-screen bg-surface-1 text-text-primary">
    <AppSidebar
      :active-type="(route.name as string) ?? 'chat'"
      @navigate="handleNavigate"
    />
    <div class="flex flex-1 flex-col overflow-hidden">
      <!-- Header: 全局标签栏 -->
      <header class="shrink-0 border-b border-border-default bg-white" style="height: 38px">
        <TabBar
          :tabs="tabsStore.tabs"
          :active-tab-id="tabsStore.activeTabId"
          @switch="onTabSwitch"
          @close="onTabClose"
          @new-chat="onNewChat"
          @rename="onTabRename"
        />
      </header>
      <!-- 内容区 -->
      <main class="flex-1 overflow-hidden pb-12 md:pb-0">
        <RouterView />
      </main>
    </div>
  </div>
</template>
```

---

### 任务 4: 从 ChatView 移除 TabBar + 修复 session 关联

**文件：**
- 修改：`packages/webui/src/views/ChatView.vue`

- [ ] **步骤 1: 删除 ChatView 中的 TabBar 渲染和事件处理**

```vue
<script setup lang="ts">
// 删除以下 import 和函数（迁移到 AuthenticatedLayout）
// import TabBar from '@/components/TabBar.vue'
// function onTabSwitch / onTabClose / onNewChat / onTabRename

// 保留 tabsStore 用于读取 activeTab 的 sessionId
import { useTabsStore } from '@/stores/tabs'
const tabsStore = useTabsStore()
</script>

<template>
  <div class="flex h-full flex-col bg-surface-1">
    <!-- 移除 <TabBar ... /> -->

    <EmptySession v-if="isEmpty" @send="handleSend" />

    <!-- 其余模板不变 -->
  </div>
</template>
```

- [ ] **步骤 2: 修复 handleSend 的 onNewSession 回调——更新当前激活标签而非写死 home**

```typescript
// 变更前
function handleSend(content: string, knowledgeBaseIds: string[]) {
  sessionStore.sendMessage(content, knowledgeBaseIds, {
    llmConfig,
    onNewSession(sessionId, title) {
      chatTabsStore.updateHomeTabSession(sessionId, title)
    },
  })
}

// 变更后
function handleSend(content: string, knowledgeBaseIds: string[]) {
  sessionStore.sendMessage(content, knowledgeBaseIds, {
    llmConfig,
    onNewSession(sessionId, title) {
      const activeTab = tabsStore.activeTab
      if (activeTab && !activeTab.sessionId) {
        // 更新当前激活标签的 sessionId（支持 home 和新 chat 标签）
        tabsStore.updateActiveTabSession(sessionId, title)
      }
    },
  })
}
```

- [ ] **步骤 3: 清理 ChatView 中已迁移到 Layout 的 tab 事件处理函数**

删除：`onTabSwitch`、`onNewChat`、`onTabClose`、`onTabRename` 函数定义。

---

### 任务 5: 适配 AppSidebar 导航逻辑

**文件：**
- 修改：`packages/webui/src/components/layout/AppSidebar.vue`

- [ ] **步骤 1: 确认侧边栏 emit 不变**

侧边栏保持 `@navigate` emit 路由名，Layout 已改为 `handleNavigate` 先操作 Store 再路由。侧边栏代码无需修改。

- [ ] **步骤 2: 检查 active-type 计算**

```typescript
// AppSidebar 接收 activeType prop，由 route.name 提供
// 在全局标签栏下，需确保 activeType 反映当前激活标签类型而非 URL
```

修改 AuthenticatedLayout 中传递给 AppSidebar 的 active-type：

```vue
<AppSidebar
  :active-type="tabsStore.activeTab?.type ?? 'chat'"
  @navigate="handleNavigate"
/>
```

---

### 任务 6: 验证

- [ ] **步骤 1: TypeScript 类型检查**

```bash
cd packages/webui && npx vue-tsc --noEmit
```
预期：零错误。

- [ ] **步骤 2: 开发服务器启动**

```bash
pnpm dev:web
```

- [ ] **步骤 3: 手动验证清单**

- [ ] 登录后 → TabBar 显示在顶部 header
- [ ] home 标签存在且不可关闭
- [ ] 点击侧边栏"知识库" → 新建知识库标签（单例）
- [ ] 再次点击"知识库" → 激活已有标签
- [ ] 新建多个 chat 标签 → 每个独立出现
- [ ] 混合场景：chat1, chat2, kb 存在时 → kb 不可关闭
- [ ] 全部 chat 标签 → 全部可关闭
- [ ] 关闭最后一个标签 → 自动创建 home
- [ ] 切换标签 → 内容区正确切换
- [ ] 刷新页面 → 标签状态丢失（预期行为）

- [ ] **步骤 4: 提交**

```bash
git add packages/webui/src/layouts/AuthenticatedLayout.vue \
        packages/webui/src/views/ChatView.vue \
        packages/webui/src/components/layout/TabBar.vue \
        packages/webui/src/components/TabBar.vue \
        packages/webui/src/components/layout/AppSidebar.vue \
        packages/webui/src/stores/tabs.ts
git commit -m "feat(f-15): lift TabBar to AuthenticatedLayout as global tab navigation

- Move TabBar from ChatView to AuthenticatedLayout header (38px)
- Add close decision tree: last non-chat tab unclosable when mixed
- Auto-create home tab when all tabs closed
- Sidebar nav drives tab activation instead of direct router.push
- Remove TabBar from ChatView"
```
