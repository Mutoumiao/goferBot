# NestJS Auth System 执行计划

> 对应 issue: `i-09-nestjs-auth-system`
> 日期: 2026-05-16
> 依赖: `i-08-nestjs-server-setup`, `i-02-prisma-setup`, `i-10-nestjs-security`

---

## 前置条件（已假设完成）

- `packages/server/src/main.ts` 已存在，NestJS 应用已启动
- `packages/server/src/prisma/prisma.service.ts` 已提供 `PrismaService`
- `packages/server/.env` 已包含 `DATABASE_URL`
- `i-10-nestjs-security` 已提供全局 `ZodValidationPipe`、`ResponseInterceptor`、`AllExceptionsFilter`
- Prisma schema 已包含 `User` 和 `RefreshToken` 模型（见 feature-spec 4.3.1）

---

## Step 1 — 安装依赖（2 分钟）

在 `packages/server/` 目录下执行：

```bash
cd packages/server
pnpm add @nestjs/passport passport passport-jwt @nestjs/jwt bcrypt
pnpm add -D @types/passport-jwt @types/bcrypt
```

验证 `package.json` 已新增：

- `@nestjs/passport`
- `@nestjs/jwt`
- `passport`
- `passport-jwt`
- `bcrypt`

---

## Step 2 — 添加 JWT 环境变量（2 分钟）

编辑 `packages/server/.env.example`，追加：

```env
JWT_SECRET=goferbot-jwt-secret-change-me-in-production
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
```

编辑 `packages/server/.env`（本地开发环境），追加同样的三行。

---

## Step 3 — 创建 User 模块骨架（3 分钟）

### 3.1 创建目录

```bash
mkdir -p packages/server/src/modules/user
mkdir -p packages/server/src/modules/auth/dto
mkdir -p packages/server/src/modules/auth/guards
mkdir -p packages/server/src/modules/auth/strategies
mkdir -p packages/server/src/common/decorators
```

### 3.2 `packages/server/src/modules/user/user.service.ts`

```typescript
import { Injectable } from '@nestjs/common'
import { PrismaService } from '../../prisma/prisma.service.js'

@Injectable()
export class UserService {
  constructor(private readonly prisma: PrismaService) {}

  async findByEmail(email: string) {
    return this.prisma.user.findUnique({ where: { email } })
  }

  async findById(id: string) {
    return this.prisma.user.findUnique({
      where: { id },
      select: {
        id: true,
        email: true,
        name: true,
        avatar: true,
        createdAt: true,
      },
    })
  }

  async create(data: { email: string; password: string; name?: string }) {
    return this.prisma.user.create({
      data,
      select: {
        id: true,
        email: true,
        name: true,
        avatar: true,
        createdAt: true,
      },
    })
  }
}
```

### 3.3 `packages/server/src/modules/user/user.module.ts`

```typescript
import { Module } from '@nestjs/common'
import { UserService } from './user.service.js'

@Module({
  providers: [UserService],
  exports: [UserService],
})
export class UserModule {}
```

---

## Step 4 — 创建 Zod DTOs（3 分钟）

### 4.1 `packages/server/src/modules/auth/dto/register.dto.ts`

```typescript
import { createZodDto } from 'nestjs-zod'
import { z } from 'zod'

export const RegisterSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8, 'Password must be at least 8 characters'),
  name: z.string().min(1).max(50).optional(),
})

export class RegisterDto extends createZodDto(RegisterSchema) {}
```

### 4.2 `packages/server/src/modules/auth/dto/login.dto.ts`

```typescript
import { createZodDto } from 'nestjs-zod'
import { z } from 'zod'

export const LoginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(1),
})

export class LoginDto extends createZodDto(LoginSchema) {}
```

### 4.3 `packages/server/src/modules/auth/dto/refresh.dto.ts`

```typescript
import { createZodDto } from 'nestjs-zod'
import { z } from 'zod'

export const RefreshSchema = z.object({
  refreshToken: z.string().min(1),
})

export class RefreshDto extends createZodDto(RefreshSchema) {}
```

### 4.4 `packages/server/src/modules/auth/dto/logout.dto.ts`

```typescript
import { createZodDto } from 'nestjs-zod'
import { z } from 'zod'

export const LogoutSchema = z.object({
  refreshToken: z.string().min(1),
})

export class LogoutDto extends createZodDto(LogoutSchema) {}
```

---

## Step 5 — 创建 JWT 策略与守卫（3 分钟）

### 5.1 `packages/server/src/modules/auth/strategies/jwt.strategy.ts`

```typescript
import { Injectable } from '@nestjs/common'
import { ConfigService } from '@nestjs/config'
import { PassportStrategy } from '@nestjs/passport'
import { ExtractJwt, Strategy } from 'passport-jwt'

export interface UserPayload {
  sub: string
  email: string
  iat: number
  exp: number
}

@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(configService: ConfigService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: configService.getOrThrow<string>('JWT_SECRET'),
    })
  }

  async validate(payload: UserPayload): Promise<UserPayload> {
    return payload
  }
}
```

### 5.2 `packages/server/src/modules/auth/guards/jwt.guard.ts`

```typescript
import { Injectable } from '@nestjs/common'
import { AuthGuard } from '@nestjs/passport'

@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {}
```

---

## Step 6 — 创建 @CurrentUser() 装饰器（2 分钟）

`packages/server/src/common/decorators/current-user.decorator.ts`

```typescript
import { createParamDecorator, ExecutionContext } from '@nestjs/common'
import { UserPayload } from '../../modules/auth/strategies/jwt.strategy.js'

export const CurrentUser = createParamDecorator(
  (data: keyof UserPayload | undefined, ctx: ExecutionContext) => {
    const request = ctx.switchToHttp().getRequest()
    const user = request.user as UserPayload
    return data ? user?.[data] : user
  },
)
```

---

## Step 7 — 实现 AuthService（5 分钟）

`packages/server/src/modules/auth/auth.service.ts`

```typescript
import { ConflictException, Injectable, UnauthorizedException } from '@nestjs/common'
import { ConfigService } from '@nestjs/config'
import { JwtService } from '@nestjs/jwt'
import { hash, compare } from 'bcrypt'
import { randomBytes } from 'crypto'
import { PrismaService } from '../../prisma/prisma.service.js'
import { RegisterDto } from './dto/register.dto.js'
import { UserPayload } from './strategies/jwt.strategy.js'

export interface AuthResponse {
  user: {
    id: string
    email: string
    name: string | null
    avatar: string | null
    createdAt: Date
  }
  accessToken: string
  refreshToken: string
}

export interface RefreshResponse {
  accessToken: string
  refreshToken: string
}

@Injectable()
export class AuthService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly jwtService: JwtService,
    private readonly configService: ConfigService,
  ) {}

  private readonly SALT_ROUNDS = 12

  private get accessExpires(): string {
    return this.configService.get<string>('JWT_ACCESS_EXPIRES') ?? '15m'
  }

  private get refreshExpires(): string {
    return this.configService.get<string>('JWT_REFRESH_EXPIRES') ?? '7d'
  }

  private generateRefreshToken(): string {
    return randomBytes(64).toString('hex')
  }

  private async hashRefreshToken(token: string): Promise<string> {
    return hash(token, this.SALT_ROUNDS)
  }

  private async signAccessToken(payload: UserPayload): Promise<string> {
    return this.jwtService.signAsync(payload, {
      expiresIn: this.accessExpires,
    })
  }

  async validateUser(email: string, password: string) {
    const user = await this.prisma.user.findUnique({ where: { email } })
    if (!user) {
      throw new UnauthorizedException('Invalid email or password')
    }
    const valid = await compare(password, user.password)
    if (!valid) {
      throw new UnauthorizedException('Invalid email or password')
    }
    return user
  }

  async register(dto: RegisterDto): Promise<AuthResponse> {
    const existing = await this.prisma.user.findUnique({
      where: { email: dto.email },
    })
    if (existing) {
      throw new ConflictException('Email already registered')
    }

    const passwordHash = await hash(dto.password, this.SALT_ROUNDS)
    const user = await this.prisma.user.create({
      data: {
        email: dto.email,
        password: passwordHash,
        name: dto.name ?? null,
      },
      select: {
        id: true,
        email: true,
        name: true,
        avatar: true,
        createdAt: true,
      },
    })

    const accessToken = await this.signAccessToken({
      sub: user.id,
      email: user.email,
    } as UserPayload)

    const refreshToken = this.generateRefreshToken()
    const tokenHash = await this.hashRefreshToken(refreshToken)

    await this.prisma.refreshToken.create({
      data: {
        tokenHash,
        userId: user.id,
        expiresAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000),
      },
    })

    return { user, accessToken, refreshToken }
  }

  async login(user: { id: string; email: string; name: string | null; avatar: string | null; createdAt: Date }): Promise<AuthResponse> {
    const accessToken = await this.signAccessToken({
      sub: user.id,
      email: user.email,
    } as UserPayload)

    const refreshToken = this.generateRefreshToken()
    const tokenHash = await this.hashRefreshToken(refreshToken)

    await this.prisma.refreshToken.create({
      data: {
        tokenHash,
        userId: user.id,
        expiresAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000),
      },
    })

    return { user, accessToken, refreshToken }
  }

  async refresh(token: string): Promise<RefreshResponse> {
    const records = await this.prisma.refreshToken.findMany({
      where: {
        expiresAt: { gt: new Date() },
      },
    })

    let matchedRecord: (typeof records)[number] | null = null
    for (const record of records) {
      const valid = await compare(token, record.tokenHash)
      if (valid) {
        matchedRecord = record
        break
      }
    }

    if (!matchedRecord) {
      throw new UnauthorizedException('Invalid or expired refresh token')
    }

    await this.prisma.refreshToken.delete({
      where: { id: matchedRecord.id },
    })

    const user = await this.prisma.user.findUnique({
      where: { id: matchedRecord.userId },
      select: {
        id: true,
        email: true,
        name: true,
        avatar: true,
        createdAt: true,
      },
    })

    if (!user) {
      throw new UnauthorizedException('Invalid or expired refresh token')
    }

    const accessToken = await this.signAccessToken({
      sub: user.id,
      email: user.email,
    } as UserPayload)

    const newRefreshToken = this.generateRefreshToken()
    const newTokenHash = await this.hashRefreshToken(newRefreshToken)

    await this.prisma.refreshToken.create({
      data: {
        tokenHash: newTokenHash,
        userId: user.id,
        expiresAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000),
      },
    })

    return { accessToken, refreshToken: newRefreshToken }
  }

  async logout(userId: string, token: string): Promise<void> {
    const records = await this.prisma.refreshToken.findMany({
      where: { userId },
    })

    for (const record of records) {
      const valid = await compare(token, record.tokenHash)
      if (valid) {
        await this.prisma.refreshToken.delete({ where: { id: record.id } })
        return
      }
    }
  }
}
```

---

## Step 8 — 实现 AuthController（3 分钟）

`packages/server/src/modules/auth/auth.controller.ts`

```typescript
import { Body, Controller, Get, Post, UseGuards } from '@nestjs/common'
import { CurrentUser } from '../../common/decorators/current-user.decorator.js'
import { UserService } from '../user/user.service.js'
import { AuthService } from './auth.service.js'
import { JwtAuthGuard } from './guards/jwt.guard.js'
import { LoginDto } from './dto/login.dto.js'
import { LogoutDto } from './dto/logout.dto.js'
import { RefreshDto } from './dto/refresh.dto.js'
import { RegisterDto } from './dto/register.dto.js'
import { UserPayload } from './strategies/jwt.strategy.js'

@Controller('api/auth')
export class AuthController {
  constructor(
    private readonly authService: AuthService,
    private readonly userService: UserService,
  ) {}

  @Post('register')
  async register(@Body() dto: RegisterDto) {
    return this.authService.register(dto)
  }

  @Post('login')
  async login(@Body() dto: LoginDto) {
    const user = await this.authService.validateUser(dto.email, dto.password)
    return this.authService.login(user)
  }

  @Post('refresh')
  async refresh(@Body() dto: RefreshDto) {
    return this.authService.refresh(dto.refreshToken)
  }

  @Post('logout')
  @UseGuards(JwtAuthGuard)
  async logout(
    @CurrentUser('sub') userId: string,
    @Body() dto: LogoutDto,
  ) {
    await this.authService.logout(userId, dto.refreshToken)
    return { success: true }
  }

  @Get('me')
  @UseGuards(JwtAuthGuard)
  async me(@CurrentUser('sub') userId: string) {
    return this.userService.findById(userId)
  }
}
```

---

## Step 9 — 组装 AuthModule（2 分钟）

`packages/server/src/modules/auth/auth.module.ts`

```typescript
import { Module } from '@nestjs/common'
import { ConfigModule, ConfigService } from '@nestjs/config'
import { JwtModule } from '@nestjs/jwt'
import { PassportModule } from '@nestjs/passport'
import { UserModule } from '../user/user.module.js'
import { AuthController } from './auth.controller.js'
import { AuthService } from './auth.service.js'
import { JwtStrategy } from './strategies/jwt.strategy.js'

@Module({
  imports: [
    ConfigModule,
    PassportModule.register({ defaultStrategy: 'jwt' }),
    JwtModule.registerAsync({
      imports: [ConfigModule],
      inject: [ConfigService],
      useFactory: (configService: ConfigService) => ({
        secret: configService.getOrThrow<string>('JWT_SECRET'),
        signOptions: {
          expiresIn: configService.get<string>('JWT_ACCESS_EXPIRES') ?? '15m',
        },
      }),
    }),
    UserModule,
  ],
  controllers: [AuthController],
  providers: [AuthService, JwtStrategy],
  exports: [AuthService],
})
export class AuthModule {}
```

---

## Step 10 — 注册模块到 AppModule（2 分钟）

编辑 `packages/server/src/app.module.ts`（假设已存在），在 `imports` 数组中加入：

```typescript
import { AuthModule } from './modules/auth/auth.module.js'
import { UserModule } from './modules/user/user.module.js'

@Module({
  imports: [
    // ... 已有模块
    UserModule,
    AuthModule,
  ],
})
export class AppModule {}
```

---

## Step 11 — TypeScript 类型检查（2 分钟）

```bash
cd packages/server
pnpm type-check
```

预期输出：`0 errors`。

若出现 `Cannot find module` 错误，检查 `tsconfig.json` 的 `paths` 映射或确保文件后缀 `.js` 与 `type: "module"` 兼容。

---

## Step 12 — 启动服务并 curl 验证（5 分钟）

### 12.1 启动服务

```bash
cd packages/server
pnpm dev
```

等待控制台输出 `Application is running on: http://localhost:3000`。

### 12.2 注册

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"securePass123","name":"Alice"}' \
  -w "\nHTTP %{http_code}\n"
```

预期：HTTP 201，响应体包含 `data.user.email`、`data.accessToken`、`data.refreshToken`。

### 12.3 登录

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"securePass123"}' \
  -w "\nHTTP %{http_code}\n"
```

预期：HTTP 200，响应体结构与注册相同。

### 12.4 获取当前用户

将 `<accessToken>` 替换为登录返回的 token：

```bash
curl -X GET http://localhost:3000/api/auth/me \
  -H "Authorization: Bearer <accessToken>" \
  -w "\nHTTP %{http_code}\n"
```

预期：HTTP 200，响应体 `data.id`、`data.email`、`data.name` 正确。

### 12.5 刷新 Token

将 `<refreshToken>` 替换为登录返回的 refresh token：

```bash
curl -X POST http://localhost:3000/api/auth/refresh \
  -H "Content-Type: application/json" \
  -d '{"refreshToken":"<refreshToken>"}' \
  -w "\nHTTP %{http_code}\n"
```

预期：HTTP 200，响应体包含新的 `data.accessToken` 和 `data.refreshToken`。

### 12.6 登出

```bash
curl -X POST http://localhost:3000/api/auth/logout \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <accessToken>" \
  -d '{"refreshToken":"<newRefreshToken>"}' \
  -w "\nHTTP %{http_code}\n"
```

预期：HTTP 200，响应体 `data.success` 为 `true`。

### 12.7 验证登出后 Refresh Token 失效

再次用旧的 refresh token 调用刷新接口：

```bash
curl -X POST http://localhost:3000/api/auth/refresh \
  -H "Content-Type: application/json" \
  -d '{"refreshToken":"<newRefreshToken>"}' \
  -w "\nHTTP %{http_code}\n"
```

预期：HTTP 401，响应体包含 `error.code: AUTH_ERROR`。

---

## Step 13 — 错误场景验证（3 分钟）

### 13.1 注册重复邮箱

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"securePass123","name":"Alice"}' \
  -w "\nHTTP %{http_code}\n"
```

预期：HTTP 409，`error.code: CONFLICT`。

### 13.2 密码错误

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"wrongPassword"}' \
  -w "\nHTTP %{http_code}\n"
```

预期：HTTP 401，`error.code: AUTH_ERROR`，消息不区分邮箱或密码错误。

### 13.3 无效 Access Token

```bash
curl -X GET http://localhost:3000/api/auth/me \
  -H "Authorization: Bearer invalid.token.here" \
  -w "\nHTTP %{http_code}\n"
```

预期：HTTP 401，`error.code: AUTH_ERROR`。

---

## Step 14 — 提交代码（2 分钟）

```bash
git add packages/server/src/modules/auth/
git add packages/server/src/modules/user/
git add packages/server/src/common/decorators/
git add packages/server/.env.example
git add packages/server/src/app.module.ts

git commit -m "feat(auth): NestJS JWT 认证系统（注册/登录/刷新/登出/me）

- 实现 AuthModule / AuthController / AuthService
- 实现 JwtAuthGuard / JwtStrategy / @CurrentUser() 装饰器
- 实现 UserModule / UserService
- Zod DTO 校验（login / register / refresh / logout）
- bcrypt 密码哈希（cost factor 12）+ refresh token 哈希存储
- refresh token 轮换机制
- curl 测试全部通过

对应 issue: i-09-nestjs-auth-system"
```

---

## 文件清单

| 文件路径 | 说明 |
|----------|------|
| `packages/server/src/modules/auth/auth.module.ts` | AuthModule，组装 JwtModule、PassportModule、UserModule |
| `packages/server/src/modules/auth/auth.controller.ts` | AuthController，5 个端点 |
| `packages/server/src/modules/auth/auth.service.ts` | AuthService，注册/登录/刷新/登出/密码哈希 |
| `packages/server/src/modules/auth/dto/login.dto.ts` | LoginDto（Zod schema） |
| `packages/server/src/modules/auth/dto/register.dto.ts` | RegisterDto（Zod schema） |
| `packages/server/src/modules/auth/dto/refresh.dto.ts` | RefreshDto（Zod schema） |
| `packages/server/src/modules/auth/dto/logout.dto.ts` | LogoutDto（Zod schema） |
| `packages/server/src/modules/auth/guards/jwt.guard.ts` | JwtAuthGuard（@Injectable()） |
| `packages/server/src/modules/auth/strategies/jwt.strategy.ts` | JwtStrategy（PassportStrategy） |
| `packages/server/src/modules/user/user.module.ts` | UserModule |
| `packages/server/src/modules/user/user.service.ts` | UserService（findByEmail / findById / create） |
| `packages/server/src/common/decorators/current-user.decorator.ts` | @CurrentUser() 装饰器 |
| `packages/server/.env.example` | 追加 JWT_SECRET、JWT_ACCESS_EXPIRES、JWT_REFRESH_EXPIRES |
| `packages/server/src/app.module.ts` | 注册 UserModule、AuthModule |

---

## 回退方案

- 若 `pnpm type-check` 报 `Cannot find module '@nestjs/jwt'`，确认依赖已安装，删除 `node_modules/.pnpm` 缓存后重新 `pnpm install`。
- 若 Prisma schema 未包含 `RefreshToken` 模型，先执行 `npx prisma migrate dev --name add_refresh_token`。
- 若 `passport-jwt` 类型报错，确认 `@types/passport-jwt` 已安装到 `devDependencies`。
