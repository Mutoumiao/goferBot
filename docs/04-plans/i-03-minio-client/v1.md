---
issue_id: i-03-minio-client
type: plan
version: 1
status: done
task_count: 24
summary: 安装 minio 依赖，实现 IStorageProvider 接口的 MinIOStorageProvider，提供类型安全的文件上传、下载、删除、URL 获取能力。启动时自动检查并创建 bucket，配置通过环境变量读取。
---

# MinIO Client 封装与 IStorageProvider 实现计划

> **For agentic workers:** 必需子技能：`superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`。步骤使用复选框（`- [ ]`）语法追踪。

**目标：** 为 `@goferbot/server` 安装 `minio` 依赖，实现 `IStorageProvider` 接口的 `MinIOStorageProvider`，提供类型安全的文件上传、下载、删除、URL 获取能力；启动时自动检查并创建 bucket；配置通过环境变量读取。

**架构：** `packages/server/src/storage/minio.ts` 实现 `IMinIOStorageProvider`（扩展 `IStorageProvider`）；`packages/server/src/config/storage.ts` 读取环境变量；`packages/server/src/storage/test-connection.ts` 提供本地验证脚本。

**技术栈：** `minio` npm 包、TypeScript、Node16 module resolution

**Issue 引用：** [docs/02-issues/i-03-minio-client.md](../../../02-issues/i-03-minio-client.md)
**Spec 引用：** [docs/03-specs/i-03-minio-client/](../../../03-specs/i-03-minio-client/)
**测试用例：** [docs/08-test-cases/i-03/](../../../08-test-cases/i-03/)（待补充）

---

## 文件结构

```
packages/server/
├── package.json                          # 新增 minio 依赖
├── .env.example                          # 新增 MinIO 环境变量示例
├── src/
│   ├── config/
│   │   └── storage.ts                    # MinIO 配置读取（MinIOConfig / getStorageConfig）
│   ├── storage/
│   │   ├── minio.ts                      # MinIOStorageProvider 实现
│   │   └── test-connection.ts            # 本地连接与操作验证脚本
│   └── interfaces/
│       ├── IStorageProvider.ts           # 已有（i-00 定义）
│       └── errors.ts                     # 已有（StorageError / NotFoundError / ValidationError）
```

---

## 任务列表

### 任务 1: 安装 `minio` 依赖

**文件：**
- 修改：`packages/server/package.json`
- 修改：`pnpm-lock.yaml`（由 pnpm 自动生成）

**规格引用：**
- 功能规格：[3.1 范围内 — MinIO Client 初始化与连接管理]
- API 规格：[6. 环境变量清单]

- [ ] **步骤 1: 在 `packages/server` 目录下安装 `minio`**

```bash
cd packages/server && pnpm add minio
```

- [ ] **步骤 2: 验证安装结果**

运行：
```bash
cat packages/server/package.json | grep -A 15 '"dependencies"'
```
预期：`minio` 出现在 `dependencies` 中；无类型定义包冲突（`minio` 自带 `.d.ts`）。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/package.json pnpm-lock.yaml
pnpm install --frozen-lockfile
git add pnpm-lock.yaml
git commit -m "deps(server): 安装 minio 依赖"
```

---

### 任务 2: 创建 `packages/server/src/config/storage.ts`（配置读取）

**文件：**
- 创建：`packages/server/src/config/storage.ts`

**规格引用：**
- 功能规格：[6.2 Bucket 名称]、[6.3 Metadata 策略]
- API 规格：[3.1 MinIOConfig]、[6. 环境变量清单]
- 行为规格：[1.1 客户端创建 — 环境变量读取与默认值]

- [ ] **步骤 1: 编写配置读取代码**

```typescript
import { StorageError } from '../interfaces/errors.js'

export interface MinIOConfig {
  /** MinIO 服务端点，如 http://localhost:9000 */
  endpoint: string
  /** Access Key */
  accessKey: string
  /** Secret Key */
  secretKey: string
  /** Bucket 名称，默认 goferbot-files */
  bucket: string
  /** Region，默认 us-east-1 */
  region: string
  /** 是否使用 SSL（根据 endpoint 协议自动推断） */
  useSSL: boolean
}

export function getStorageConfig(): MinIOConfig {
  const endpoint = process.env.MINIO_ENDPOINT ?? 'http://localhost:9000'
  const accessKey = process.env.MINIO_ACCESS_KEY
  const secretKey = process.env.MINIO_SECRET_KEY
  const bucket = process.env.MINIO_BUCKET ?? 'goferbot-files'
  const region = process.env.MINIO_REGION ?? 'us-east-1'

  if (!accessKey) {
    throw new StorageError('环境变量 MINIO_ACCESS_KEY 未设置')
  }
  if (!secretKey) {
    throw new StorageError('环境变量 MINIO_SECRET_KEY 未设置')
  }

  const useSSL = endpoint.startsWith('https://')
  const cleanEndpoint = endpoint.replace(/^https?:\/\//, '')

  return {
    endpoint: cleanEndpoint,
    accessKey,
    secretKey,
    bucket,
    region,
    useSSL,
  }
}
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/config/storage.ts
git commit -m "feat(storage): 创建 MinIO 配置读取模块"
```

---

### 任务 3: 创建 `packages/server/src/storage/minio.ts`（核心实现）

**文件：**
- 创建：`packages/server/src/storage/minio.ts`

**规格引用：**
- 功能规格：[3.1 范围内 — 全部存储操作]、[6.1 Storage Key 格式]、[6.3 Metadata 策略]、[6.4 返回的 URL 类型]
- API 规格：[1.1 IStorageProvider]、[1.2 IMinIOStorageProvider]、[2.1 StorageMeta]、[2.2 StorageUrlOptions]、[4.1 实例化与注入]
- 行为规格：[1.1 客户端创建]、[1.2 Bucket 检查与创建]、[2. 上传流程]、[3. 下载流程]、[4. 删除流程]、[5. URL 获取流程]、[6. 错误处理行为]

- [ ] **步骤 1: 编写 MinIOStorageProvider 实现**

```typescript
import { Client as MinioClient } from 'minio'
import type {
  IStorageProvider,
  StorageMeta,
  StorageUrlOptions,
} from '../interfaces/IStorageProvider.js'
import { StorageError, NotFoundError, ValidationError } from '../interfaces/errors.js'
import type { MinIOConfig } from '../config/storage.js'

export interface IMinIOStorageProvider extends IStorageProvider {
  initialize(): Promise<void>
  getPresignedUploadUrl(key: string, expiry?: number): Promise<string>
}

export class MinIOStorageProvider implements IMinIOStorageProvider {
  private client: MinioClient
  private bucket: string
  private endpoint: string

  constructor(config: MinIOConfig) {
    this.client = new MinioClient({
      endPoint: config.endpoint,
      port: config.useSSL ? undefined : this.extractPort(config.endpoint),
      useSSL: config.useSSL,
      accessKey: config.accessKey,
      secretKey: config.secretKey,
      region: config.region,
    })
    this.bucket = config.bucket
    this.endpoint = `${config.useSSL ? 'https' : 'http'}://${config.endpoint}`
  }

  private extractPort(endpoint: string): number | undefined {
    const parts = endpoint.split(':')
    if (parts.length === 2) {
      const port = parseInt(parts[1], 10)
      return Number.isNaN(port) ? undefined : port
    }
    return undefined
  }

  async initialize(): Promise<void> {
    try {
      const exists = await this.client.bucketExists(this.bucket)
      if (!exists) {
        await this.client.makeBucket(this.bucket)
        console.info(`[storage] bucket '${this.bucket}' 创建成功`)
      }
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err)
      if (message.includes('AccessDenied') || message.includes('InvalidAccessKeyId')) {
        throw new StorageError(
          `MinIO 访问被拒绝，请检查 AccessKey / SecretKey（endpoint: ${this.endpoint}）`,
          err
        )
      }
      if (message.includes('ECONNREFUSED') || message.includes('ETIMEDOUT')) {
        throw new StorageError(
          `无法连接到 MinIO（endpoint: ${this.endpoint}），请检查服务是否运行`,
          err
        )
      }
      throw new StorageError(`初始化 MinIO 失败：${message}`, err)
    }
  }

  private validateKey(key: string): void {
    if (!key || !key.startsWith('users/') || !key.includes('/kb/')) {
      throw new ValidationError('storage key 不能为空或格式错误，期望格式：users/<user-id>/kb/<kb-id>/<doc-id>_<filename>')
    }
  }

  private extractOriginalName(key: string): string {
    const lastSlash = key.lastIndexOf('/')
    return lastSlash >= 0 ? key.slice(lastSlash + 1) : key
  }

  async upload(
    key: string,
    stream: ReadableStream<Uint8Array> | NodeJS.ReadableStream,
    contentType: string
  ): Promise<StorageMeta> {
    this.validateKey(key)
    if (!stream) {
      throw new ValidationError('上传流不能为空')
    }

    const meta: Record<string, string> = {
      'X-Amz-Meta-Original-Name': this.extractOriginalName(key),
      'X-Amz-Meta-Content-Type': contentType,
      'X-Amz-Meta-Upload-Time': new Date().toISOString(),
    }

    try {
      // MinIO Node.js SDK 接受 NodeJS.ReadableStream；若传入 Web ReadableStream，需转换
      const nodeStream = this.toNodeStream(stream)
      const result = await this.client.putObject(this.bucket, key, nodeStream, undefined, meta)
      const etag = typeof result === 'string' ? result : result.etag
      const size = typeof result === 'string' ? 0 : (result.size ?? 0)

      console.info(`[storage] 上传成功 key=${key} size=${size}`)

      return {
        key,
        size,
        etag,
        contentType,
        lastModified: new Date(),
      }
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err)
      if (message.includes('NoSuchBucket')) {
        throw new StorageError(`上传失败：bucket '${this.bucket}' 不存在`, err)
      }
      if (message.includes('ECONNREFUSED') || message.includes('ETIMEDOUT')) {
        throw new StorageError(`上传失败：无法连接到 MinIO (${this.endpoint})`, err)
      }
      if (message.includes('AccessDenied')) {
        throw new StorageError('上传失败：访问被拒绝，请检查 MinIO 凭据', err)
      }
      throw new StorageError(`上传失败：${message}`, err)
    }
  }

  private toNodeStream(
    stream: ReadableStream<Uint8Array> | NodeJS.ReadableStream
  ): NodeJS.ReadableStream {
    if (typeof (stream as ReadableStream<Uint8Array>).getReader === 'function') {
      const webStream = stream as ReadableStream<Uint8Array>
      const reader = webStream.getReader()
      const { Readable } = await import('stream')
      return new Readable({
        async read() {
          try {
            const { done, value } = await reader.read()
            if (done) {
              this.push(null)
            } else {
              this.push(Buffer.from(value))
            }
          } catch (err) {
            this.destroy(err instanceof Error ? err : new Error(String(err)))
          }
        },
      })
    }
    return stream as NodeJS.ReadableStream
  }

  async download(key: string): Promise<ReadableStream<Uint8Array>> {
    this.validateKey(key)

    try {
      const nodeStream = await this.client.getObject(this.bucket, key)

      return new ReadableStream<Uint8Array>({
        start(controller) {
          nodeStream.on('data', (chunk: Buffer) => {
            controller.enqueue(new Uint8Array(chunk))
          })
          nodeStream.on('end', () => controller.close())
          nodeStream.on('error', (err: Error) => controller.error(err))
        },
        cancel() {
          nodeStream.destroy()
        },
      })
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err)
      if (message.includes('NoSuchKey')) {
        throw new NotFoundError('文件', key)
      }
      if (message.includes('NoSuchBucket')) {
        throw new StorageError(`下载失败：bucket '${this.bucket}' 不存在`, err)
      }
      if (message.includes('ECONNREFUSED') || message.includes('ETIMEDOUT')) {
        throw new StorageError('下载失败：无法连接到 MinIO', err)
      }
      throw new StorageError(`下载失败：${message}`, err)
    }
  }

  async delete(key: string): Promise<void> {
    this.validateKey(key)

    try {
      await this.client.removeObject(this.bucket, key)
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err)
      if (message.includes('ECONNREFUSED') || message.includes('ETIMEDOUT')) {
        throw new StorageError('删除失败：无法连接到 MinIO', err)
      }
      if (message.includes('AccessDenied')) {
        throw new StorageError('删除失败：访问被拒绝', err)
      }
      // removeObject 对不存在的 key 通常静默成功；若出现其他错误，继续包装
      if (!message.includes('NoSuchKey')) {
        throw new StorageError(`删除失败：${message}`, err)
      }
    }
  }

  async getUrl(key: string, _options?: StorageUrlOptions): Promise<string> {
    this.validateKey(key)

    try {
      await this.client.statObject(this.bucket, key)
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err)
      if (message.includes('NoSuchKey')) {
        throw new NotFoundError('文件', key)
      }
      if (message.includes('ECONNREFUSED') || message.includes('ETIMEDOUT')) {
        throw new StorageError('获取 URL 失败：无法连接到 MinIO', err)
      }
      throw new StorageError(`获取 URL 失败：${message}`, err)
    }

    return `${this.endpoint}/${this.bucket}/${encodeURIComponent(key)}`
  }

  async getPresignedUploadUrl(key: string, _expiry = 3600): Promise<string> {
    this.validateKey(key)
    throw new StorageError('预签名 URL 尚未实现（Phase 6）')
  }
}
```

- [ ] **步骤 2: 修复 `toNodeStream` 的异步问题（将 `upload` 改为 async 已支持）**

上述代码中 `toNodeStream` 内部使用了 `await import('stream')`，由于调用方 `upload` 已经是 `async`，可直接使用。若类型检查提示 `await` 在构造函数或非 async 方法中非法，确认 `upload` 签名含 `async` 即可。

- [ ] **步骤 3: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 4: 提交**

```bash
git add packages/server/src/storage/minio.ts
git commit -m "feat(storage): 实现 MinIOStorageProvider（upload/download/delete/getUrl）"
```

---

### 任务 4: 创建 `.env.example`（环境变量示例）

**文件：**
- 创建：`packages/server/.env.example`

**规格引用：**
- 功能规格：[6. 环境变量清单]
- API 规格：[6. 环境变量清单]
- 行为规格：[1.1 客户端创建]

- [ ] **步骤 1: 编写 `.env.example`**

```bash
# PostgreSQL
DATABASE_URL=postgresql://gofer:gofer_dev_pass@localhost:5432/goferbot

# MinIO
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=goferbot-files
MINIO_REGION=us-east-1
```

- [ ] **步骤 2: 提交**

```bash
git add packages/server/.env.example
git commit -m "chore(env): 添加 .env.example，包含 MinIO 配置项"
```

---

### 任务 5: 创建 `packages/server/src/storage/test-connection.ts`（本地验证脚本）

**文件：**
- 创建：`packages/server/src/storage/test-connection.ts`

**规格引用：**
- 功能规格：[8. 验收标准 — 所有方法具备类型安全签名]
- 行为规格：[7.1 启动时序]、[2.1 正常上传]、[3.1 正常下载]、[4.1 正常删除]、[5.1 内网直连 URL]

- [ ] **步骤 1: 编写验证脚本**

```typescript
import { MinIOStorageProvider } from './minio.js'
import { getStorageConfig } from '../config/storage.js'

async function main() {
  const config = getStorageConfig()
  const storage = new MinIOStorageProvider(config)

  console.log('[test] 初始化 MinIOStorageProvider...')
  await storage.initialize()
  console.log('[test] bucket 检查/创建完成')

  const testKey = 'users/00000000-0000-0000-0000-000000000000/kb/00000000-0000-0000-0000-000000000001/doc-test_hello.txt'
  const testContent = 'Hello from GoferBot MinIO test!'
  const testBuffer = Buffer.from(testContent, 'utf-8')

  // 上传：将 Buffer 包装为 ReadableStream
  console.log('[test] 上传测试文件...')
  const webStream = new ReadableStream<Uint8Array>({
    start(controller) {
      controller.enqueue(new Uint8Array(testBuffer))
      controller.close()
    },
  })
  const meta = await storage.upload(testKey, webStream, 'text/plain')
  console.log('[test] 上传成功:', meta)

  // 获取 URL
  console.log('[test] 获取文件 URL...')
  const url = await storage.getUrl(testKey)
  console.log('[test] URL:', url)

  // 下载
  console.log('[test] 下载测试文件...')
  const downloadStream = await storage.download(testKey)
  const reader = downloadStream.getReader()
  const chunks: Uint8Array[] = []
  while (true) {
    const { done, value } = await reader.read()
    if (done) break
    chunks.push(value)
  }
  const downloaded = Buffer.concat(chunks.map(c => Buffer.from(c))).toString('utf-8')
  console.log('[test] 下载内容:', downloaded)

  if (downloaded !== testContent) {
    throw new Error('下载内容不匹配')
  }

  // 删除
  console.log('[test] 删除测试文件...')
  await storage.delete(testKey)
  console.log('[test] 删除成功')

  // 幂等删除验证（再次删除不应抛错）
  console.log('[test] 再次删除同一 key（验证幂等）...')
  await storage.delete(testKey)
  console.log('[test] 幂等删除通过')

  console.log('[test] 全部验证通过')
}

main().catch(err => {
  console.error('[test] 验证失败:', err)
  process.exit(1)
})
```

- [ ] **步骤 2: 运行验证脚本（需 MinIO 服务运行）**

确保 MinIO 已启动：
```bash
docker compose -f docker-compose.dev.yml ps minio
```
若未运行：
```bash
docker compose -f docker-compose.dev.yml up -d minio
```

运行脚本：
```bash
cd packages/server && npx tsx src/storage/test-connection.ts
```

预期输出：
```
[test] 初始化 MinIOStorageProvider...
[test] bucket 检查/创建完成
[test] 上传测试文件...
[storage] 上传成功 key=... size=...
[test] 上传成功: { key: ..., size: ..., etag: ..., contentType: 'text/plain', lastModified: ... }
[test] 获取文件 URL...
[test] URL: http://localhost:9000/goferbot-files/...
[test] 下载测试文件...
[test] 下载内容: Hello from GoferBot MinIO test!
[test] 删除测试文件...
[test] 删除成功
[test] 再次删除同一 key（验证幂等）...
[test] 幂等删除通过
[test] 全部验证通过
```

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/storage/test-connection.ts
git commit -m "test(storage): 添加 MinIO 连接与操作验证脚本"
```

---

### 任务 6: 验证 `pnpm type-check` 与 `pnpm dev:server`

**文件：**
- 无文件变更（纯验证）

**规格引用：**
- Issue 验收标准：`pnpm type-check` 通过；`pnpm dev:server` 可正常启动

- [ ] **步骤 1: 类型检查**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 2: 启动 server 开发模式（验证无运行时错误）**

运行：`cd packages/server && pnpm dev`
预期：进程启动，监听端口（默认 3000），无 `StorageError` 或 `MinIO` 连接异常抛出。

若当前 `src/index.ts` 尚未引入 `MinIOStorageProvider`，可临时在入口添加以下代码进行验证（验证后移除或保留注释）：

```typescript
// import { getStorageConfig } from './config/storage.js'
// import { MinIOStorageProvider } from './storage/minio.js'
//
// const storageConfig = getStorageConfig()
// const storage = new MinIOStorageProvider(storageConfig)
// await storage.initialize()
```

- [ ] **步骤 3: 记录验证结果**

在 `PROGRESS.md` 或本计划末尾备注：类型检查通过 / server 启动无异常。

---

## 规格覆盖检查

### 功能规格覆盖

| 功能规格条目 | 对应任务 | 状态 |
|---|---|---|
| MinIO Client 初始化与连接管理 | 任务 1、3 | 已覆盖 |
| Bucket 自动检查与创建（启动时） | 任务 3（`initialize`） | 已覆盖 |
| 文件上传：`Buffer` / `NodeJS.ReadableStream` → MinIO | 任务 3（`upload`） | 已覆盖 |
| 文件下载：返回 `ReadableStream<Uint8Array>` | 任务 3（`download`） | 已覆盖 |
| 文件删除：根据 `storageKey` 删除对象 | 任务 3（`delete`） | 已覆盖 |
| 获取访问 URL：内网直连 URL | 任务 3（`getUrl`） | 已覆盖 |
| 自定义 metadata 支持 | 任务 3（`X-Amz-Meta-*`） | 已覆盖 |
| `IStorageProvider` 接口的完整实现 | 任务 3 | 已覆盖 |
| Storage Key 格式 `users/<user-id>/kb/<kb-id>/<doc-id>_<filename>` | 任务 3（`validateKey`） | 已覆盖 |
| 配置从环境变量读取 | 任务 2、4 | 已覆盖 |

### 行为规格覆盖

| 行为规格条目 | 对应任务 | 状态 |
|---|---|---|
| 1.1 客户端创建（环境变量、默认值、异常） | 任务 2、3 | 已覆盖 |
| 1.2 Bucket 检查与创建 | 任务 3（`initialize`） | 已覆盖 |
| 2.1 正常上传（metadata、返回 StorageMeta） | 任务 3（`upload`） | 已覆盖 |
| 2.2 覆盖上传（幂等） | 任务 3（`putObject` 天然覆盖） | 已覆盖 |
| 2.3 上传错误场景（ValidationError / StorageError） | 任务 3 | 已覆盖 |
| 3.1 正常下载（Node → Web Stream 转换） | 任务 3（`download`） | 已覆盖 |
| 3.2 下载错误场景（NotFoundError / StorageError） | 任务 3 | 已覆盖 |
| 4.1 正常删除 | 任务 3（`delete`） | 已覆盖 |
| 4.2 幂等删除 | 任务 3（静默成功） | 已覆盖 |
| 4.3 与元数据的级联关系（上层负责） | 任务 3（注释说明） | 已覆盖 |
| 5.1 内网直连 URL（`statObject` 前置检查） | 任务 3（`getUrl`） | 已覆盖 |
| 5.2 预签名上传 URL（预留） | 任务 3（`getPresignedUploadUrl` 抛 StorageError） | 已覆盖 |
| 6.1 错误包装原则（MinIO 异常映射） | 任务 3 | 已覆盖 |
| 6.2 日志行为（info / error） | 任务 3（`console.info` / 异常堆栈） | 已覆盖 |
| 7.1 启动时序 | 任务 3（`initialize`） | 已覆盖 |
| 7.2 关闭时序（无状态，无需显式关闭） | 任务 3（注释说明） | 已覆盖 |

### API 规格覆盖

| API 规格条目 | 对应任务 | 状态 |
|---|---|---|
| 1.1 `IStorageProvider` 接口实现 | 任务 3 | 已覆盖 |
| 1.2 `IMinIOStorageProvider` 扩展接口 | 任务 3 | 已覆盖 |
| 2.1 `StorageMeta` 类型 | 任务 3（返回值） | 已覆盖 |
| 2.2 `StorageUrlOptions` 类型 | 任务 3（`getUrl` 参数） | 已覆盖 |
| 3.1 `MinIOConfig` 类型 | 任务 2 | 已覆盖 |
| 4.1 实例化与注入示例 | 任务 3（代码注释） | 已覆盖 |
| 4.2 上传示例 | 任务 5（验证脚本） | 已覆盖 |
| 4.3 下载示例 | 任务 5（验证脚本） | 已覆盖 |
| 4.4 删除示例 | 任务 5（验证脚本） | 已覆盖 |
| 6. 环境变量清单 | 任务 2、4 | 已覆盖 |

### 占位符扫描

- [x] 无 "TBD" / "TODO" / "稍后实现" / "填写细节"
- [x] 无 "添加适当的错误处理" 等模糊描述
- [x] 所有任务均含具体代码块与验证命令
- [x] 无 "类似于任务 N" 的重复引用

### 类型一致性

- [x] `upload` 签名接受 `ReadableStream<Uint8Array> | NodeJS.ReadableStream`，与 `IStorageProvider` 一致
- [x] `download` 返回 `Promise<ReadableStream<Uint8Array>>`，与 `IStorageProvider` 一致
- [x] `delete` 返回 `Promise<void>`，与 `IStorageProvider` 一致
- [x] `getUrl` 返回 `Promise<string>`，与 `IStorageProvider` 一致
- [x] 错误类使用 `StorageError` / `NotFoundError` / `ValidationError`，与 `errors.ts` 一致
- [x] 文件扩展名使用 `.js`（Node16 module resolution）

---

## 执行交接

**计划已保存到 `docs/04-plans/i-03-minio-client/v1.md`。两种执行方式：**

1. **子代理驱动（推荐）** — 每个任务新子代理，任务间审查
2. **内联执行** — 当前会话中顺序执行，带检查点

**选择哪种？**
