# 清理 ChatPage.vue 僵尸组件 实现计划

> **For agentic workers:** 使用 `subagent-driven-development`。步骤用复选框追踪。

**目标：** 删除未引用的 ChatPage.vue 遗留组件。

**架构：** 单文件删除——验证零引用后直接移除，无消费者需迁移。

**技术栈：** Vue 3 + TypeScript

**Issue 引用：** docs/02-issues/f-18-cleanup-chatpage.md
**Spec 引用：** docs/03-specs/f-18-cleanup-chatpage/
**测试用例：** docs/08-test-cases/f-18-cleanup-chatpage/

---

## 文件变更清单

| 操作 | 文件路径 |
|------|----------|
| 删除 | `packages/webui/src/components/ChatPage.vue` |

---

### 任务 1: 验证零引用 + 删除

- [ ] **步骤 1: 全局搜索引用**

```bash
grep -rn "ChatPage" packages/webui/src/
```
预期：无输出（已确认：grep 结果为空）。

- [ ] **步骤 2: 确认文件名精确匹配**

```bash
ls -la packages/webui/src/components/ChatPage.vue
```
预期：文件存在。

- [ ] **步骤 3: 删除文件**

```bash
rm packages/webui/src/components/ChatPage.vue
```

---

### 任务 2: 验证构建

- [ ] **步骤 1: TypeScript 类型检查**

```bash
cd packages/webui && npx vue-tsc --noEmit
```
预期：零错误。

- [ ] **步骤 2: Vite 构建**

```bash
cd packages/webui && npx vite build
```
预期：构建成功，无 ChatPage 相关警告。

- [ ] **步骤 3: 开发服务器启动**

```bash
pnpm dev:web
```
预期：启动正常，无报错。

- [ ] **步骤 4: 提交**

```bash
git add packages/webui/src/components/ChatPage.vue
git commit -m "refactor(f-18): remove unused ChatPage.vue legacy component

Zero references confirmed via grep. No consumers to migrate."
```
