# Milvus Client 封装实现计划

> **For agentic workers:** 必需子技能：`superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`。步骤使用复选框（`- [ ]`）语法追踪。

**目标：** 在 `@goferbot/server` 中封装 Milvus Client，实现 `IVectorStore` 接口，提供 Collection 管理、向量批量插入、ANN 搜索（带 kb_id 过滤）、向量删除能力，启动时完成连接检查与 Collection 自动创建。

**架构：** 基于 `@zilliz/milvus2-sdk-node`；实现类 `MilvusVectorStore` 满足 `IVectorStore`；配置从环境变量读取；所有错误包装为 `VectorStoreError`。

**技术栈：** `@zilliz/milvus2-sdk-node`、TypeScript

**Issue 引用：** [docs/02-issues/i-04-milvus-client.md](../../../02-issues/i-04-milvus-client.md)
**Spec 引用：** [docs/03-specs/features/milvus-client/](../../../03-specs/features/milvus-client/)
**测试用例：** [docs/08-test-cases/i-04/](../../../08-test-cases/i-04/)（待补充）

---

## 文件结构

```
packages/server/
├── package.json                          # 新增 @zilliz/milvus2-sdk-node 依赖
├── src/
│   ├── vector/
│   │   └── milvus.ts                     # MilvusVectorStore 实现
│   └── interfaces/
│       ├── IVectorStore.ts               # 已存在（VectorRecord / VectorSearchOptions / VectorSearchResult / IVectorStore）
│       └── errors.ts                     # 已存在（VectorStoreError）
├── .env.example                          # 新增 MILVUS_COLLECTION / MILVUS_VECTOR_DIM
└── scripts/
    └── test-milvus.ts                    # 连接与操作验证脚本（临时，可删）
```

---

## 任务列表

### 任务 1: 安装 `@zilliz/milvus2-sdk-node` 依赖

**文件：**
- 修改：`packages/server/package.json`
- 修改：`pnpm-lock.yaml`（由 pnpm 自动生成）

**规格引用：**
- 功能规格：[2.1 范围内 — Milvus Client 封装（基于 `@zilliz/milvus2-sdk-node`）]
- 行为规格：[1.1 服务启动检查]

- [ ] **步骤 1: 在 `packages/server` 目录下安装依赖**

```bash
cd packages/server && pnpm add @zilliz/milvus2-sdk-node
```

- [ ] **步骤 2: 验证安装结果**

运行：`cat packages/server/package.json | grep -A 15 '"dependencies"'`
预期：`@zilliz/milvus2-sdk-node` 出现在 `dependencies` 中。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/package.json pnpm-lock.yaml
pnpm install --frozen-lockfile
git add pnpm-lock.yaml
git commit -m "deps(server): 安装 @zilliz/milvus2-sdk-node"
```

---

### 任务 2: 更新 `.env.example`（补充 Milvus Collection 与维度配置）

**文件：**
- 修改：`packages/server/.env.example`（若不存在则修改根目录 `.env.example`）

**规格引用：**
- 功能规格：[6. 配置项 — MILVUS_COLLECTION / MILVUS_VECTOR_DIM]
- 行为规格：[1.1 服务启动检查]

- [ ] **步骤 1: 在根目录 `.env.example` 追加 Milvus Collection 配置**

修改 `D:\projects\ai-stared-project\knowledge-base\.env.example`，在 `# --- Milvus ---` 区块追加：

```env
# --- Milvus ---
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_METRIC_PORT=9091
MILVUS_COLLECTION=knowledge_chunks
MILVUS_VECTOR_DIM=1536
```

- [ ] **步骤 2: 验证格式**

运行：`grep -E '^(MILVUS_COLLECTION|MILVUS_VECTOR_DIM)' .env.example`
预期：输出两行配置。

- [ ] **步骤 3: 提交**

```bash
git add .env.example
git commit -m "chore(env): 添加 MILVUS_COLLECTION 与 MILVUS_VECTOR_DIM 环境变量示例"
```

---

### 任务 3: 创建 `packages/server/src/vector/milvus.ts` — MilvusVectorStore 实现

**文件：**
- 创建：`packages/server/src/vector/milvus.ts`

**规格引用：**
- 功能规格：[3. 涉及组件 — MilvusClient 实现]
- 行为规格：[1.1 服务启动检查]、[1.2 ensureCollection]、[2.1 insertVectors]、[3.1 searchVectors]、[4.1 deleteByIds]
- API 规格：[1. 接口定义 — MilvusVectorStore]

- [ ] **步骤 1: 编写完整实现文件**

```typescript
import { MilvusClient, DataType, type RowData } from '@zilliz/milvus2-sdk-node';
import type {
  IVectorStore,
  VectorRecord,
  VectorSearchOptions,
  VectorSearchResult,
} from '../interfaces/IVectorStore.js';
import { VectorStoreError } from '../interfaces/errors.js';

export interface MilvusVectorStoreOptions {
  /** Milvus 服务地址，默认 localhost */
  host?: string;
  /** Milvus gRPC 端口，默认 19530 */
  port?: string | number;
  /** Collection 名称，默认 'knowledge_chunks' */
  collectionName?: string;
  /** 向量维度，默认 1536 */
  vectorDim?: number;
  /** 相似度度量，默认 'COSINE' */
  metricType?: 'COSINE' | 'IP' | 'L2';
}

export class MilvusVectorStore implements IVectorStore {
  private client: MilvusClient;
  private readonly collectionName: string;
  private readonly vectorDim: number;
  private readonly metricType: 'COSINE' | 'IP' | 'L2';

  constructor(options: MilvusVectorStoreOptions = {}) {
    const host = options.host ?? process.env.MILVUS_HOST ?? 'localhost';
    const port = options.port ?? process.env.MILVUS_PORT ?? '19530';
    this.collectionName = options.collectionName ?? process.env.MILVUS_COLLECTION ?? 'knowledge_chunks';
    this.vectorDim = options.vectorDim ?? Number(process.env.MILVUS_VECTOR_DIM ?? 1536);
    this.metricType = options.metricType ?? 'COSINE';

    this.client = new MilvusClient({
      address: `${host}:${port}`,
    });
  }

  /** 启动时检查连接健康 */
  async checkHealth(): Promise<void> {
    try {
      const res = await this.client.checkHealth();
      if (!res.isHealthy) {
        throw new VectorStoreError('Milvus 连接失败: 服务未就绪');
      }
      console.log(`[Milvus] Connected to ${this.client.clientConfig.address}`);
    } catch (err: any) {
      throw new VectorStoreError(`Milvus 连接失败: ${err?.message ?? err}`, err);
    }
  }

  /** 幂等地创建/校验 collection */
  async ensureCollection(): Promise<void> {
    try {
      const has = await this.client.hasCollection({
        collection_name: this.collectionName,
      });

      if (!has.value) {
        await this.client.createCollection({
          collection_name: this.collectionName,
          fields: [
            {
              name: 'id',
              data_type: DataType.VarChar,
              is_primary_key: true,
              max_length: 64,
            },
            {
              name: 'chunk_id',
              data_type: DataType.VarChar,
              max_length: 64,
            },
            {
              name: 'kb_id',
              data_type: DataType.VarChar,
              max_length: 64,
            },
            {
              name: 'file_id',
              data_type: DataType.VarChar,
              max_length: 64,
            },
            {
              name: 'embedding',
              data_type: DataType.FloatVector,
              dim: this.vectorDim,
            },
          ],
        });

        await this.client.createIndex({
          collection_name: this.collectionName,
          field_name: 'embedding',
          index_type: 'AUTOINDEX',
          metric_type: this.metricType,
        });

        await this.client.createIndex({
          collection_name: this.collectionName,
          field_name: 'kb_id',
          index_type: 'Trie',
        });

        await this.client.loadCollection({
          collection_name: this.collectionName,
        });

        console.log(`[Milvus] Collection '${this.collectionName}' created (dim=${this.vectorDim})`);
        return;
      }

      // 已存在：校验维度并确保加载
      const desc = await this.client.describeCollection({
        collection_name: this.collectionName,
      });

      const embField = desc.schema.fields.find((f) => f.name === 'embedding');
      const actualDim = embField?.type_params?.find((p) => p.key === 'dim')?.value;
      if (actualDim && Number(actualDim) !== this.vectorDim) {
        throw new VectorStoreError(
          `Collection 维度不匹配: 期望 ${this.vectorDim}, 实际 ${actualDim}`
        );
      }

      await this.client.loadCollection({
        collection_name: this.collectionName,
      });

      console.log(`[Milvus] Collection '${this.collectionName}' already exists, verified`);
    } catch (err: any) {
      if (err instanceof VectorStoreError) throw err;
      throw new VectorStoreError(`ensureCollection 失败: ${err?.message ?? err}`, err);
    }
  }

  /** 批量插入向量 */
  async insertVectors(vectors: VectorRecord[]): Promise<void> {
    if (!vectors.length) {
      throw new VectorStoreError('insertVectors 参数不能为空数组');
    }
    if (vectors.length > 1000) {
      throw new VectorStoreError('单次插入不得超过 1000 条');
    }

    for (const v of vectors) {
      if (v.embedding.length !== this.vectorDim) {
        throw new VectorStoreError(
          `维度不匹配: 期望 ${this.vectorDim}, 实际 ${v.embedding.length}`
        );
      }
    }

    const fields_data: RowData[] = [
      { field_name: 'id', type: DataType.VarChar, data: vectors.map((v) => v.id) },
      { field_name: 'chunk_id', type: DataType.VarChar, data: vectors.map((v) => v.chunkId) },
      { field_name: 'kb_id', type: DataType.VarChar, data: vectors.map((v) => v.kbId) },
      { field_name: 'file_id', type: DataType.VarChar, data: vectors.map((v) => v.fileId) },
      {
        field_name: 'embedding',
        type: DataType.FloatVector,
        data: vectors.map((v) => v.embedding),
      },
    ];

    try {
      const res = await this.client.insert({
        collection_name: this.collectionName,
        fields_data,
      });
      if (res.status.error_code !== 'Success') {
        throw new VectorStoreError(`向量插入失败: ${res.status.reason}`);
      }
      console.log(`[Milvus] Inserted ${vectors.length} vectors into '${this.collectionName}'`);
    } catch (err: any) {
      if (err instanceof VectorStoreError) throw err;
      throw new VectorStoreError(`向量插入失败: ${err?.message ?? err}`, err);
    }
  }

  /** ANN 搜索 */
  async searchVectors(
    queryVector: number[],
    options?: VectorSearchOptions
  ): Promise<VectorSearchResult[]> {
    if (queryVector.length !== this.vectorDim) {
      throw new VectorStoreError(
        `维度不匹配: 期望 ${this.vectorDim}, 实际 ${queryVector.length}`
      );
    }

    const topK = options?.topK ?? 5;
    const filterParts: string[] = [];
    if (options?.filter?.kbId) {
      filterParts.push(`kb_id == "${options.filter.kbId}"`);
    }
    const expr = filterParts.length ? filterParts.join(' && ') : undefined;

    try {
      const res = await this.client.search({
        collection_name: this.collectionName,
        vector: queryVector,
        filter: expr,
        limit: topK,
        output_fields: ['chunk_id'],
      });

      if (res.status.error_code !== 'Success') {
        throw new VectorStoreError(`搜索失败: ${res.status.reason}`);
      }

      const results: VectorSearchResult[] = (res.results ?? []).map((r: any) => ({
        id: String(r.id),
        chunkId: String(r.chunk_id),
        score: Number(r.score),
      }));

      results.sort((a, b) => b.score - a.score);

      console.log(
        `[Milvus] Searched '${this.collectionName}', topK=${topK}, filter=${expr ?? 'none'}, results=${results.length}`
      );
      return results;
    } catch (err: any) {
      if (err instanceof VectorStoreError) throw err;
      throw new VectorStoreError(`搜索失败: ${err?.message ?? err}`, err);
    }
  }

  /** 按 Milvus 主键删除 */
  async deleteByIds(ids: string[]): Promise<void> {
    if (!ids.length) {
      throw new VectorStoreError('deleteByIds 参数不能为空数组');
    }

    const quoted = ids.map((id) => `"${id}"`).join(',');
    const expr = `id in [${quoted}]`;

    try {
      const res = await this.client.delete({
        collection_name: this.collectionName,
        expr,
      });
      if (res.status.error_code !== 'Success') {
        throw new VectorStoreError(`向量删除失败: ${res.status.reason}`);
      }
      console.log(`[Milvus] Deleted vectors from '${this.collectionName}', expr=${expr}`);
    } catch (err: any) {
      if (err instanceof VectorStoreError) throw err;
      throw new VectorStoreError(`向量删除失败: ${err?.message ?? err}`, err);
    }
  }

  /** 按 file_id 删除（实现类扩展） */
  async deleteByFileId(fileId: string): Promise<void> {
    const expr = `file_id == "${fileId}"`;
    try {
      const res = await this.client.delete({
        collection_name: this.collectionName,
        expr,
      });
      if (res.status.error_code !== 'Success') {
        throw new VectorStoreError(`向量删除失败: ${res.status.reason}`);
      }
      console.log(`[Milvus] Deleted vectors by file_id from '${this.collectionName}', expr=${expr}`);
    } catch (err: any) {
      if (err instanceof VectorStoreError) throw err;
      throw new VectorStoreError(`向量删除失败: ${err?.message ?? err}`, err);
    }
  }

  /** 按 kb_id 删除（实现类扩展） */
  async deleteByKbId(kbId: string): Promise<void> {
    const expr = `kb_id == "${kbId}"`;
    try {
      const res = await this.client.delete({
        collection_name: this.collectionName,
        expr,
      });
      if (res.status.error_code !== 'Success') {
        throw new VectorStoreError(`向量删除失败: ${res.status.reason}`);
      }
      console.log(`[Milvus] Deleted vectors by kb_id from '${this.collectionName}', expr=${expr}`);
    } catch (err: any) {
      if (err instanceof VectorStoreError) throw err;
      throw new VectorStoreError(`向量删除失败: ${err?.message ?? err}`, err);
    }
  }
}
```

- [ ] **步骤 2: 验证类型检查通过**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 3: 提交**

```bash
git add packages/server/src/vector/milvus.ts
git commit -m "feat(vector): 实现 MilvusVectorStore，覆盖 IVectorStore 全部方法"
```

---

### 任务 4: 创建测试脚本验证 Milvus 连接与 CRUD

**文件：**
- 创建：`packages/server/scripts/test-milvus.ts`

**规格引用：**
- 行为规格：[1.1 服务启动检查]、[2.1 insertVectors]、[3.1 searchVectors]、[4.1 deleteByIds]
- Issue 验收标准：启动时检查 Milvus 连接，失败时给出明确错误

- [ ] **步骤 1: 编写测试脚本**

```typescript
import { MilvusVectorStore } from '../src/vector/milvus.js';
import { VectorStoreError } from '../src/interfaces/errors.js';

async function main() {
  const store = new MilvusVectorStore({
    collectionName: 'test_knowledge_chunks',
    vectorDim: 4,
  });

  // 1. 连接检查
  console.log('--- 1. checkHealth ---');
  await store.checkHealth();

  // 2. 创建/校验 collection
  console.log('--- 2. ensureCollection ---');
  await store.ensureCollection();

  // 3. 插入向量
  console.log('--- 3. insertVectors ---');
  await store.insertVectors([
    {
      id: 'vec-001',
      chunkId: 'chunk-001',
      kbId: 'kb-001',
      fileId: 'file-001',
      embedding: [1, 0, 0, 0],
    },
    {
      id: 'vec-002',
      chunkId: 'chunk-002',
      kbId: 'kb-001',
      fileId: 'file-001',
      embedding: [0, 1, 0, 0],
    },
    {
      id: 'vec-003',
      chunkId: 'chunk-003',
      kbId: 'kb-002',
      fileId: 'file-002',
      embedding: [0, 0, 1, 0],
    },
  ]);

  // 4. 搜索（无过滤）
  console.log('--- 4. searchVectors (no filter) ---');
  const resultsAll = await store.searchVectors([1, 0, 0, 0], { topK: 3 });
  console.log(resultsAll);

  // 5. 搜索（带 kb_id 过滤）
  console.log('--- 5. searchVectors (kbId filter) ---');
  const resultsFiltered = await store.searchVectors([1, 0, 0, 0], {
    topK: 3,
    filter: { kbId: 'kb-001' },
  });
  console.log(resultsFiltered);

  // 6. 删除指定 ID
  console.log('--- 6. deleteByIds ---');
  await store.deleteByIds(['vec-003']);

  // 7. 再次搜索确认删除
  console.log('--- 7. search after delete ---');
  const resultsAfterDelete = await store.searchVectors([0, 0, 1, 0], { topK: 3 });
  console.log(resultsAfterDelete);

  // 8. 清理 collection
  console.log('--- 8. cleanup ---');
  const { MilvusClient } = await import('@zilliz/milvus2-sdk-node');
  const client = new MilvusClient({
    address: `${process.env.MILVUS_HOST ?? 'localhost'}:${process.env.MILVUS_PORT ?? '19530'}`,
  });
  await client.dropCollection({ collection_name: 'test_knowledge_chunks' });
  console.log('测试完成，临时 collection 已清理');
}

main().catch((err) => {
  if (err instanceof VectorStoreError) {
    console.error('[VectorStoreError]', err.message);
  } else {
    console.error(err);
  }
  process.exit(1);
});
```

- [ ] **步骤 2: 确保 Milvus 服务运行**

运行：`docker compose -f docker-compose.dev.yml ps milvus`
预期：状态为 `running (healthy)`。

若未运行：
```bash
docker compose -f docker-compose.dev.yml up -d milvus
```

- [ ] **步骤 3: 执行测试脚本**

运行：`cd packages/server && npx tsx scripts/test-milvus.ts`
预期：
- 输出 `Connected to localhost:19530`
- 输出 `Collection 'test_knowledge_chunks' created (dim=4)`
- 插入 3 条向量成功
- 搜索结果按 score 降序排列
- kbId 过滤仅返回 `kb-001` 的结果
- 删除 `vec-003` 后再次搜索无该记录
- 最后输出 `测试完成，临时 collection 已清理`

- [ ] **步骤 4: 提交（可选，脚本为临时验证文件）**

```bash
git add packages/server/scripts/test-milvus.ts
git commit -m "test(vector): 添加 Milvus 连接与 CRUD 验证脚本"
```

---

### 任务 5: 验证 `pnpm type-check` 与 `pnpm dev:server`（或 `pnpm dev`）

**文件：**
- 无文件变更（纯验证）

**规格引用：**
- Issue 验收标准：提供类型安全的接口定义；启动时检查 Milvus 连接，失败时给出明确错误

- [ ] **步骤 1: 类型检查**

运行：`cd packages/server && pnpm type-check`
预期：无错误。

- [ ] **步骤 2: 启动服务端（确认无编译/启动错误）**

运行：`cd packages/server && pnpm dev`
预期：
- 进程启动，无 TypeScript 编译错误；
- 若 Milvus 未运行，终端应抛出 `VectorStoreError: Milvus 连接失败: ...` 并退出；
- 若 Milvus 运行正常，应输出 `[Milvus] Connected to ...` 与 `[Milvus] Collection '...' already exists, verified`。

> 注：当前 `src/index.ts` 可能尚未调用 `MilvusVectorStore`，此步骤仅验证 `milvus.ts` 本身不会导致编译或运行时模块加载错误。后续集成到启动流程时，再在 `src/index.ts` 中引入 `checkHealth` + `ensureCollection`。

- [ ] **步骤 3: 停止进程**

按 `Ctrl+C` 终止。

---

## 规格覆盖检查

### 功能规格覆盖

| 功能规格条目 | 对应任务 | 状态 |
|---|---|---|
| Milvus Client 封装（基于 `@zilliz/milvus2-sdk-node`） | 任务 1、3 | 已覆盖 |
| Collection 管理（创建、存在性检查、结构校验） | 任务 3（ensureCollection） | 已覆盖 |
| 向量批量插入 | 任务 3（insertVectors） | 已覆盖 |
| ANN 近似最近邻搜索（带 kb_id 过滤） | 任务 3（searchVectors） | 已覆盖 |
| 向量删除（按 Milvus 主键） | 任务 3（deleteByIds） | 已覆盖 |
| 启动时连接检查与明确错误提示 | 任务 3（checkHealth）、任务 5 | 已覆盖 |
| 与 PostgreSQL chunks 表的关联映射 | 任务 3（chunk_id 字段） | 已覆盖 |
| 配置项（host、port、collection、dim） | 任务 2、3（构造函数） | 已覆盖 |

### 行为规格覆盖

| 行为规格条目 | 对应任务 | 状态 |
|---|---|---|
| 1.1 服务启动检查（checkHealth） | 任务 3、4、5 | 已覆盖 |
| 1.2 ensureCollection（幂等、创建、校验、加载） | 任务 3、4 | 已覆盖 |
| 2.1 insertVectors（校验、分批限制、字段格式） | 任务 3、4 | 已覆盖 |
| 3.1 searchVectors（维度校验、过滤、降序） | 任务 3、4 | 已覆盖 |
| 4.1 deleteByIds（非空校验、表达式构造） | 任务 3、4 | 已覆盖 |
| 4.2 按 document_id 删除（deleteByFileId） | 任务 3 | 已覆盖 |
| 5. 错误场景（连接失败、维度不匹配、超时等） | 任务 3、4、5 | 已覆盖 |
| 6. 生命周期（启动 → 运行时） | 任务 3、5 | 已覆盖 |
| 7. 与 chunks 表协作时序（chunk_id 关联） | 任务 3 | 已覆盖 |
| 8. 日志与可观测性 | 任务 3（console.log） | 已覆盖 |

### API 规格覆盖

| API 规格条目 | 对应任务 | 状态 |
|---|---|---|
| 1. 接口定义（MilvusVectorStore 实现 IVectorStore） | 任务 3 | 已覆盖 |
| 2. 构造函数选项（host / port / collectionName / vectorDim / metricType） | 任务 3 | 已覆盖 |
| 3.1 ensureCollection | 任务 3 | 已覆盖 |
| 3.2 insertVectors | 任务 3 | 已覆盖 |
| 3.3 searchVectors | 任务 3 | 已覆盖 |
| 3.4 deleteByIds | 任务 3 | 已覆盖 |
| 4.1 deleteByFileId（扩展） | 任务 3 | 已覆盖 |
| 4.2 deleteByKbId（扩展） | 任务 3 | 已覆盖 |
| 5. 类型对照表（VarChar / FloatVector） | 任务 3 | 已覆盖 |
| 6. 无 REST API | 任务 3（纯内部服务） | 已覆盖 |

### 占位符扫描

- [x] 无 "TBD" / "TODO" / "稍后实现" / "填写细节"
- [x] 无 "添加适当的错误处理" 等模糊描述
- [x] 所有任务均含具体代码块与验证命令
- [x] 无 "类似于任务 N" 的重复引用

### 类型一致性

- [x] `MilvusVectorStore` 显式实现 `IVectorStore` 接口
- [x] `VectorRecord` / `VectorSearchOptions` / `VectorSearchResult` 类型复用 `IVectorStore.ts` 定义
- [x] 所有错误均包装为 `VectorStoreError`，保留原始 `cause`
- [x] 使用 `.js` 扩展名导入，兼容 `Node16` module resolution

---

## 执行交接

**计划已保存到 `docs/04-plans/milvus-client/2026-05-16.md`。两种执行方式：**

1. **子代理驱动（推荐）** — 每个任务新子代理，任务间审查
2. **内联执行** — 当前会话中顺序执行，带检查点

**选择哪种？**
