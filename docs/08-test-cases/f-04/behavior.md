---
issue_id: f-04
type: test-case
kind: behavior
tc_count: 12
status: drafted
summary: 测试聊天页面多标签栏行为：初始状态、新建/切换/关闭/重命名标签、首页标签不可关闭特性、横向滚动溢出、标签栏与消息区域联动交互。
---

# 测试用例：聊天页面标签栏 (f-04)

| TC-ID | 场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|-------|------|----------|----------|----------|--------|
| TC-F04-001 | 初始状态 | 用户已登录，进入 Chat 页面 | 1. 从 Sidebar 点击"消息"进入 `/` | 仅显示"首页"标签，处于激活状态，下方显示空会话态 | P0 |
| TC-F04-002 | 新建标签 | 在 Chat 页面 | 1. 点击标签栏右侧 `+` 按钮 | 创建新会话标签，标题"新会话"，自动激活，下方显示空会话态 | P0 |
| TC-F04-003 | 切换标签 | 存在多个标签 | 1. 点击非当前标签 | 被点击标签高亮，消息区域显示对应会话内容 | P0 |
| TC-F04-004 | 关闭标签 | 存在可关闭标签 | 1. 悬停标签显示关闭按钮<br>2. 点击关闭按钮 | 标签消失，自动激活左侧相邻标签 | P0 |
| TC-F04-005 | 关闭当前标签 | 当前标签非"首页" | 1. 关闭当前激活标签 | 标签消失，切换到左侧相邻标签，消息区域更新 | P0 |
| TC-F04-006 | 关闭最后一个可关闭标签 | 仅剩"首页"和一个可关闭标签 | 1. 关闭可关闭标签 | 切换到"首页"，消息区域清空 | P1 |
| TC-F04-007 | 首页不可关闭 | 在 Chat 页面 | 1. 悬停"首页"标签 | 不显示关闭按钮 | P0 |
| TC-F04-008 | 重命名标签 | 标签处于普通状态 | 1. 双击标签标题<br>2. 输入新标题<br>3. 按 Enter | 标题更新为新值，调用 API 保存 | P1 |
| TC-F04-009 | 取消重命名 | 标签处于编辑状态 | 1. 双击标签进入编辑<br>2. 按 Escape | 标题恢复为原值 | P1 |
| TC-F04-010 | 新建会话失败 | 网络异常或 API 错误 | 1. 点击 `+` 按钮 | 不创建标签，显示错误 Toast | P1 |
| TC-F04-011 | 标签横向滚动 | 标签数量超过可视区域 | 1. 创建多个标签直到超出宽度 | 标签栏可横向滚动 | P2 |
| TC-F04-012 | 首页发送消息 | 在"首页"标签下 | 1. 输入消息并发送 | 创建新会话，首页标签的 sessionId 更新为新会话 | P0 |
