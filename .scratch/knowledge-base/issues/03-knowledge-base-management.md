Status: closed
Category: enhancement

## What to build

实现知识库的 CRUD 管理和文件导入功能。用户可以创建知识库（物理目录）、导入文件、浏览文件层级、搜索文件名、删除和恢复知识库。

端到端行为：用户点击左侧文件夹图标 → 打开知识库管理页（单例标签）→ 左侧显示一级知识库列表（图标+名称），点击"新建"输入名称后创建物理目录 `docs/<名称>/` → 右侧显示资源管理器视图（当前知识库的文件和文件夹）→ 顶部工具栏有面包屑导航、搜索框、排序下拉（名称/日期）、添加文件按钮 → 用户点击"添加文件"→ Rust 打开系统文件对话框 → Rust 读取选中文件 → Rust HTTP POST 到 sidecar → sidecar 保存到当前知识库目录 → 前端刷新文件列表 → 双击文件夹进入子目录，面包屑更新 → 在搜索框输入文件名 → 显示跨目录扁平搜索结果 → 双击结果中的文件夹进入对应目录 → 面包屑支持回退/前进（含搜索状态）→ 用户删除知识库 → 物理移动到 `.trash/<名称>-<timestamp>/` → 用户可恢复，若同名冲突则重命名为"<名称>-副本"。

## Acceptance criteria

- [x] SQLite Schema：`knowledge_bases` 表（`id`, `name`, `path`, `created_at`）
- [x] Sidecar 知识库 CRUD API：`GET /knowledge-bases`, `POST /knowledge-bases`, `DELETE /knowledge-bases/:id`
- [x] Sidecar 文件列表 API：`GET /knowledge-bases/:id/files?path=...`（返回当前目录层级）
- [x] 前端知识库管理页：左侧知识库列表 + 右侧资源管理器视图
- [x] 新建知识库：输入名称，创建物理目录，SQLite 插入记录
- [x] 文件导入链路：前端调用 Rust IPC → Rust 打开对话框 + 读取文件 → Rust HTTP POST 到 sidecar → sidecar 保存到目标路径
- [x] 资源管理器：双击文件夹进入子目录，面包屑导航
- [x] 文件名搜索：跨目录扁平结果，双击文件夹进入对应目录
- [x] 面包屑回退/前进：维护导航历史栈（浏览状态 + 搜索状态）
- [x] 知识库删除：物理移动至 `.trash/<名称>-<timestamp>/`
- [x] 知识库恢复：移回原位，同名冲突重命名为"<名称>-副本"
- [x] 第一版不实现 30 天自动清理

## Blocked by

- [01-sidecar-startup](../01-sidecar-startup.md) — 必须先完成 sidecar 启动与前端就绪机制

## Comments

> *This was generated by AI during triage.*

**2026-05-08 — 已完成**
- 实现知识库 CRUD API（GET / POST / DELETE / restore）
- 实现文件列表、导入、搜索 API
- 实现前端 KnowledgeBasePage + FileExplorer 组件
- 实现 Pinia useKnowledgeBaseStore（历史栈、面包屑、搜索状态管理）
- 实现 Rust IPC `import_files` 文件导入链路
- 实现回收站机制（物理移动 + 恢复 + 同名冲突重命名）
- 验收标准全部达成
- 补充完整测试用例文档：`docs/test-cases/issues-01-02-03-test-cases.md`
- 已有自动化测试覆盖：KnowledgeBase API（`tests/unit/server/knowledgeBases.test.ts`）、useKnowledgeBaseStore（`src/stores/knowledgeBase.spec.ts`）

## Agent Brief

**Category:** enhancement
**Summary:** 实现知识库的 CRUD 管理和文件导入功能，包括资源管理器视图、面包屑导航和回收站机制。

**Current behavior:**
项目尚无知识库概念，没有文件存储、目录浏览或导入功能。

**Desired behavior:**
用户可通过知识库管理页创建知识库（对应物理目录）、导入文件、浏览文件层级、搜索文件名。删除的知识库移入 `.trash/` 并支持恢复。

**Key interfaces:**
- `GET /knowledge-bases` / `POST /knowledge-bases` / `DELETE /knowledge-bases/:id` — sidecar API
- `GET /knowledge-bases/:id/files?path=...` — sidecar API，返回当前目录层级
- `knowledge_bases` 表 — SQLite schema，字段：`id`, `name`, `path`, `created_at`
- 前端知识库管理页 — 左侧知识库列表 + 右侧资源管理器视图
- 文件导入链路 — 前端调用 Rust IPC → Rust 打开对话框 + 读取文件 → Rust HTTP POST 到 sidecar → sidecar 保存到目标路径

**Acceptance criteria:**
- [x] SQLite Schema：`knowledge_bases` 表
- [x] Sidecar 知识库 CRUD API
- [x] Sidecar 文件列表 API
- [x] 前端知识库管理页：左侧知识库列表 + 右侧资源管理器视图
- [x] 新建知识库：输入名称，创建物理目录，SQLite 插入记录
- [x] 文件导入链路：前端调用 Rust IPC → Rust 打开对话框 + 读取文件 → Rust HTTP POST 到 sidecar → sidecar 保存到目标路径
- [x] 资源管理器：双击文件夹进入子目录，面包屑导航
- [x] 文件名搜索：跨目录扁平结果，双击文件夹进入对应目录
- [x] 面包屑回退/前进：维护导航历史栈（浏览状态 + 搜索状态）
- [x] 知识库删除：物理移动至 `.trash/<名称>-<timestamp>/`
- [x] 知识库恢复：移回原位，同名冲突重命名为"<名称>-副本"

**Out of scope:**
- RAG 索引与检索（由 #04 负责）
- 30 天自动清理（第一版不实现）
